# CSP 2022 入门级第一轮

- 来源：洛谷有题
- URL：https://ti.luogu.com.cn/problemset/1039
- 年份：2022
- 组别：CSP-J1
- 题目数：44

## 第 1 题

【来源：洛谷有题；试卷：1039；原题：1；洛谷题目ID：1036】

## 一、单项选择题（共 15 题，每题 2 分，共计 30 分；每题有且仅有一个正确选项）

以下哪种功能没有涉及 C++ 语言的面向对象特性支持：（ ）。

A. C++ 中调用 `printf` 函数

B. C++ 中调用用户定义的类成员函数

C. C++ 中构造一个 `class` 或 `struct`

D. C++ 中构造来源于同一基类的多个派生类

答案：A

## 第 2 题

【来源：洛谷有题；试卷：1039；原题：2；洛谷题目ID：1037】

有 $6$ 个元素，按照 $6,5,4,3,2,1$ 的顺序进入栈 $S$，请问下列哪个出栈序列是非法的（ ）。

A. $5,4,3,6,1,2$

B. $4,5,3,1,2,6$

C. $3,4,6,5,2,1$

D. $2,3,4,1,5,6$

答案：C

## 第 3 题

【来源：洛谷有题；试卷：1039；原题：3；洛谷题目ID：1038】

运行以下代码片段的行为是（ ）。
```
int x = 101;
int y = 201;
int *p = &x;
int *q = &y;
p = q;
```

A. 将 $x$ 的值赋为 $201$

B. 将 $y$ 的值赋为 $101$

C. 将 $q$ 指向 $x$ 的地址

D. 将 $p$ 指向 $y$ 的地址

答案：D

## 第 4 题

【来源：洛谷有题；试卷：1039；原题：4；洛谷题目ID：1039】

链表和数组的区别包括（ ）。

A. 数组不能排序，链表可以

B. 链表比数组能存储更多的信息

C. 数组大小固定，链表大小可动态调整

D. 以上均正确

答案：C

## 第 5 题

【来源：洛谷有题；试卷：1039；原题：5；洛谷题目ID：1040】

对假设栈 $S$ 和队列 $Q$ 的初始状态为空。存在 $e_1\sim e_6$ 六个互不相同的数据，每个数据按照进栈 $S$、出栈 $S$、进队列 $Q$、出队列 $Q$ 的顺序操作，不同数据间的操作可能会交错。已知栈 $S$ 中依次有数据 $e_1$、$e_2$、$e_3$、$e_4$、$e_5$ 和 $e_6$ 进栈，队列 $Q$ 依次有数据 $e_2$、$e_4$、$e_3$、$e_6$、$e_5$ 和 $e_1$ 出队列。则栈 $S$ 的容量至少是（ ）个数据。

A. $2$

B. $3$

C. $4$

D. $6$

答案：B

## 第 6 题

【来源：洛谷有题；试卷：1039；原题：6；洛谷题目ID：1041】

对表达式 `a+(b-c)*d` 的前缀表达式为（ ），其中 +、-、* 是运算符。

A. `*+a-bcd`

B. `+a*-bcd`

C. `abc-d*+`

D. `abc-+d`

答案：B

## 第 7 题

【来源：洛谷有题；试卷：1039；原题：7；洛谷题目ID：1042】

假设字母表 $\{a,b,c,d,e\}$ 在字符串出现的频率分别为 $10\%$，$15\%$，$30\%$，$16\%$，$29\%$。若使用哈夫曼编码方式对字母进行不定长的二进制编码，字母 $d$ 的编码长度（ ）位。

A. $1$

B. $2$

C. $2$ 或 $3$

D. $3$

答案：B

## 第 8 题

【来源：洛谷有题；试卷：1039；原题：8；洛谷题目ID：1043】

一棵有 $n$ 个结点的完全二叉树用数组进行存储与表示，已知根结点存储在数组的第 $1$ 个位置。若存储在数组第 $9$ 个位置的结点存在兄弟结点和两个子结点，则它的兄弟结点和右子结点的位置分别是（ ）。

A. $8$、$18$

B. $10$、$18$

C. $8$、$19$

D. $10$、$19$

答案：C

## 第 9 题

【来源：洛谷有题；试卷：1039；原题：9；洛谷题目ID：1044】

考虑由 $N$ 个顶点构成的有向连通图，采用邻接矩阵的数据结构表示时，该矩阵中至少存在（ ）个非零元素。

A. $N-1$

B. $N$

C. $N+1$

D. $N^2$

答案：B

## 第 10 题

【来源：洛谷有题；试卷：1039；原题：10；洛谷题目ID：1045】

以下对数据结构的表述不恰当的一项为：（ ）。

A. 图的深度优先遍历算法常使用的数据结构为栈。

B. 栈的访问原则后进先出，队列的访问原则是先进先出。

C. 队列常常被用于广度优先搜索算法。

D. 栈与队列存在本质不同，无法用栈实现队列。

答案：D

## 第 11 题

【来源：洛谷有题；试卷：1039；原题：11；洛谷题目ID：1046】

以下哪组操作能完成在双向循环链表结点 $p$ 之后插入结点 $s$ 的效果（其中，`next` 域为结点的直接后继，`prev` 域为结点的直接前驱）：（ ）。

A. `p->next->prev=s; s->prev=p; p->next=s; s->next=p->next;`

B. `p->next->prev=s; p->next=s; s->prev=p; s->next=p->next;`

C. `s->prev=p; s->next=p->next; p->next=s; p->next->prev=s;`

D. `s->next=p->next; p->next->prev=s; s->prev=p; p->next=s;`

答案：D

## 第 12 题

【来源：洛谷有题；试卷：1039；原题：12；洛谷题目ID：1047】

以下排序算法的常见实现中，哪个选项的说法是错误的：（ ）。

A. 冒泡排序算法是稳定的

B. 简单选择排序是稳定的

C. 简单插入排序是稳定的

D. 归并排序算法是稳定的

答案：B

## 第 13 题

【来源：洛谷有题；试卷：1039；原题：13；洛谷题目ID：1048】

八进制数 $32.1$ 对应的十进制数是（ ）。

A. $24.125$

B. $24.250$

C. $26.125$

D. $26.250$

答案：C

## 第 14 题

【来源：洛谷有题；试卷：1039；原题：14；洛谷题目ID：1049】

一个字符串中任意个连续的字符组成的子序列称为该字符串的子串，则字符串 $\tt abcab$ 有（ ）个内容互不相同的子串。

A. $12$

B. $13$

C. $14$

D. $15$

答案：B

## 第 15 题

【来源：洛谷有题；试卷：1039；原题：15；洛谷题目ID：1050】

以下对递归方法的描述中，正确的是：（ ）。

A. 递归是允许使用多组参数调用函数的编程技术

B. 递归是通过调用自身来求解问题的编程技术

C. 递归是面向对象和数据而不是功能和逻辑的编程语言模型

D. 递归是将用某种高级语言转换为机器代码的编程技术

答案：B

## 第 16 题

【来源：洛谷有题；试卷：1039；原题：16-1；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 1：当输入为 `13 8` 时，输出为

A. 正确

B. 错误

答案：A

## 第 17 题

【来源：洛谷有题；试卷：1039；原题：16-2；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 2

A. 正确

B. 错误

答案：B

## 第 18 题

【来源：洛谷有题；试卷：1039；原题：16-3；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 3

A. 正确

B. 错误

答案：B

## 第 19 题

【来源：洛谷有题；试卷：1039；原题：16-4；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 4

A. 正确

B. 错误

答案：B

## 第 20 题

【来源：洛谷有题；试卷：1039；原题：16-5；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 5

A. 正确

B. 错误

答案：B

## 第 21 题

【来源：洛谷有题；试卷：1039；原题：16-6；洛谷题目ID：1051】

## 二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √，错误填 ⨉ ；除特殊说明外，判断题 1.5 分，选择题 3 分，共计 40 分）

### (1)

```cpp
01 #include <iostream>
02
03 using namespace std；
04
05 int main()
06 {
07     unsigned short x, y；
08     cin >> x >> y；
09     x = (x | x << 2)& 0x33；
10     x = (x | x << 1)& 0x55；
11     y = (y | y << 2)& 0x33；
12     y = (y | y << 1)& 0x55；
13     unsigned short z = x | y << 1；
14     cout << z << endl；
15     return 0；
16 }
```

**假设输入的 $x,y$ 均是不超过 $15$ 的自然数，完成下面的判断题和单选题：**

**判断题**

1. 删去第 $7$ 行与第 $13$ 行的 unsigned，程序行为不变。
2. 将第 $7$ 行与第 $13$ 行的 short 均改为 char，程序行为不变。
3. 程序总是输出一个整数“0”。
4. 当输入为 `2 2` 时，输出为 `10`。
5. 当输入为 `2 2` 时，输出为 `59`。

**单选题**

6. 当输入为 `13 8` 时，输出为（ ）。

子题 6

A. $0$

B. $209$

C. $197$

D. $226$

答案：B

## 第 22 题

【来源：洛谷有题；试卷：1039；原题：17-1；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 1：算法 $g(n,m)$ 最为准确的时间复杂度分析结果为

A. 正确

B. 错误

答案：B

## 第 23 题

【来源：洛谷有题；试卷：1039；原题：17-2；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 2：当输入为 `20 2` 时，输出的第一行为

A. 正确

B. 错误

答案：A

## 第 24 题

【来源：洛谷有题；试卷：1039；原题：17-3；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 3：**（$4$ 分）** 当输入 `100 100` 时，输出的第一行为

A. 正确

B. 错误

答案：A

## 第 25 题

【来源：洛谷有题；试卷：1039；原题：17-4；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 4

A. $O(n^{3/2}m)$

B. $O(nm)$

C. $O(n^{2}m)$

D. $O(nm^{2})$

答案：C

## 第 26 题

【来源：洛谷有题；试卷：1039；原题：17-5；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 5

A. $4$

B. $5$

C. $6$

D. $20$

答案：C

## 第 27 题

【来源：洛谷有题；试卷：1039；原题：17-6；洛谷题目ID：1052】

### （2）
```
1  #include <algorithm>
2  #include <iostream>
3  #include <limits>
4
5  using namespace std;
6
7  const int MAXN = 105;
8  const int MAXK = 105;
9
10 int h[MAXN][MAXK];
11
12 int f(int n, int m)
13 {
14     if (m == 1) return n;
15     if (n == 0) return 0;
16
17     int ret = numeric_limits<int>::max();
18     for (int i = 1; i <= n; i++)
19         ret = min(ret, max(f(n - i,m), f(i - 1, m - 1)) + 1);
20     return ret;
21 }
22
23 int g(int n, int m)
24 {
25     for (int i = 1;i <= n; i++)
26         h[i][1]= i;
27     for (int j = 1;j<= m; j++)
28         h[0][j]= 0;
29
30     for (int i= 1; i <= n; i++){
31         for (int j= 2; j <= m; j++){
32             h[i][j] = numeric_limits<int>::max();
33             for (int k = 1;k <= i;k++)
34             h[i][j]= min(
35                 h[i][j],
36                 max(h[i - k][j],h[k - 1][j - 1]) +1);
37         }
38     }
39
40     return h[n][m];
41 }
42
43 int main()
44 {
45     int n,m;
46     cin >> n>> m;
47     cout << f(n, m) << endl << g(n, m)<< endl;
48     return 0;
49 }
```
**假设输入的n、m均是不超过100 的正整数，完成下面的判断题和单选题：**

**判断题**

1. 当输入为 `7 3` 时，第 $19$ 行用来取最小值的 `min` 函数执行了 $449$ 次。
2. 输出的两行整数总是相同的。
3. 当 $m$ 为 $1$ 时，输出的第一行总为 $n$。

**单选题**

4. 算法 $g(n,m)$ 最为准确的时间复杂度分析结果为（ ）。
5. 当输入为 `20 2` 时，输出的第一行为（ ）。
6. **（$4$ 分）** 当输入 `100 100` 时，输出的第一行为（ ）。

子题 6

A. $6$

B. $7$

C. $8$

D. $9$

答案：B

## 第 28 题

【来源：洛谷有题；试卷：1039；原题：18-1；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 1：当输入为 `2 1` 时，输出的第一个数最接近

A. 正确

B. 错误

答案：A

## 第 29 题

【来源：洛谷有题；试卷：1039；原题：18-2；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 2：当输入为 `3 10` 时，输出的第一个数最接近

A. 正确

B. 错误

答案：A

## 第 30 题

【来源：洛谷有题；试卷：1039；原题：18-3；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 3：当输入为 `256 11` 时，输出的第一个数

A. 正确

B. 错误

答案：B

## 第 31 题

【来源：洛谷有题；试卷：1039；原题：18-4；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 4

A. 正确

B. 错误

答案：B

## 第 32 题

【来源：洛谷有题；试卷：1039；原题：18-5；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 5

A. $1$

B. $1.414$

C. $1.5$

D. $2$

答案：C

## 第 33 题

【来源：洛谷有题；试卷：1039；原题：18-6；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 6

A. $1.7$

B. $1.732$

C. $1.75$

D. $2$

答案：B

## 第 34 题

【来源：洛谷有题；试卷：1039；原题：18-7；洛谷题目ID：1053】

### (3)

```
1  #include <iostream>
2
3  using namespace std;
4
5  int n,k;
6
7  int solve1()
8  {
9      int l = 0, r = n;
10     while(l <= r){
11         int mid = (l + r) / 2;
12         if (mid * mid <= n) l = mid + 1;
13         else r = mid - 1;
14     }
15     return l - 1;
16 }
17
18 double solve2(double x)
19 {
20         if (x == 0) return x;
21         for (int i = 0; i < k; i++)
22             x = (x + n / x) / 2;
23     return x;
24 }
25
26 int main()
27 {
28     cin >> n >> k;
29     double ans = solve2(solve1());
30     cout << ans << ' ' << (ans * ans == n) << endl;
31     return 0;
32 }
```

**假设 int 为32位有符号整数类型，输入的 n 是不超过47000的自然数、k 是不超过 int 表示范围的自然数，完成下面的判断题和单选题：**

**判断题**

1. 该算法最准确的时间复杂度分析结果为 $O(\log n+k)$。
2. 当输入为 `9801 1` 时，输出的第一个数为 `99`。
3. 对于任意输入的 $n$，随着所输入 $k$ 的增大，输出的第二个数会变成 $1$。
4. 该程序有存在缺陷。当输入的 $n$ 过大时，第 $12$ 行的乘法有可能溢出，因此应当将 `mid` 强制转换为 $64$ 位整数再计算。

**单选题**

5. 当输入为 `2 1` 时，输出的第一个数最接近（ ）。
6. 当输入为 `3 10` 时，输出的第一个数最接近（ ）。
7. 当输入为 `256 11` 时，输出的第一个数（ ）。

子题 7

A. 等于 $16$

B. 接近但小于 $16$

C. 接近但大于 $16$

D. 前三种情况都有可能

答案：A

## 第 35 题

【来源：洛谷有题；试卷：1039；原题：19-1；洛谷题目ID：1054】

## 三、完善程序（单选题，每小题 $3$ 分，共计 $30$ 分）

### （1）（枚举因数）从小到大打印正整数 $n$ 的所有正因数。

试补全枚举程序。

```
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> fac;
    fac.reserve((int)ceil(sqrt(n)));

    int i;
    for (i = 1; i * i < n; ++i){
        if (①){
            fac.push_back(i);
        }
    }

    for (int k = 0; k < fac.size(); ++k){
        cout << ② << "";
    }
    if (③) {
        cout << ④ << "";
    }
    for (int k = fac.size() - 1; k >= 0; --k){
        cout << ⑤ << "";
    }
}
```

①~⑤处应填（ ）

子题 1

A. `n % i == 0`

B. `n % i == 1`

C. `n % (i-1) == 0`

D. `n % (i-1) == 1`

答案：A

## 第 36 题

【来源：洛谷有题；试卷：1039；原题：19-2；洛谷题目ID：1054】

## 三、完善程序（单选题，每小题 $3$ 分，共计 $30$ 分）

### （1）（枚举因数）从小到大打印正整数 $n$ 的所有正因数。

试补全枚举程序。

```
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> fac;
    fac.reserve((int)ceil(sqrt(n)));

    int i;
    for (i = 1; i * i < n; ++i){
        if (①){
            fac.push_back(i);
        }
    }

    for (int k = 0; k < fac.size(); ++k){
        cout << ② << "";
    }
    if (③) {
        cout << ④ << "";
    }
    for (int k = fac.size() - 1; k >= 0; --k){
        cout << ⑤ << "";
    }
}
```

①~⑤处应填（ ）

子题 2

A. `n / fac[k]`

B. `fac[k]`

C. `fac[k]-1`

D. `n / (fac[k]-1)`

答案：B

## 第 37 题

【来源：洛谷有题；试卷：1039；原题：19-3；洛谷题目ID：1054】

## 三、完善程序（单选题，每小题 $3$ 分，共计 $30$ 分）

### （1）（枚举因数）从小到大打印正整数 $n$ 的所有正因数。

试补全枚举程序。

```
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> fac;
    fac.reserve((int)ceil(sqrt(n)));

    int i;
    for (i = 1; i * i < n; ++i){
        if (①){
            fac.push_back(i);
        }
    }

    for (int k = 0; k < fac.size(); ++k){
        cout << ② << "";
    }
    if (③) {
        cout << ④ << "";
    }
    for (int k = fac.size() - 1; k >= 0; --k){
        cout << ⑤ << "";
    }
}
```

①~⑤处应填（ ）

子题 3

A. `(i-1)*(i-1)== n`

B. `(i-1)*i == n`

C. `i*i == n`

D. `i*(i-1) == n`

答案：C

## 第 38 题

【来源：洛谷有题；试卷：1039；原题：19-4；洛谷题目ID：1054】

## 三、完善程序（单选题，每小题 $3$ 分，共计 $30$ 分）

### （1）（枚举因数）从小到大打印正整数 $n$ 的所有正因数。

试补全枚举程序。

```
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> fac;
    fac.reserve((int)ceil(sqrt(n)));

    int i;
    for (i = 1; i * i < n; ++i){
        if (①){
            fac.push_back(i);
        }
    }

    for (int k = 0; k < fac.size(); ++k){
        cout << ② << "";
    }
    if (③) {
        cout << ④ << "";
    }
    for (int k = fac.size() - 1; k >= 0; --k){
        cout << ⑤ << "";
    }
}
```

①~⑤处应填（ ）

子题 4

A. `n-i`

B. `n-i+1`

C. `i-1`

D. `i`

答案：D

## 第 39 题

【来源：洛谷有题；试卷：1039；原题：19-5；洛谷题目ID：1054】

## 三、完善程序（单选题，每小题 $3$ 分，共计 $30$ 分）

### （1）（枚举因数）从小到大打印正整数 $n$ 的所有正因数。

试补全枚举程序。

```
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    vector<int> fac;
    fac.reserve((int)ceil(sqrt(n)));

    int i;
    for (i = 1; i * i < n; ++i){
        if (①){
            fac.push_back(i);
        }
    }

    for (int k = 0; k < fac.size(); ++k){
        cout << ② << "";
    }
    if (③) {
        cout << ④ << "";
    }
    for (int k = fac.size() - 1; k >= 0; --k){
        cout << ⑤ << "";
    }
}
```

①~⑤处应填（ ）

子题 5

A. `n / fac[k]`

B. `fac[k]`

C. `fac[k]-1`

D. `n / (fac[k]-1)`

答案：A

## 第 40 题

【来源：洛谷有题；试卷：1039；原题：20-1；洛谷题目ID：1055】

### （2）（洪水填充）

现有用字符标记像素颜色的 $8\times 8$ 图像。颜色填充的操作描述如下：给定起始像素的位置待填充的颜色，将起始像素和所有可达的像素（可达的定义：经过一次或多次的向上、下、左、右四个方向移动所能到达且终点和路径上所有像素的颜色都与起始像素颜色相同），替换为给定的颜色。

试补全程序。

```
#include<bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c): r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt,
              int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);

    int prev_color = image[cur.r][cur.c];
    ②;

    while (!queue.empty()) {
        Point pt = queue.front ();
        queue.pop ();

        Point points[4] = {③, Point(pt.r - 1, pt.c),
                           Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};

    Point cur(4, 4);
    char new_color = 'y';

    flood_fill(image, cur, new_color);

    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << '';
        }
        cout << endl;
    }
//输出:
// g g g g g g g g
// g g g g g g r r
// g r r g g r g g
// g y y y y r g r
// g g g y y r g r
// g g g y y y y r
// g g g g g y g g
// g g g g g y y g

    return 0;
}
```

①~⑤处应填（ ）

子题 1

A. `image[r][c] == prev_color`

B. `image[r][c] != prev_color`

C. `image[r][c] == new_color`

D. `image[r][c] != new_color`

答案：A

## 第 41 题

【来源：洛谷有题；试卷：1039；原题：20-2；洛谷题目ID：1055】

### （2）（洪水填充）

现有用字符标记像素颜色的 $8\times 8$ 图像。颜色填充的操作描述如下：给定起始像素的位置待填充的颜色，将起始像素和所有可达的像素（可达的定义：经过一次或多次的向上、下、左、右四个方向移动所能到达且终点和路径上所有像素的颜色都与起始像素颜色相同），替换为给定的颜色。

试补全程序。

```
#include<bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c): r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt,
              int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);

    int prev_color = image[cur.r][cur.c];
    ②;

    while (!queue.empty()) {
        Point pt = queue.front ();
        queue.pop ();

        Point points[4] = {③, Point(pt.r - 1, pt.c),
                           Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};

    Point cur(4, 4);
    char new_color = 'y';

    flood_fill(image, cur, new_color);

    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << '';
        }
        cout << endl;
    }
//输出:
// g g g g g g g g
// g g g g g g r r
// g r r g g r g g
// g y y y y r g r
// g g g y y r g r
// g g g y y y y r
// g g g g g y g g
// g g g g g y y g

    return 0;
}
```

①~⑤处应填（ ）

子题 2

A. `image[cur.r+1][cur.c] = new_color`

B. `image[cur.r][cur.c] = new_color`

C. `image[cur.r][cur.c+1] = new_color`

D. `image[cur.r][cur.c] = prev_color`

答案：B

## 第 42 题

【来源：洛谷有题；试卷：1039；原题：20-3；洛谷题目ID：1055】

### （2）（洪水填充）

现有用字符标记像素颜色的 $8\times 8$ 图像。颜色填充的操作描述如下：给定起始像素的位置待填充的颜色，将起始像素和所有可达的像素（可达的定义：经过一次或多次的向上、下、左、右四个方向移动所能到达且终点和路径上所有像素的颜色都与起始像素颜色相同），替换为给定的颜色。

试补全程序。

```
#include<bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c): r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt,
              int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);

    int prev_color = image[cur.r][cur.c];
    ②;

    while (!queue.empty()) {
        Point pt = queue.front ();
        queue.pop ();

        Point points[4] = {③, Point(pt.r - 1, pt.c),
                           Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};

    Point cur(4, 4);
    char new_color = 'y';

    flood_fill(image, cur, new_color);

    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << '';
        }
        cout << endl;
    }
//输出:
// g g g g g g g g
// g g g g g g r r
// g r r g g r g g
// g y y y y r g r
// g g g y y r g r
// g g g y y y y r
// g g g g g y g g
// g g g g g y y g

    return 0;
}
```

①~⑤处应填（ ）

子题 3

A. `Point(pt.r, pt.c)`

B. `Point(pt.r, pt.c+1)`

C. `Point(pt.r+1, pt.c)`

D. `Point(pt.r+1, pt.c+1)`

答案：C

## 第 43 题

【来源：洛谷有题；试卷：1039；原题：20-4；洛谷题目ID：1055】

### （2）（洪水填充）

现有用字符标记像素颜色的 $8\times 8$ 图像。颜色填充的操作描述如下：给定起始像素的位置待填充的颜色，将起始像素和所有可达的像素（可达的定义：经过一次或多次的向上、下、左、右四个方向移动所能到达且终点和路径上所有像素的颜色都与起始像素颜色相同），替换为给定的颜色。

试补全程序。

```
#include<bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c): r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt,
              int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);

    int prev_color = image[cur.r][cur.c];
    ②;

    while (!queue.empty()) {
        Point pt = queue.front ();
        queue.pop ();

        Point points[4] = {③, Point(pt.r - 1, pt.c),
                           Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};

    Point cur(4, 4);
    char new_color = 'y';

    flood_fill(image, cur, new_color);

    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << '';
        }
        cout << endl;
    }
//输出:
// g g g g g g g g
// g g g g g g r r
// g r r g g r g g
// g y y y y r g r
// g g g y y r g r
// g g g y y y y r
// g g g g g y g g
// g g g g g y y g

    return 0;
}
```

①~⑤处应填（ ）

子题 4

A. `prev_color = image[p.r][p.c]`

B. `new_color = image[p.r][p.c]`

C. `image[p.r][p.c] = prev_color`

D. `image[p.r][p.c] = new_color`

答案：D

## 第 44 题

【来源：洛谷有题；试卷：1039；原题：20-5；洛谷题目ID：1055】

### （2）（洪水填充）

现有用字符标记像素颜色的 $8\times 8$ 图像。颜色填充的操作描述如下：给定起始像素的位置待填充的颜色，将起始像素和所有可达的像素（可达的定义：经过一次或多次的向上、下、左、右四个方向移动所能到达且终点和路径上所有像素的颜色都与起始像素颜色相同），替换为给定的颜色。

试补全程序。

```
#include<bits/stdc++.h>
using namespace std;

const int ROWS = 8;
const int COLS = 8;

struct Point {
    int r, c;
    Point(int r, int c): r(r), c(c) {}
};

bool is_valid(char image[ROWS][COLS], Point pt,
              int prev_color, int new_color) {
    int r = pt.r;
    int c = pt.c;
    return (0 <= r && r < ROWS && 0 <= c && c < COLS &&
            ① && image[r][c] != new_color);
}

void flood_fill(char image[ROWS][COLS], Point cur, int new_color) {
    queue<Point> queue;
    queue.push(cur);

    int prev_color = image[cur.r][cur.c];
    ②;

    while (!queue.empty()) {
        Point pt = queue.front ();
        queue.pop ();

        Point points[4] = {③, Point(pt.r - 1, pt.c),
                           Point(pt.r, pt.c + 1), Point(pt.r, pt.c - 1)};
        for (auto p : points) {
            if (is_valid(image, p, prev_color, new_color)) {
                ④;
                ⑤;
            }
        }
    }
}

int main() {
    char image[ROWS][COLS] = {{'g', 'g', 'g', 'g', 'g', 'g', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'g', 'r', 'r'},
                              {'g', 'r', 'r', 'g', 'g', 'r', 'g', 'g'},
                              {'g', 'b', 'b', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'r', 'g', 'r'},
                              {'g', 'g', 'g', 'b', 'b', 'b', 'b', 'r'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'g', 'g'},
                              {'g', 'g', 'g', 'g', 'g', 'b', 'b', 'g'}};

    Point cur(4, 4);
    char new_color = 'y';

    flood_fill(image, cur, new_color);

    for (int r = 0; r < ROWS; r++) {
        for (int c = 0; c < COLS; c++) {
            cout << image[r][c] << '';
        }
        cout << endl;
    }
//输出:
// g g g g g g g g
// g g g g g g r r
// g r r g g r g g
// g y y y y r g r
// g g g y y r g r
// g g g y y y y r
// g g g g g y g g
// g g g g g y y g

    return 0;
}
```

①~⑤处应填（ ）

子题 5

A. `queue.push(p)`

B. `queue. push (pt)`

C. `queue.push(cur)`

D. `queue. push(Point (ROWS, COLS))`

答案：A
