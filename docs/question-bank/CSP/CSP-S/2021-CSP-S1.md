# CSP 2021 提高级第一轮

- 来源：洛谷有题
- URL：https://ti.luogu.com.cn/problemset/1037
- 年份：2021
- 组别：CSP-S1
- 题目数：43

## 第 1 题

【来源：洛谷有题；试卷：1037；原题：1；洛谷题目ID：900】

在 Linux 系统终端中，用于列出当前目录下所含的文件和子目录的命令为（ ）。

A. ls

B. cd

C. cp

D. all

答案：A

## 第 2 题

【来源：洛谷有题；试卷：1037；原题：2；洛谷题目ID：901】

二进制数 $00101010_{2}$ 和 $00010110_{2}$ 的和为（）。

A. $00111100_{2}$

B. $01000000_{2}$

C. $00111100_{2}$

D. $01000010_{2}$

答案：B

## 第 3 题

【来源：洛谷有题；试卷：1037；原题：3；洛谷题目ID：902】

在程序运行过程中，如果递归调用的层数过多，可能会由于（ ）引发错误。

A. 系统分配的栈空间溢出

B. 系统分配的队列空间溢出

C. 系统分配的链表空间溢出

D. 系统分配的堆空间溢出

答案：A

## 第 4 题

【来源：洛谷有题；试卷：1037；原题：4；洛谷题目ID：903】

以下排序方法中，（ ）是不稳定的。

A. 插入排序

B. 冒泡排序

C. 堆排序

D. 归并排序

答案：C

## 第 5 题

【来源：洛谷有题；试卷：1037；原题：5；洛谷题目ID：904】

以比较为基本运算，对于 $2n$ 个数，同时找到最大值和最小值，最坏情况下需要的最小的比 较次数为（ ）。

A. $4n-2$

B. $3n+1$

C. $3n-2$

D. $2n+1$

答案：C

## 第 6 题

【来源：洛谷有题；试卷：1037；原题：6；洛谷题目ID：905】

现有一个地址区间为 $0\sim 10$ 的哈希表，对于出现冲突情况，会往后找第一个空的地址存储 （到 $10$ 冲突了就从 $0$ 开始往后），现在要依次存储 $(0,1,2，3,4,5,6,7)$，哈希函数为 $h(x)=x^{2} \bmod {11}$。请问 $7$ 存储在哈希表哪个地址中（ ）。

A. 5

B. 6

C. 7

D. 8

答案：C

## 第 7 题

【来源：洛谷有题；试卷：1037；原题：7；洛谷题目ID：906】

$G$ 是一个非连通简单无向图（没有自环和重边），共有 $36$ 条边，则该图至少有（ ）个点。

A. 8

B. 9

C. 10

D. 11

答案：C

## 第 8 题

【来源：洛谷有题；试卷：1037；原题：8；洛谷题目ID：907】

令根结点的高度为 $1$，则一棵含有 $2021$ 个结点的二叉树的高度至少为（ ）。

A. 10

B. 11

C. 12

D. 2021

答案：B

## 第 9 题

【来源：洛谷有题；试卷：1037；原题：9；洛谷题目ID：908】

前序遍历和中序遍历相同的二叉树为且仅为（  ）。

A. 只有 $1$ 个点的二叉树

B. 根结点没有左子树的二叉树

C. 非叶子结点只有左子树的二叉树

D. 非叶子结点只有右子树的二叉树

答案：D

## 第 10 题

【来源：洛谷有题；试卷：1037；原题：10；洛谷题目ID：909】

定义一种字符串操作为交换相邻两个字符。将 $\texttt{DACFEB}$ 变为  $\texttt{ABCDEF}$ 最少需要 ( ) 次上述操作。

A. 7

B. 8

C. 9

D. 6

答案：A

## 第 11 题

【来源：洛谷有题；试卷：1037；原题：11；洛谷题目ID：910】

有如下递归代码
```
solve(t, n):
  if t=1 return 1
  else return 5*solve(t-1,n) mod n
```
则 `solve(23,23)` 的结果为（ ）。

A. 1

B. 7

C. 12

D. 22

答案：A

## 第 12 题

【来源：洛谷有题；试卷：1037；原题：12；洛谷题目ID：911】

斐波那契数列的定义为： $F_{1}=1$，$F_{2}=1$，$F_{n}=F_{n-1}+F_{n-2}$$(n\geq 3)$ 。现在用如下程序来计算斐波那契数列的第 $n$ 项，其时间复杂度为（ ）。
```
F(n):
  if n<=2 return 1
  else return F(n-1) + F(n-2)
```

A. $O(n)$

B. $O(n^{2})$

C. $O(2^{n})$

D. $O(n\ log\ n)$

答案：C

## 第 13 题

【来源：洛谷有题；试卷：1037；原题：13；洛谷题目ID：912】

有 $8$ 个苹果从左到右排成一排，你要从中挑选至少一个苹果，并且不能同时挑选相邻的两个苹果，一共有（ ）种方案。

A. 36

B. 48

C. 54

D. 64

答案：C

## 第 14 题

【来源：洛谷有题；试卷：1037；原题：14；洛谷题目ID：913】

设一个三位数 $n= \overline{abc}$， $a, b, c$ 均为 $1\sim 9$ 之间的整数，若以 $a$、 $b$、 $c$ 作为三角形的三条边可以构成等腰三角形（包括等边），则这样的 $n$ 有（ ）个。

A. 81

B. 120

C. 165

D. 216

答案：C

## 第 15 题

【来源：洛谷有题；试卷：1037；原题：15；洛谷题目ID：914】

有如下的有向图，节点为 $A ,B , \cdots , J$, 其中每条边的长度都标在图中。则节点 $A$ 到节点 $J$ 的最短路径长度为（ ）。
![](https://ipic.luogu.com.cn/l7l0c.png)

A. 16

B. 19

C. 20

D. 22

答案：B

## 第 16 题

【来源：洛谷有题；试卷：1037；原题：16-1；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 1：将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。

A. 正确

B. 错误

答案：A

## 第 17 题

【来源：洛谷有题；试卷：1037；原题：16-2；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 2：将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。

A. 正确

B. 错误

答案：B

## 第 18 题

【来源：洛谷有题；试卷：1037；原题：16-3；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 3：将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。

A. 正确

B. 错误

答案：B

## 第 19 题

【来源：洛谷有题；试卷：1037；原题：16-4；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 4：（2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`

A. 正确

B. 错误

答案：A

## 第 20 题

【来源：洛谷有题；试卷：1037；原题：16-5；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 5：当输入为 `1 1 1 1 1 1 1 2` 时，输出为

A. `3.1416`

B. `6.2832`

C. `4.7124`

D. `4.1888`

答案：D

## 第 21 题

【来源：洛谷有题；试卷：1037；原题：16-6；洛谷题目ID：915】

二、阅读程序（程序输入不超过数组或字符串定义的范围；判断题正确填 √ ，错误填 × ；除特 殊说明外，判断题 $1.5$ 分，选择题 $3$ 分，共计 $40$ 分）
### (1)
![](https://ipic.luogu.com.cn/1rorg.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
16. 将第 21 行中 `t` 的类型声明从 `int` 改为 `double`， 不会 影响程序运行的结果。（）

17. 将第 26、27 行中的 `/ sqrt(t) / 2`替换为`/ 2 / sqrt(t)`，不会影响程序运行的结果。（ ）

18. 将第 28 行中的 `x * x` 改成 `sq(x)`、`y * y` 改成 `sq(y)`，不会影响程序运行的结果。（ ）

19. （2 分） 当输入为 `0 0 0 1 1 0 0 1` 时，输出为 `1.3090`  ( )

#### 单选题
20. 当输入为 `1 1 1 1 1 1 1 2` 时，输出为（ ）。
A. `3.1416`
B. `6.2832`
C. `4.7124`
D. `4.1888`

21. （2.5 分）这段代码的含义为（ ）。
A. 求圆的面积并
B. 求球的体积并
C. 求球的体积交
D. 求椭球的体积并

子题 6：（2.5 分）这段代码的含义为

A. 求圆的面积并

B. 求球的体积并

C. 求球的体积交

D. 求椭球的体积并

答案：C

## 第 22 题

【来源：洛谷有题；试卷：1037；原题：17-1；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 1：程序总是会正常执行并输出两行两个相等的数。

A. 正确

B. 错误

答案：A

## 第 23 题

【来源：洛谷有题；试卷：1037；原题：17-2；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 2：第 28 行与第 38 行分别有可能执行两次及以上。

A. 正确

B. 错误

答案：B

## 第 24 题

【来源：洛谷有题；试卷：1037；原题：17-3；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 3：当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。

A. 正确

B. 错误

答案：B

## 第 25 题

【来源：洛谷有题；试卷：1037；原题：17-4；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 4：`solve1(1, n)` 的时间复杂度为

A. $O(\log \ n)$

B. $O(n)$

C. $O(n \log \ n)$

D. $O(n^{2})$

答案：B

## 第 26 题

【来源：洛谷有题；试卷：1037；原题：17-5；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 5：`solve2(1, n)` 的时间复杂度为

A. $O(\log \ n)$

B. $O(n)$

C. $O(n \log \ n)$

D. $O(n^{2})$

答案：C

## 第 27 题

【来源：洛谷有题；试卷：1037；原题：17-6；洛谷题目ID：916】

### (2)
![](https://ipic.luogu.com.cn/u921e.png)
![](https://ipic.luogu.com.cn/7f6vs.png)
**假设输入的所有数的绝对值都不超过 $1000$ ，完成下面的判断题和单选题：**
#### 判断题
22. 程序总是会正常执行并输出两行两个相等的数。（ ）

23. 第 28 行与第 38 行分别有可能执行两次及以上。（ ）

24. 当输入为 `5 -10 11 -9 5 -7` 时，输出的第二行为 `7`。( )

#### 单选题

25. `solve1(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log n)$
D. $O(n^{2})$

26. `solve2(1, n)` 的时间复杂度为（ ）。
A. $O(\log n)$
B. $O(n)$
C. $O(n \log  n)$
D. $O(n^{2})$

27. 当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为（  ）。
A. `13`
B. `17`
C. `24`
D. `12`

子题 6：当输入为 `10 -3 2 10 0 -8 9 -4 -5 9 4` 时，输出的第一行为

A. `13`

B. `17`

C. `24`

D. `12`

答案：B

## 第 28 题

【来源：洛谷有题；试卷：1037；原题：18-1；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 1：程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。

A. 正确

B. 错误

答案：B

## 第 29 题

【来源：洛谷有题；试卷：1037；原题：18-2；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 2：对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。

A. 正确

B. 错误

答案：A

## 第 30 题

【来源：洛谷有题；试卷：1037；原题：18-3；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 3：当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。

A. 正确

B. 错误

答案：B

## 第 31 题

【来源：洛谷有题；试卷：1037；原题：18-4；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 4：设输入字符串长度为 $n$，`encode` 函数的时间复杂度为

A. $O( \sqrt{n})$

B. $O(n)$

C. $O(n\log n)$

D. $O(n^2)$

答案：B

## 第 32 题

【来源：洛谷有题；试卷：1037；原题：18-5；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 5：输出的第一行为

A. `0xff`

B. `255`

C. `0xFF`

D. `-1`

答案：D

## 第 33 题

【来源：洛谷有题；试卷：1037；原题：18-6；洛谷题目ID：917】

### (3)
![](https://ipic.luogu.com.cn/k2oqx.png)
![](https://ipic.luogu.com.cn/uhixa.png)
**假设输入总是合法的（一个整数和一个不含空白字符的字符串，用空格隔开），完成下面的判断题和单选题：**

#### 判断题
28. 程序总是先输出 **一行** 一个整数，再输出 **一行** 一个字符串。（ ）

29. 对于任意不含空白字符的字符串 `str1`，先执行程序输入`0 str1`，得到输出的第二行记为 `str2` 再执行程序输入`1 str2`，输出的第二行必为 `str1`。（ ）

30. 当输入为`1 SGVsbG93b3JsZA==`时，输出的第二行为`HelloWorld`。( )

#### 单选题

31. 设输入字符串长度为 $n$，`encode` 函数的时间复杂度为（ ）。
A. $O( \sqrt{n})$
B. $O(n)$
C. $O(n\log  n)$
D. $O(n^2)$

32. 输出的第一行为（ ）。
A. `0xff`
B. `255`
C. `0xFF`
D. `-1`

33. （4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为（ ）。
A. `Q1NQMjAyMWNzcAv=`
B. `Q1NQMjAyMGNzcA==`
C. `Q1NQMjAyMGNzcAv=`
D. `Q1NQMjAyMWNzcA==`

子题 6：（4 分） 当输入为 `0 CSP2021csp` 时，输出的第二行为

A. `Q1NQMjAyMWNzcAv=`

B. `Q1NQMjAyMGNzcA==`

C. `Q1NQMjAyMGNzcAv=`

D. `Q1NQMjAyMWNzcA==`

答案：D

## 第 34 题

【来源：洛谷有题；试卷：1037；原题：19-1；洛谷题目ID：918】

## 三、完善程序（单选题，每小题 3 分，共计 30 分）
**(1) （魔法数字）** 小 H 的魔法数字是 $4$。给定 $n$， 他希望用若干个 $4$ 进行若干次加法、减法和整除运算得到 $n$。但由于小 H 计算能力有限，计算过程中只能出现不超过 $M = 10000$ 的正整数。求至少可能用到多少个 $4$。

例如，当 $n=2$ 时，有 $2=\dfrac{4 + 4}{4}$，用到了 $3$ 个 $4$，是最优方案。

试补全程序。
![](https://ipic.luogu.com.cn/ink47.png)
![](https://ipic.luogu.com.cn/xp28q.png)

34. ①处应填（ ）
A. `F[4] = 0`
B. `F[1] = 4`
C. `F[1] = 2`
D. `F[4] = 1`

35. ②处应填（ ）
A. `!Vis[n]`
B. `r < n`
C. `F[M] == INT_MAX`
D. `F[n] == INT_MAX`

36. ③处应填（ ）
A. `F[i] == r`
B. `!Vis[i] && F[i] == r`
C. `F[i] < F[x]`
D. `!Vis[i] && F[i] < F[x]`

37. ④处应填（ ）
A. `F[i] < F[x]`
B. `F[i]<=r`
C. `Vis[i]`
D. `i <= x`

子题 1：①处应填

A. `F[4] = 0`

B. `F[1] = 4`

C. `F[1] = 2`

D. `F[4] = 1`

答案：D

## 第 35 题

【来源：洛谷有题；试卷：1037；原题：19-2；洛谷题目ID：918】

## 三、完善程序（单选题，每小题 3 分，共计 30 分）
**(1) （魔法数字）** 小 H 的魔法数字是 $4$。给定 $n$， 他希望用若干个 $4$ 进行若干次加法、减法和整除运算得到 $n$。但由于小 H 计算能力有限，计算过程中只能出现不超过 $M = 10000$ 的正整数。求至少可能用到多少个 $4$。

例如，当 $n=2$ 时，有 $2=\dfrac{4 + 4}{4}$，用到了 $3$ 个 $4$，是最优方案。

试补全程序。
![](https://ipic.luogu.com.cn/ink47.png)
![](https://ipic.luogu.com.cn/xp28q.png)

34. ①处应填（ ）
A. `F[4] = 0`
B. `F[1] = 4`
C. `F[1] = 2`
D. `F[4] = 1`

35. ②处应填（ ）
A. `!Vis[n]`
B. `r < n`
C. `F[M] == INT_MAX`
D. `F[n] == INT_MAX`

36. ③处应填（ ）
A. `F[i] == r`
B. `!Vis[i] && F[i] == r`
C. `F[i] < F[x]`
D. `!Vis[i] && F[i] < F[x]`

37. ④处应填（ ）
A. `F[i] < F[x]`
B. `F[i]<=r`
C. `Vis[i]`
D. `i <= x`

子题 2：②处应填

A. `!Vis[n]`

B. `r < n`

C. `F[M] == INT_MAX`

D. `F[n] == INT_MAX`

答案：A

## 第 36 题

【来源：洛谷有题；试卷：1037；原题：19-3；洛谷题目ID：918】

## 三、完善程序（单选题，每小题 3 分，共计 30 分）
**(1) （魔法数字）** 小 H 的魔法数字是 $4$。给定 $n$， 他希望用若干个 $4$ 进行若干次加法、减法和整除运算得到 $n$。但由于小 H 计算能力有限，计算过程中只能出现不超过 $M = 10000$ 的正整数。求至少可能用到多少个 $4$。

例如，当 $n=2$ 时，有 $2=\dfrac{4 + 4}{4}$，用到了 $3$ 个 $4$，是最优方案。

试补全程序。
![](https://ipic.luogu.com.cn/ink47.png)
![](https://ipic.luogu.com.cn/xp28q.png)

34. ①处应填（ ）
A. `F[4] = 0`
B. `F[1] = 4`
C. `F[1] = 2`
D. `F[4] = 1`

35. ②处应填（ ）
A. `!Vis[n]`
B. `r < n`
C. `F[M] == INT_MAX`
D. `F[n] == INT_MAX`

36. ③处应填（ ）
A. `F[i] == r`
B. `!Vis[i] && F[i] == r`
C. `F[i] < F[x]`
D. `!Vis[i] && F[i] < F[x]`

37. ④处应填（ ）
A. `F[i] < F[x]`
B. `F[i]<=r`
C. `Vis[i]`
D. `i <= x`

子题 3：③处应填

A. `F[i] == r`

B. `!Vis[i] && F[i] == r`

C. `F[i] < F[x]`

D. `!Vis[i] && F[i] < F[x]`

答案：D

## 第 37 题

【来源：洛谷有题；试卷：1037；原题：19-4；洛谷题目ID：918】

## 三、完善程序（单选题，每小题 3 分，共计 30 分）
**(1) （魔法数字）** 小 H 的魔法数字是 $4$。给定 $n$， 他希望用若干个 $4$ 进行若干次加法、减法和整除运算得到 $n$。但由于小 H 计算能力有限，计算过程中只能出现不超过 $M = 10000$ 的正整数。求至少可能用到多少个 $4$。

例如，当 $n=2$ 时，有 $2=\dfrac{4 + 4}{4}$，用到了 $3$ 个 $4$，是最优方案。

试补全程序。
![](https://ipic.luogu.com.cn/ink47.png)
![](https://ipic.luogu.com.cn/xp28q.png)

34. ①处应填（ ）
A. `F[4] = 0`
B. `F[1] = 4`
C. `F[1] = 2`
D. `F[4] = 1`

35. ②处应填（ ）
A. `!Vis[n]`
B. `r < n`
C. `F[M] == INT_MAX`
D. `F[n] == INT_MAX`

36. ③处应填（ ）
A. `F[i] == r`
B. `!Vis[i] && F[i] == r`
C. `F[i] < F[x]`
D. `!Vis[i] && F[i] < F[x]`

37. ④处应填（ ）
A. `F[i] < F[x]`
B. `F[i]<=r`
C. `Vis[i]`
D. `i <= x`

子题 4：④处应填

A. `F[i] < F[x]`

B. `F[i]<=r`

C. `Vis[i]`

D. `i <= x`

答案：C

## 第 38 题

【来源：洛谷有题；试卷：1037；原题：20-1；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 1：①处应填

A. `p->son[0] = S[top--]`

B. `p->son[1] = S[top--]`

C. `S[top--]->son[0] = p`

D. `S[top--]->son[1] = p`

答案：A

## 第 39 题

【来源：洛谷有题；试卷：1037；原题：20-2；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 2：②处应填

A. `p->son[0] = S[top]`

B. `p->son[1] = S[top]`

C. `S[top]->son[0] = p`

D. `S[top]->son[1] = p`

答案：D

## 第 40 题

【来源：洛谷有题；试卷：1037；原题：20-3；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 3：③处应填

A. `x->dep < y->dep`

B. `x < y`

C. `x->dep > y->dep`

D. `x->val < y->val`

答案：A

## 第 41 题

【来源：洛谷有题；试卷：1037；原题：20-4；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 4：④处应填

A. `A[i * b + j - 1] == A[i * b + j]->son[0]`

B. `A[i * b + j]->val < A[i * b + j - 1]->val`

C. `A[i * b + j] == A[i * b + j - 1]->son[1]`

D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`

答案：D

## 第 42 题

【来源：洛谷有题；试卷：1037；原题：20-5；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 5：⑤处应填

A. `v += (S >> i & 1) ? -1 : 1`

B. `v += (S >> i & 1) ? 1 : -1`

C. `v += (S >> (i - 1) & 1) ? 1 : -1`

D. `v += (S >> (i - 1) & 1) ? -1 : 1`

答案：D

## 第 43 题

【来源：洛谷有题；试卷：1037；原题：20-6；洛谷题目ID：919】

**(2) （ RMQ 区间最值问题）**  给定序列 $a_0,\cdots,a_{n-1},$ $m$ 次询问，每次询问给定 $l,r$，求 $\max \{a_l,\ ...,a_r\}$。

为了解决该问题，有一个算法叫 **the Method of Four Russians** ，其**时间复杂度为 $O(n+m)$** ，步骤如下：

- 建立 Cartesian（笛卡尔）树，将问题转化为树上的 LCA（最近公共祖先）问题。

- 对于 LCA 问题，可以考虑其 Euler 序（即按照 DFS 过程，经过所有点，环游回根的序列），即求 Euler 序列上两点间一个新的 RMQ 问题。

- 注意新的问题为 $\pm1$ RMQ，即相邻两点的深度差一定为 $1$。

下面解决这个 $\pm1$ RMQ 问题，“序列”指 Euler 序列：

- 设 $t$ 为 Euler 序列长度。取 $b=\lceil \frac{\log_2 t}{2} \rceil$  将序列每 $b$ 个分为一大块， 使用 ST 表（倍增表）处理大块间的 RMQ 问题，复杂度 $O(\frac{t}{b}\log t)=O(n)$。

- **（重点）** 对于一个块内的 RMQ 问题，也需要 $O(1)$ 的算法。由于差分数组 $2^{b-1}$ 种，可以预处理出所有情况下的最值位置，预处理复杂度  $O(b2^b)$，不超过 $O(n)$。

- 最终，对于一个查询，可以转化为中间整的大块的 RMQ 问题，以及两端块内的 RMQ 问题。

试补全程序。

![](https://ipic.luogu.com.cn/xk90i.png)
![](https://ipic.luogu.com.cn/28mlp.png)
![](https://ipic.luogu.com.cn/yrnei.png)
![](https://ipic.luogu.com.cn/efrqf.png)

38. ①处应填（ ）
A. `p->son[0] = S[top--]`
B. `p->son[1] = S[top--]`
C. `S[top--]->son[0] = p`
D. `S[top--]->son[1] = p`


39. ②处应填（ ）
A. `p->son[0] = S[top]`
B. `p->son[1] = S[top]`
C. `S[top]->son[0] = p`
D. `S[top]->son[1] = p`


40. ③处应填（ ）
A. `x->dep < y->dep`
B. `x < y`
C. `x->dep > y->dep`
D. `x->val < y->val`

41. ④处应填（ ）
A. `A[i * b + j - 1] == A[i * b + j]->son[0]`
B. `A[i * b + j]->val < A[i * b + j - 1]->val`
C. `A[i * b + j] == A[i * b + j - 1]->son[1]`
D. `A[i * b + j]->dep < A[i * b + j - 1]->dep`


42. ⑤处应填（ ）
A. `v += (S >> i & 1) ? -1 : 1`
B. `v += (S >> i & 1) ? 1 : -1`
C. `v += (S >> (i - 1) & 1) ? 1 : -1`
D. `v += (S >> (i - 1) & 1) ? -1 : 1`


43. ⑥处应填（ ）
A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`
B. `Dif[p]`
C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`
D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

子题 6：⑥处应填

A. `(Dif[p] >> (r - p * b)) & ((1 << (r - l)) - 1)`

B. `Dif[p]`

C. `(Dif[p] >> (l - p * b)) & ((1 << (r - l)) - 1)`

D. `(Dif[p] >> ((p + 1) * b - r)) & ((1 << (r - l + 1)) - 1)`

答案：C
