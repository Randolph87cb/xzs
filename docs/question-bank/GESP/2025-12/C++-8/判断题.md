# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1204
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1723013038866464.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

若一项任务可用两种互斥方案完成：方案 A 有 $ m $ 种做法，方案 B 有 $ n $ 种做法，则总做法数为 $ m + n $。

答案：正确

解析：题干表述成立。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第2题

在 C++ 语言中，引用一旦被初始化，就不能再改为引用另一个变量。

答案：正确

解析：题干表述成立。指针和引用题要区分地址、解引用和别名关系；引用初始化后绑定原变量，给引用赋值就是修改原变量。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第3题

快速排序和归并排序的平均时间复杂度都是 $ O(n \log n) $，但快速排序是不稳定的排序算法，归并排序是稳定的排序算法。

答案：正确

解析：题干表述成立。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第4题

使用 `math.h` 或 `cmath` 头文件中的函数，表达式 `sqrt(4)` 的结果类型为 `double`。

答案：正确

解析：题干表述成立。数学库函数题要注意函数参数含义和返回类型，`sqrt`、`pow`、三角函数等通常返回 `double`。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第5题

在杨辉三角形中，第 $ n $ 行（从 0 开始计数，即第 $ n $ 行有 $ n + 1 $ 个数）的所有数字之和等于 $ 2^n $。

答案：正确

解析：题干表述成立。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第6题

使用二叉堆优化的 Dijkstra 最短路算法，在某些特殊情况下时间复杂度不如朴素实现的 $ O(V^2) $。

答案：正确

解析：题干表述成立。Dijkstra 的松弛条件是发现经过当前点到达邻点的距离更短时，才更新该邻点距离。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第7题

$ n $ 个不同元素依次入栈的出栈序列数与将 $ n $ 个不同元素划分成若干非空子集的方案数相等。

答案：错误

解析：题干表述不成立。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第8题

快速排序在最坏情况下的时间复杂度为 $ O(n \log n) $，可以通过随机化选择基准值（pivot）的方法完全避免退化。

答案：错误

解析：题干表述不成立。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第9题

在 C++ 语言中，一个类可以拥有多个构造函数，也可以拥有多个析构函数。

答案：错误

解析：题干表述不成立。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第10题

求两个序列的最长公共子序列（LCS）时，使用滚动数组优化空间后，仍然可以还原出具体的 LCS 序列。

答案：错误

解析：题干表述不成立。LCS 的状态转移在字符相等时继承左上角加一，不相等时取去掉一侧字符后的较大值。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。





