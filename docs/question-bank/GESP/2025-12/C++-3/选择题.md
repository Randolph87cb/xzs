# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1199
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1723012369874976.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

请将二进制数 $(1101.101)_2$，转换为等值的 $10$ 进制数（ ）。

【知识点：进制转换与进制字面量】

A. $14.25$
B. $13.625$
C. $13.5$
D. $14.5$

答案：B

解析：B 项「13.625」符合题干要求。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

以下关于标准 C++ 一维数组的描述，哪一项是错误的？（ ）

【知识点：数组与字符数组】

A. 数组的下标从 $0$ 开始
B. 数组在内存中是连续存放的
C. 数组的长度只能在运行时才能确定
D. 可以在程序中修改数组某个元素的值

答案：C

解析：本题问不符合要求的一项，C 项「数组的长度只能在运行时才能确定」正是与题干规则冲突的说法或做法。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项能够满足题意或表述成立，因此应选 C。



## 第3题

下列代码段执行后，变量 `sum` 的值是（ ）

```cpp
{
    int sum = 0;
    for (int i = 1; i <= 10; i++) {
        if (i % 3 == 8 >> 2)
            continue;
        sum += i;
    }
}
```

【知识点：位运算】

A. $12$
B. $17$
C. $33$
D. $40$

答案：D

解析：D 项「40」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

执行以下程序段，输出值是（ ）

```cpp
int x = 5;
if (x == 3 >> 2) x = 8 >> 3;
cout << x << endl;
```

【知识点：位运算】

A. $0$
B. $5$
C. $120$
D. $1$

答案：B

解析：B 项「5」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

已知字符串 `s = "C++ Programming"`，下列程序运行的结果是（ ）

```cpp
cout << (int)(s.length() | 3) << endl;
```

【知识点：位运算】

A. $14$
B. $15$
C. $16$
D. $17$

答案：B

解析：B 项「15」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

以下代码是一个程序的部分代码，能够正确执行，输出的结果是（ ）

```cpp
int arr[5] = {1, 2, 3, 4, 5};
int p = arr[1];
cout << (p + 2) << endl;
```

【知识点：数组与字符数组】

A. $1$
B. $2$
C. $3$
D. $4$

答案：D

解析：D 项「4」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

以下关于 C++ 求最小值函数 `min()` 的描述，不正确的是（ ）

【知识点：枚举与基础算法】

A. 函数必须返回一个值
B. 该函数可以嵌套调用
C. 调用 `min(5)` 是错误的，不能通过编译
D. 调用 `min()`（不传任何参数）可以通过编译，结果为 $0$

答案：D

解析：本题问不符合要求的一项，D 项「调用 min()（不传任何参数）可以通过编译，结果为 0」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 D。



## 第8题

在一个特定的计算机系统中，假如 `unsigned int` 类型需要占用 $2$ 个字节的存储空间（每个字节有 $8$ 位），则 `unsigned int` 可以表示的数据范围是（ ）

【知识点：位运算】

A. 0 ~ 65535
B. 0 ~ 65536
C. -65536 ~ 65535
D. 0 ~ 32767

答案：A

解析：A 项「0 ~ 65535」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

以下代码执行后，数组 `arr` 的内容是（ ）

```cpp
int arr[6] = {1, 2, 3, 4, 5, 6};
for (int i = 0; i < 6; i += 2) {
    arr[i] = arr[i] + arr[i+1];
    arr[i+1] = arr[i] - arr[i+1];
    arr[i] = arr[i] - arr[i+1];
}
```

【知识点：数组与字符数组】

A. $\{2,1,4,3,6,5\}$
B. $\{1,2,3,4,5,6\}$
C. $\{3,1,4,2,5,3\}$
D. $\{1,3,2,5,4,6\}$

答案：A

解析：A 项「\{2,1,4,3,6,5\}」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

`a & b | (c ^ d)`，其中 $a = 3$，$b = 7$，$c = 15$，$d = 4$，计算结果是（ ）

【知识点：位运算】

A. 十进制 $11$
B. 二进制 $11$
C. 八进制 $11$
D. 十六进制 $11$

答案：A

解析：A 项「十进制 11」符合题干要求。指针和引用题要区分地址、解引用和别名关系；引用初始化后绑定原变量，给引用赋值就是修改原变量。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

整型变量 $x$ 的初始值为 $10$，以下代码的输出结果是（ ）

```cpp
int t = x--;
t -= x;
cout << t << endl;
```

【知识点：控制结构与程序阅读】

A. $0$
B. $-1$
C. $1$
D. 死循环

答案：C

解析：C 项「1」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

根据下面的流程图，如果成绩 `score` 输入 $60$，输出的结果是（ ）

![](https://cdn.luogu.com.cn/upload/image_hosting/ng3uhw6l.png)

【知识点：控制结构与程序阅读】

A. 优秀
B. 及格
C. 不及格
D. 没有输出

答案：B

解析：B 项「及格」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

以下代码运行后，`sum` 的结果是（ ）

```cpp
int arr[5] = {2, 4, 6, 8, 10};
int sum = 0;
for (int i = 0; i < 5; i++) {
    switch (arr[i] % (1|2)) {
        case 0: sum += 1; break;
        case 1: sum += 2; break;
        case 2: sum += 3; break;
    }
}
```

【知识点：位运算】

A. $10$
B. $11$
C. $14$
D. $15$

答案：B

解析：B 项「11」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

以下关于 C++ 中 `abs()` 函数的描述，正确的是（ ）

【知识点：函数与标准库】

A. `abs()` 函数可用于计算 `int` 类型整数的绝对值，头文件为 `<cmath>` 或 `<cstdlib>`
B. 调用 `abs(3.14)` 可以四舍五入得到 $3$
C. `abs(-1, -2)` 的返回值是 $1$
D. 若传入负数浮点数，`abs()` 会自动截断小数部分后返回整数绝对值

答案：A

解析：A 项「abs() 函数可用于计算 int 类型整数的绝对值，头文件为 <cmath> 或 <cstdlib>」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

如果字符串 `s` 的值是 `GESP`，以下代码 `s` 的最后结果是（ ）

```cpp
for (int i = 0; i < s.length(); i++) {
    s[i] = toupper(s[i] + i);
}
```

【知识点：字符串处理】

A. `GESP`
B. `HFTR`
C. `hesp`
D. `GFUS`

答案：D

解析：D 项「GFUS」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。





