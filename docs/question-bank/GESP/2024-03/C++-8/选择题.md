# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1148
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1602047270977568.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

为丰富食堂菜谱，炒菜部进行头脑风暴。肉类有鸡肉、牛肉、羊肉、猪肉 4 种，切法有肉排、肉块、肉末 3 种，配菜有圆白菜、油菜、豆腐 3 种，辣度有麻辣、微辣、不辣 3 种。不考虑口感的情况下，选 1 种肉、1 种切法、1 种配菜、1 种辣度产生一道菜（例如：麻辣牛肉片炒豆腐），这样能产生多少道菜？（ ）。

A. $13$
B. $42$
C. $63$
D. $108$

答案：D

解析：本题答案选 D。选项 D 的内容「$108$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第2题

已知袋中有 2 个相同的红球、3 个相同的绿球、5 个相同的黄球。每次取出一个不放回，全部取出。可能产生多少种序列？（ ）。

A. $6$
B. $1440$
C. $2520$
D. $3628800$

答案：C

解析：本题答案选 C。选项 C 的内容「$2520$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第3题

以下二维数组的初始化，哪个是符合语法的？（ ）。

A. `int a[][] = {{1, 2}, {3, 4}};`
B. `int a[][2] = {};`
C. `int a[2][2] = {{1, 2, 3}, {4, 5, 6}};`
D. `int a[2][] = {{1, 2, 3}, {4, 5, 6}};`

答案：B

解析：本题答案选 B。选项 B 的内容「`int a[][2] = {};`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第4题

下面有关 C++ 拷贝构造函数的说法，错误的是（ ）。

A. 必须实现拷贝构造函数，否则一定会出现编译错误。
B. 对象作为函数参数、以值传递方式传入函数时，会自动调用拷贝构造函数。
C. 对象作为函数返回值、以值传递方式从函数返回时，会自动调用拷贝构造函数。
D. 使用一个对象初始化另一个对象时，会自动调用拷贝构造函数。

答案：A

解析：本题答案选 A。选项 A 的内容「必须实现拷贝构造函数，否则一定会出现编译错误。」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第5题

使用邻接表表达一个无向简单图，图中包含 $v$ 个顶点、$e$ 条边，则该表中边节点的个数为（ ）。

A. $v \times (v - 1)$
B. $v \times v$
C. $2 \times e$
D. $e$

答案：C

解析：本题答案选 C。选项 C 的内容「$2 \times e$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第6题

关于生成树的说法，错误的是（ ）。

A. 一个无向连通图可以有多个生成树。
B. 一个无向图，只要连通，就一定有生成树。
C. $n$ 个顶点的无向完全图，有 $n^{n-2}$ 棵生成树。
D. $n$ 个顶点的无向图，生成树包含 $n-1$ 条边。

答案：D

解析：本题答案选 D。选项 D 的内容「$n$ 个顶点的无向图，生成树包含 $n-1$ 条边。」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第7题

已知三个 `double` 类型的变量 `a`、`b` 和 `theta` 分别表示一个三角形的两条边长及二者的夹角（弧度），则下列哪个表达式可以计算这个三角形的周长？（ ）。

A. `a * b * sin(theta) / 2`
B. `a + b + (a + b) * sin(theta) / 2`
C. `a * b * cos(theta) / 2`
D. `a + b + sqrt(a * a + b * b - 2 * a * b * cos(theta))`

答案：D

解析：本题答案选 D。选项 D 的内容「`a + b + sqrt(a * a + b * b - 2 * a * b * cos(theta))`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第8题

在有 $n$ 个元素的二叉排序树中进行查找，其最好、最差时间复杂度分别为（ ）。

A. $O(1)$、$O(n)$
B. $O(1)$、$O(\log n)$
C. $O(\log n)$、$O(\log n)$
D. $O(\log n)$、$O(n)$

答案：A

解析：本题答案选 A。选项 A 的内容「$O(1)$、$O(n)$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第9题

如下图所示，半径为 $r$、圆心角为 $t$（弧度）的扇形，下面哪个表达式能够求出顶部阴影部分的面积？（ ）

![](https://cdn.luogu.com.cn/upload/image_hosting/1kf4r9wx.png)

A. `r * r * sin(t) / 2`
B. `r * r * t / 2`
C. `r * r * (t - sin(t))`
D. `r * r * (t - sin(t)) / 2`

答案：D

解析：本题答案选 D。选项 D 的内容「`r * r * (t - sin(t)) / 2`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第10题

下面程序的时间复杂度为（ ）。

```cpp
int fib(int n) {
    if (n <= 1)
        return 1;
    return fib(n - 1) + fib(n - 2);
}
```

A. $O(2^n)$
B. $O(\phi^n)$，其中 $\phi = \frac{\sqrt{5}+1}{2}$
C. $O(n)$
D. $O(1)$

答案：B

解析：本题答案选 B。选项 B 的内容「$O(\phi^n)$，其中 $\phi = \frac{\sqrt{5}+1}{2}$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第11题

下面程序的时间复杂度为（ ）。

```cpp
int choose(int n, int m) {
    if (m == 0 || m == n)
        return 1;
    return choose(n - 1, m - 1) + choose(n - 1, m);
}
```

A. $O(2^n)$
B. $O(2^m \times (n - m))$
C. $O(C(n, m))$
D. $O(m \times (n - m))$

答案：C

解析：本题答案选 C。选项 C 的内容「$O(C(n, m))$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第12题

下面程序的时间复杂度为（ ）。

```cpp
int primes[MAXP], num = 0;
bool isPrime[MAXN] = {false};
void sieve() {
    for (int n = 2; n <= MAXN; n++) {
        if (!isPrime[n])
            primes[num++] = n;
        for (int i = 0; i < num && n * primes[i] <= MAXN; i++) {
            isPrime[n * primes[i]] = true;
            if (n % primes[i] == 0)
                break;
        }
    }
}
```

A. $O(n)$
B. $O(n \times \log n)$
C. $O(n \times \log \log n)$
D. $O(n^2)$

答案：A

解析：本题答案选 A。选项 A 的内容「$O(n)$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第13题

下面程序的输出为（ ）。

```cpp
#include <iostream>
using namespace std;

int a[10][10];
int main() {
    int m = 5, n = 4;
    for (int x = 0; x <= m; x++)
        a[x][0] = 1;
    for (int y = 1; y <= n; y++)
        a[0][y] = 1;
    for (int x = 1; x <= m; x++)
        for (int y = 1; y <= n; y++)
            a[x][y] = a[x - 1][y] + a[x][y - 1];
    cout << a[m][n] << endl;
    return 0;
}
```

A. $4$
B. $5$
C. $126$
D. $3024$

答案：C

解析：本题答案选 C。选项 C 的内容「$126$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第14题

下面程序的输出为（ ）。

```cpp
#include <iostream>
using namespace std;

int main() {
    int cnt = 0;
    for (int x = 0; x <= 10; x++)
        for (int y = 0; y <= 10; y++)
            for (int z = 0; z <= 10; z++)
                if (x + y + z == 15)
                    cnt++;
    cout << cnt << endl;
    return 0;
}
```

A. $90$
B. $91$
C. $96$
D. $100$

答案：B

解析：本题答案选 B。选项 B 的内容「$91$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第15题

下面的程序使用邻接矩阵表达的带权无向图，则从顶点 0 到顶点 3 的最短距离为（ ）。

```cpp
int weight[4][4] = {
    { 0,  1,  7, 100},
    { 1,  0,  5,  15},
    { 7,  5,  0,   6},
    {100, 15,  6,   0}};
```

A. $100$
B. $16$
C. $12$
D. $13$

答案：C

解析：本题答案选 C。选项 C 的内容「$12$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。