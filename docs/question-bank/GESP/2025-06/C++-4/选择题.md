# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1184
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1687195991670816.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

在 C++ 中，声明一个指向整型变量的指针的正确语法是（ ）。

A. `int* ptr;`
B. `*int ptr;`
C. `int ptr*;`
D. `ptr int;`

答案：A

解析：A 项「int* ptr;」符合题干要求。指针和引用题要区分地址、解引用和别名关系；引用初始化后绑定原变量，给引用赋值就是修改原变量。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

下面的函数接收一个 3 行 4 列的二维数组并输出其中元素，则横线上不能填写（ ）。

```cpp
void printArray(________) {
    for (int i = 0; i < 3; ++i)
        for (int j = 0; j < 4; ++j)
            std::cout << arr[i][j] << " ";
}
```

A. `int arr[3][4]`
B. `int arr[][4]`
C. `int (*arr)[4]`
D. `int** arr`

答案：D

解析：D 项「int** arr」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

在 C++ 中，`int arr[3][4]` 和 `int* arr = new int[12]` 均可模拟一个 3 行 4 列的二维数组。关于这两种方式，下面说法错误的是（ ）。

A. `int arr[3][4]` 在栈上分配空间，适合数组较小的情况；
B. `int* arr = new int[12]` 在堆上分配空间，数组较大时也适用；
C. 这两种方式申请的内存空间都是连续的。
D. 这两种方式申请的内存都能自动释放。

答案：D

解析：本题问不符合要求的一项，D 项「这两种方式申请的内存都能自动释放。」正是与题干规则冲突的说法或做法。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项能够满足题意或表述成立，因此应选 D。



## 第4题

关于以下 C++ 代码，说法正确的是（ ）。

```cpp
int main() {
    greet();
    return 0;
}

void greet() {
    cout << "Hello!" << endl;
}
```

A. 正确编译并输出 Hello!
B. 编译错误：找不到函数 greet()
C. 编译警告但可以运行
D. 链接错误

答案：B

解析：B 项「编译错误：找不到函数 greet()」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

在 C++ 中，如果希望通过函数修改传入的结构体对象的内容，应该使用哪种参数传递方式？

A. 值传递或引用传递
B. 值传递或指针传递
C. 引用传递或指针传递
D. 仅指针传递

答案：C

解析：C 项「引用传递或指针传递」符合题干要求。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

以下哪个选项正确描述了 C++ 中形参和实参的区别？

A. 形参是函数调用时传递给函数的具体值，实参是函数定义中声明的变量。
B. 形参是函数定义中声明的变量，实参是函数调用时传递给函数的具体值。
C. 形参和实参在函数调用时是完全相同的。
D. 形参只在函数内部可见，实参在函数外部可见。

答案：B

解析：B 项「形参是函数定义中声明的变量，实参是函数调用时传递给函数的具体值。」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

运行如下代码会输出（ ）。

```cpp
int value = 100;

void print1() {
    int value = 50;
    cout << value << " ";
    cout << ::value << " ";
}

void print2() {
    cout << value << " ";
}

print1();
print2();
```

A. 100 100 100
B. 50 50 50
C. 50 100 100
D. 50 50 100

答案：C

解析：C 项「50 100 100」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

小杨在整理一副扑克牌的所有红心扑克牌，使其从小到大排列。他的做法是：最开始抓到第 1 张扑克牌被认为已经排好序；然后抓第 2 张扑克牌，将其插入至有序部分的正确位置；不断循环步骤，每次将新抓到扑克牌插入至有序部分，直至抓完所有扑克牌，这样抓牌结束时就完成了扑克牌的排序。小杨这种整理扑克牌的方式与（ ）排序的方式最接近。

A. 冒泡排序
B. 插入排序
C. 选择排序
D. 直接排序

答案：B

解析：B 项「插入排序」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

以下哪种情况是使用插入排序的合适场景？

A. 数据量非常大，且乱序严重
B. 希望获得稳定排序，但不要求实时性
C. 数据几乎有序，只需少量调整
D. 想在交换次数最少的前提下排好大数组

答案：C

解析：C 项「数据几乎有序，只需少量调整」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

以下关于递推算法基本思想的描述，正确的是（ ）。

A. 递推算法通过将问题分解为相互独立的子问题来解决。
B. 递推算法从已知的基础情况出发，通过某种关系逐步推导出更大规模问题的解。
C. 递推算法通常用于穷举所有可能的解决方案。
D. 递推算法适用于在每一步做出局部最优选择以达到全局最优。

答案：B

解析：B 项「递推算法从已知的基础情况出发，通过某种关系逐步推导出更大规模问题的解。」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

给定如下算法，其时间复杂度为（ ）。

```cpp
bool f(int arr[], int n, int target) {
    for (int i = 0; i < n; i++) {
        int sum = 0;
        for (int j = 0; j < n; j++) {
            if (i & (1 << j)) {
                sum += arr[j];
            }
        }
        if (sum == target) return true;
    }
    return false;
}
```

A. $O(n)$
B. $O(n^2)$
C. $O(n^3)$
D. $O(2^n)$

答案：B

解析：B 项「O(n^2)」符合题干要求。复杂度题要统计循环执行次数或递推式的总规模，忽略常数和低阶项后得到渐近量级。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

下述斐波那契数列计算的时间复杂度是（ ）。

```cpp
int fibonacci(int n) {
    if (n == 0) return 0;
    if (n == 1) return 1;
    return fibonacci(n - 1) + fibonacci(n - 2);
}
```

A. $O(n)$
B. $O(n^2)$
C. $O(n^3)$
D. $O(2^n)$

答案：D

解析：D 项「O(2^n)」符合题干要求。递归题要先看终止条件，再按递推式逐层计算；缺少终止条件会导致无限递归或栈溢出。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

关于下面 C++ 程序的描述，（ ）最准确。

```cpp
ifstream in("data.txt");
string line;
while (getline(in, line)) {
    cout << line << endl;
}
```

A. 将从标准输入读取每行，并输出到屏幕
B. 程序无法运行，因为 getline 只能读取 cin
C. 将 data.txt 中的每一行读取并输出到屏幕
D. 程序将创建 data.txt 并写入默认文本

答案：C

解析：C 项「将 data.txt 中的每一行读取并输出到屏幕」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

在 C++ 中，异常处理机制（try-catch 块）的主要目的是（ ）。

A. 提高程序的运行速度。
B. 在程序发生运行时错误时，提供一种结构化的错误处理方式。
C. 确保程序在编译时没有错误。
D. 减少程序的内存占用。

答案：B

解析：B 项「在程序发生运行时错误时，提供一种结构化的错误处理方式。」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

为了提高冒泡排序的效率，如果某轮“冒泡”中没有执行任何交换操作，说明数组已经完成排序，可直接返回结果，则两条横线上分别应该填写（ ）。

```cpp
void bubbleSortWithFlag(vector<int> &nums) {
    for (int i = nums.size() - 1; i > 0; i--) {
        bool flag;
        // 在此处填入代码

        for (int j = 0; j < i; j++) {
            if (nums[j] > nums[j + 1]) {
                swap(nums[j], nums[j + 1]);
                // 在此处填入代码
            }
        }
        if (!flag)
            break;
    }
}
```

A.
```cpp
flag = false;
flag = false;
```

B.
```cpp
flag = false;
flag = true;
```

C.
```cpp
flag = true;
flag = false;
```

D.
```cpp
flag = true;
flag = true;
```

答案：B

解析：B 项「代码片段」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。





