# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1183
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1699464809021472.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

C++ 中 `string` 的 `==` 运算符比较的是字符串的内存地址，而非内容。

【知识点：字符串处理】

答案：错误

解析：题干表述不成立。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第2题

`string` 的 `substr(1, 3)` 返回从下标 $1$ 开始的 $3$ 个字符的子串。

【知识点：数组与字符数组】

答案：正确

解析：题干表述成立。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第3题

$x$ 是浮点数，`(x >> 1)` 等价于 `x / 2`。

【知识点：位运算】

答案：错误

解析：题干表述不成立。表达式和控制结构题要按运算符优先级、整数除法、取余规则以及循环控制语句逐步执行。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第4题

`string("hello") == "hello"` 的比较结果为 `true`。

【知识点：字符串处理】

答案：正确

解析：题干表述成立。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第5题

`sort` 可以直接用于排序 `set` 中的元素。

【知识点：枚举与基础算法】

答案：错误

解析：题干表述不成立。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第6题

`(x & 1) == 0` 可以判断整数 $x$ 是否为偶数。

【知识点：位运算】

答案：正确

解析：题干表述成立。指针和引用题要区分地址、解引用和别名关系；引用初始化后绑定原变量，给引用赋值就是修改原变量。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第7题

`string` 的 `substr(2, 10)` 在字符串长度不足时会抛出异常。

【知识点：字符串处理】

答案：错误

解析：题干表述不成立。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第8题

在数学纸面计算中，`pow(2, 3)` 的计算结果一定是 $8$，但是在 C++ 中，如果遇到数据类型是浮点数，那就不一定正确。

【知识点：函数与标准库】

答案：正确

解析：题干表述成立。数学库函数题要注意函数参数含义和返回类型，`sqrt`、`pow`、三角函数等通常返回 `double`。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第9题

在 C++ 中，枚举的底层类型可以是非整型（如 `float` 或 `double`）。

【知识点：枚举与基础算法】

答案：错误

解析：题干表述不成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第10题

函数声明 `double f();` 返回 `int` 时，会自动转换为 `double`。

【知识点：函数与标准库】

答案：正确

解析：题干表述成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。





