# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1166
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1650239656165408.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

在 Windows 的资源管理器中为已有文件 A 建立副本的操作是 `Ctrl+C`，然后 `Ctrl+V`。（ ）

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第2题

在 C++ 代码中，假设 $N$ 为正整数，则 `cout << (N - N / 10 * 10)` 将获得 $N$ 的个位数。（ ）

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第3题

在 C++ 语句 `cout << (10 <= N <= 12)` 中，假设 N 为 12，则其输出为 1。（ ）

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第4题

如果 C++ 表达式 `int(sqrt(N))*int(sqrt(N)) == N` 的值为 True，则说明 $N$ 为完全平方数，如 4、9、25 等。（ ）

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第5题

下面 C++ 代码执行后将输出 `2*3=6`。（ ）

```cpp
int a = 2, b = 3;
printf("%%a*%%b=%d", a*b);
```

答案：错误

解析：题干说法错误。题目中的表述与 C++ 实际语法、表达式求值、输入输出格式、控制流程或计算机基础概念不一致，或把适用条件说得过于绝对，因此不能判为正确。

## 第6题

以下 C++ 代码因为循环变量为 `_` 将导致错误，即 `_` 不能作为变量名称，不符合 C++ 变量命名规范。（ ）

```cpp
for (int _ = 0; _ < 10; _++)
    continue;
```

答案：错误

解析：题干说法错误。题目中的表述与 C++ 实际语法、表达式求值、输入输出格式、控制流程或计算机基础概念不一致，或把适用条件说得过于绝对，因此不能判为正确。

## 第7题

下面 C++ 代码执行后因为有 `break`，将输出 0。（ ）

```cpp
int i;
for (i = 0; i < 10; i++){
    continue;
    break;
}
cout << i;
```

答案：错误

解析：题干说法错误。题目中的表述与 C++ 实际语法、表达式求值、输入输出格式、控制流程或计算机基础概念不一致，或把适用条件说得过于绝对，因此不能判为正确。

## 第8题

下面的 C++ 代码执行后将输出 18 行 “OK”。（ ）

```cpp
int i,j;
for (i = 8; i > 2; i-=2)
    for (j =0; j < i; j++)
        printf("OK\n");
```

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第9题

将下面 C++ 代码中的 `i = 1` 调整为 `i = 0` 的输出结果相同。（ ）

```cpp
int i;
int cnt = 0;
for (i = 1; i < 5; i++)
    if(i%2) cnt += 1;
cout << cnt;
```

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。

## 第10题

下面两段 C++ 代码都是用于求 1-10 的和，其运行结果相同。通常说来，for 循环都可以用 while 循环实现。（ ）

```cpp
int tnt;
int i;

tnt = 0;
for (i = 1; i < 10 + 1; i++)
    tnt += i;
cout << tnt << endl;
```

```cpp
int tnt;
int i;

tnt = 0;
i = 1;
while (i <= 10){
    tnt += i;
    i += 1;
}
cout << tnt << endl;
```

答案：正确

解析：题干说法正确。按题目涉及的 C++ 语法、表达式求值、输入输出格式、控制流程或计算机基础概念判断，题干中的结论与实际规则一致。