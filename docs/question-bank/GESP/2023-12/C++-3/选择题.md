# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1135
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1584917876047904.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

下面 C++ 数组的定义中，会丢失数据的是

【知识点：数组与字符数组】

A. `char dict_key[] = {'p','t','o'};`
B. `int dict_value[] = {33,22,11};`
C. `char dict_name[]={'chen','wang','zhou'};`
D. `float dict_value[]={3,2,1};`

答案：C

## 第2题

在下列编码中，不能够和二进制"1101 1101"相等的是()。

【知识点：进制转换与进制字面量】

A. $(221)_{10}$ 进制
B. $(335)_8$ 进制
C. $(dd)_{16}$ 进制
D. $(5d)_{16}$ 进制

答案：D

## 第3题

下面 C++ 代码执行后不能输出"GESP"的是()。

【知识点：字符串处理】

A. `string str("GESP"); cout<<str<<endl;`
B. `string str="GESP"; cout<<str<<endl;`
C. `string str("GESP"); cout<<str[1]<<str[2]<<str[3]<<str[4]<<endl;`
D. `string str{"GESP"}; cout<<str<<endl;`

答案：C

## 第4题

执行下面C++代码输出是()。
```cpp
int temp=0;
for(int i=1;i<7;i++)
{
    for(int j=1;j<5;j++)
    {
        if(i/j==2)
        {
            temp++;
        }
    }
}
cout<<temp<<endl;
```

【知识点：控制结构与程序阅读】

A. 10
B. 8
C. 4
D. 3

答案：C

## 第5题

执行下面 C++ 代码后，输出是()。
```cpp
string str=("chen");
int x=str.length();
int temp=0;
for(int i=0;i<=x;i++)
{
    temp++;
}
cout<<temp<<endl;
```

【知识点：字符串处理】

A. $4$
B. $2$
C. $5$
D. $3$

答案：C

## 第6题

第6题 执行下面 C++ 代码后输出的是()。
```cpp
string str=("chen");
int x=str.length();
cout<<x<<endl;
```

【知识点：字符串处理】

A. $4$
B. $3$
C. $2$
D. $5$

答案：A

## 第7题

执行下面 C++ 代码后输出的是()。
```cpp
string str=("chen");
cout<<str[5]<<endl;
```

【知识点：字符串处理】

A. 输出未知的数
B. 输出'n'
C. 输出'\0'
D. 输出空格

答案：A

## 第8题

下面 C++ 代码执行后的输出是()。
```cpp
char ch[10]={'1'};
cout<<ch[2]<<endl;
```

【知识点：数组与字符数组】

A. $0$
B. $1$
C. 输出空格
D. 什么也不输出

答案：D

## 第9题

下面 C++ 代码用于统计每种字符出现的次数，当输出为 $3$ 时，横线上不能填入的代码是()。
```cpp
string str="GESP is a good programming test!";
int x=0;
for(int i=0;i<str.length();i++)
{
    if(_________)
    {
        x++;
    }
}
cout<<x<<endl;
```

【知识点：字符串处理】

A. `str[i]=='o'`
B. `str[i]=='a'+14`
C. `str[i]==115`
D. `str[i]==111`

答案：C

## 第10题

32 位计算机中，C++ 的整型变量 int 能够表示的数据范围是()。

【知识点：原码反码补码与整数表示】

A. $2^{31} \sim (2^{31})-1$
B. $2^{32}$
C. $-2^{31} \sim +(2^{31})-1$
D. $-(2^{31})+1 \sim 2^{31}$

答案：C

## 第11题

下面 C++ 程序执行的结果是()。

```cpp
int cnt=0;
for(int i=0;i<=20;i++)
{
    if(i%3==0&&i%5==0)
    {
        cnt++;
    }
}
cout<<cnt;
```

【知识点：控制结构与程序阅读】

A. $2$
B. $3$
C. $5$
D. $4$

答案：A

## 第12题

C++ 的数据类型转换让人很难琢磨透，下列代码输出的值是()。

```cpp
int a=3;
int b=2;
cout<<a/b*1.0<<endl;
```

【知识点：运算符、表达式与类型转换】

A. $1.5$
B. $1$
C. $2$
D. $1.50$

答案：B

## 第13题

C++ 代码用于抽取字符串中的电话号码。约定：电话号码全部是数字，数字之间没有其他符号如连字符或空格等。代码中变量 strSrc 仅仅是示例，可以包含更多字符。下面有关代码说法，正确的说法是()。
```cpp
string strSrc="红十字：01084025890火警电话：119急救电话：120紧急求助：110";
string tel="";
for(int i=0;i<=strSrc.length();i++)
{
    if(strSrc[i]>='0'&&strSrc[i]<='9')
    {
        tel=tel+strSrc[i];
    }
    else if(tel!="")
    {
        cout<<tel<<endl;
        tel="";
    }
}
```

【知识点：ASCII/字符编码】

A. 代码将换行输出各个含有数字的电话号码。
B. 代码将不换行输出各个含有数字的电话号码，号码中间没有分隔。
C. 代码将不换行输出各个含有数字的电话号码，号码中间有分隔。
D. 不能够输出数字电话号码。

答案：A

## 第14题

某公司新出了一款无人驾驶的小汽车，通过声控智能驾驶系统，乘客只要告诉汽车目的地，车子就能自动选择一条优化路线，告诉乘客后驶达那里。请问下面哪项不是驾驶系统完成选路所必须的。()

【知识点：计算机基础常识】

A. 麦克风
B. 扬声器
C. 油量表
D. 传感器

答案：C

## 第15题

现代计算机是指电子计算机，它所基于的是()体系结构。

【知识点：计算机基础常识】

A. 艾伦·图灵
B. 冯·诺依曼
C. 阿塔纳索夫
D. 埃克特-莫克利

答案：B
