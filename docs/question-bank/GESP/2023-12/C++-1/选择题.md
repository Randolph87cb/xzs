# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1133
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1585703601307680.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

以下 C++ 不可以作为变量的名称的是（）。

A. CCF GESP
B. ccfGESP
C. CCFgesp
D. CCF_GESP

答案：A

解析：本题选 A。选项 A“CCF GESP”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第2题

C++ 表达式 `10 - 3 * (2 + 1) % 10` 的值是（）。

A. 0
B. 1
C. 2
D. 3

答案：B

解析：本题选 B。选项 B“1”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第3题

假设现在是上午十点，求出 $N$ 小时（正整数）后是第几天几时，如输入 $20$ 小时则为第 $2$ 天 $6$ 点，如 $N$ 输入 $4$ 则为今天 $14$ 点。为实现相应功能，应在横线处填写代码是（）。

```cpp
int N, dayX, hourX;
cin >> N;
dayX = _____, hourX = _____;
if (dayX == 0)
    cout << "今天" << hourX << "点";
else
    cout << "第" << (dayX + 1) << "天" << hourX << "点";
```

A. ` (10 + N) % 24 `, ` (10 + N) / 24 `
B. ` (10 + N) / 24 `, ` (10 + N) % 24 `
C. ` N % 24 `, ` N / 24 `
D. ` 10 / 24 `, ` 10 % 24 `

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 ` (10 + N) / 24 `, ` (10 + N) % 24 ` 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第4题

下面的程序用于判断 $N$ 是否为偶数，横线处应填写代码是（ ）。

```cpp
cin >> N;
if (______)
    cout << "偶数";
else
    cout << "奇数";
````

A. ` N % 2 == 0 `
B. ` N % 2 = 0 `
C. ` N % 2 `
D. ` N % 2 != 0 `

答案：A

解析：本题选 A。该表达式准确刻画了题目要求的条件；其他选项存在链式比较含义错误、逻辑连接错误或只覆盖了部分情况的问题。

## 第5题

下面对 C++ 代码执行后输出的描述，正确的是（ ）。

```cpp
cin >> N;
cnt = 0;
for(int i = 1; i < N; i++)
    cnt += 1;
cout << cnt;
```

A. 如果输入的 $N$ 是小于等于 $2$ 整数，第 $5$ 行将输出 $0$。
B. 如果输入的 $N$ 是大于等于 $2$ 整数，第 $5$ 行将输出 $N-1$。
C. 如果输入的 $N$ 是大于等于 $2$ 整数，第 $5$ 行将输出 $N$。
D. 以上说法均不正确。

答案：B

解析：本题选 B。选项 B“如果输入的 $N$ 是大于等于 $2$ 整数，第 $5$ 行将输出 $N-1$。”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第6题

下面 C++ 代码执行后的输出是（ ）。

```cpp
cnt = 0;
for(int i = 1; i < 10; i++){
    cnt += 1;
    i += 2;
}
cout << cnt;
```

A. 10
B. 9
C. 3
D. 1

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 3 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第7题

下面 C++ 代码执行后的输出是（ ）。

```cpp
cnt = 0;
for(int i = 1; i < 20; i++){
    if(i%2)
        continue;
    else if(i%3==0 && i%5==0)
        break;
    cnt += i;
}
cout << cnt;
```

A. 90
B. 44
C. 20
D. 10

答案：A

解析：本题选 A。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 90 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第8题

下面 C++ 代码执行后的输出是（ ）。

```cpp
N = 10;
cnt = 0;
while(1){
    if(N == 0) break;
    cnt += 1;
    N -= 2;
}
cout << cnt;
```

A. 11
B. 10
C. 5
D. 4

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 5 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第9题

下面 C++ 代码用于判断一个数是否为质数（素数），在横线处应填入代码是（ ）。

```cpp
cin >> N;
cnt = 0;
for (int i = 1; i < N + 1; i++)
    if (N % i == 0)
        _____;
if (cnt == 2)
    cout << N << "是质数。";
else
    cout << N << "不是质数。";
```

A. ` cnt = 1 `
B. ` cnt = 2 `
C. ` cnt =+ 1 `
D. ` cnt += 1 `

答案：D

解析：本题选 D。该表达式准确刻画了题目要求的条件；其他选项存在链式比较含义错误、逻辑连接错误或只覆盖了部分情况的问题。

## 第10题

对下面的代码，描述正确的是（ ）。

```cpp
#include <stdlib.h>
using namespace std;

int main(){
    int arr[] = {2,6,3,5,4,8,1,0,9,10};
    for(int i = 0; i < 10; i++)
        cout << arr[i] << " ";
    cout << i << endl;
    cout << endl;
    return 0;
}
```

A. 输出 2 6 3 5 4 8 1 0 9 10 10
B. 输出 2 6 3 5 4 8 1 0 9 9
C. 输出 2 6 3 5 4 8 1 0 9 10
D. 提示有编译错误

答案：D

解析：本题选 D。选项 D“提示有编译错误”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第11题

执行下面 C++ 代码后输出是（ ）。

```cpp
cnt = 0;
for (i = 10; i > 3; i -= 3)
    cnt = cnt + i;
cout << cnt;
```

A. 3
B. 21
C. 27
D. 49

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 21 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第12题

定义变量 `char c`，下面对 c 赋值的语句，不符合语法的是（ ）。

A. ` c = (char)66; `
B. ` c = (char)(66); `
C. ` c = char(66); `
D. ` c = char 66; `

答案：D

解析：本题选 D。该选项违反 C++ 语法规则，例如赋值左侧必须是可修改的对象、运算符用法必须合法；其他选项可以按隐式转换或正常表达式规则通过。

## 第13题

下图所示 C++ 代码对大写字母 'A' 到 'Z' 分组，对每个字母输出所属的组号，那么输入 'C' 时将输出的组号是（ ）。

```cpp
char c;
while(1){
    cin >> c;
    if(c=='q') break;
    switch(c){
        case 'A': cout << "1 "; break;
        case 'B': cout << "3 ";
        case 'C': cout << "3 ";
        case 'D': cout << "5 "; break;
        case 'E': cout << "5 "; break;
        default: cout << "9 ";
    }
    cout << endl;
}
```

A. 3
B. 3 5
C. 3 5 9
D. 以上都不对

答案：B

解析：本题选 B。该表达式准确刻画了题目要求的条件；其他选项存在链式比较含义错误、逻辑连接错误或只覆盖了部分情况的问题。

## 第14题

某公司新出了一款无人驾驶的小汽车，通过声控智能驾驶系统，乘客只要告诉汽车目的地，车子就能自动选择一条优化路线，告诉乘客后驶达那里。请问下面哪项不是驾驶系统完成选路所必须的。（ ）

A. 麦克风
B. 扬声器
C. 油量表
D. 传感器

答案：C

解析：本题选 C。选项 C“油量表”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第15题

现代计算机是指电子计算机，它所基于的是（ ）体系结构。

A. 艾伦·图灵
B. 冯·诺依曼
C. 阿塔纳索夫
D. 埃克特-莫克利

答案：B

解析：本题选 B。选项 B“冯·诺依曼”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。
