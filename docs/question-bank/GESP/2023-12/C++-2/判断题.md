# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1134
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1601261860290592.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

C++ 表达式 `-7/2` 的值为整数 $-3$。( )

答案：正确

## 第2题

C++ 表达式 `2*int('9')*2` 的值为 $36$。( )

答案：错误

## 第3题

C++ 表达式 `3+2 && 5-5` 的值为 `false`。( )

答案：正确

## 第4题

在 C++ 代码中，执行 `srand(0)` 后连续两次执行 `rand()` 的结果相等。( )

答案：错误

## 第5题

C++ 代码中 `while(1){...}` 的判断条件不是逻辑值，将导致语法错误。( )

答案：错误

## 第6题

执行以下 C++ 代码后将输出 $0$。( )

```cpp
Sum = 0;
for (i = -500; i < 500; i++)
    Sum += i;
cout << Sum;
```

答案：错误

## 第7题

在 C++ 代码中，运算符只能处理相同的数据类型，不同类型之间必须转换为相同的数据类型。( )

答案：错误

## 第8题

在 C++ 代码中，虽然变量都有数据类型，但同一个变量也可以先后用不同类型的值赋值。( )

答案：正确

## 第9题

小杨最近在准备考 GESP，他用的 Dev C++ 来练习和运行程序，所以 Dev C++ 也是一个小型操作系统。( )

答案：错误

## 第10题

任何一个 while 循环都可以转化为等价的 for 循环。( )

答案：正确
