# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1140
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1584918480027680.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

小杨要从 A 城到 B 城，又想顺路游览一番。他有两个选项：1、坐高铁路到 C 城游览，再坐高铁或飞机到 B 城；2、坐船到 D 城游览，再坐船、高铁或飞机到 B 城。请问小杨从 A 城到 B 城共有几种交通方案可以选择？（ ）。

A. $2$
B. $3$
C. $5$
D. $6$

答案：C

解析：本题选 C。按题意列式并注意整数除法与实数运算的区别，$5$ 能得到所需结果；其他选项存在运算顺序、公式或类型转换问题。

## 第2题

以下哪个函数声明是符合语法的，且在调用时可以将二维数组的名字作为实际参数传递给形式参数 $a$？（ ）。

A. `void QuickSort(int a[][10], int n);`
B. `void QuickSort(int a[5][], int m);`
C. `void QuickSort(int a[][], int n, int m);`
D. `void QuickSort(int ** a, int n, int m);`

答案：A

解析：本题选 A。该选项违反 C++ 语法规则，例如赋值左侧必须是可修改的对象、运算符用法必须合法；其他选项可以按隐式转换或正常表达式规则通过。

## 第3题

下面有关 C++ 类和对象的说法，错误的是（ ）。

A. 对象的生命周期开始时，会执行构造函数。
B. 对象的生命周期结束时，会执行析构函数。
C. 类的析构函数可以为虚函数。
D. 类的构造函数可以为虚函数。

答案：D

解析：本题选 D。选项 D“类的构造函数可以为虚函数。”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第4题

使用邻接矩阵表达 $n$ 个顶点的有向图，则该矩阵的大小为（ ）。

A. $n \times (n + 1)$
B. $n \times n$
C. $n \times (n - 1)$
D. $n \times (n - 1)/2$

答案：B

解析：本题选 B。按图的定义和遍历/存储规则分析，$n \times n$ 符合题目条件；其他选项忽略了方向、连通性或邻接结构带来的限制。

## 第5题

$5$ 位同学排队，其中一位同学不能排在第一，则共有多少种可能的排队方式？（ ）。

A. $5$
B. $24$
C. $96$
D. $120$

答案：C

解析：本题选 C。选项 C“$96$”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第6题

一个无向图包含 $n$ 个顶点，则其最小生成树包含多少条边？（ ）。

A. $n - 1$
B. $n$
C. $n + 1$
D. 最小生成树可能不存在。

答案：D

解析：本题选 D。依据树的定义、遍历序列或完全二叉树性质推导，最小生成树可能不存在。 符合结构关系；其他选项与节点位置、叶子数量或遍历规则不一致。

## 第7题

已知三个 `double` 类型的变量 $a$、$b$ 和 $theta$ 分别表示一个三角形的两条边长及二者的夹角（弧度），则下列哪个表达式可以计算这个三角形的面积？（ ）。

A. `a * b * sin(theta) / 2`
B. `(a + b) * sin(theta) / 2`
C. `a * b * cos(theta) / 2`
D. `sqrt(a * a + b * b - 2 * a * b * cos(theta))`

答案：A

解析：本题选 A。按题意列式并注意整数除法与实数运算的区别，`a * b * sin(theta) / 2` 能得到所需结果；其他选项存在运算顺序、公式或类型转换问题。

## 第8题

对有 $n$ 个元素的二叉排序树进行中序遍历，其时间复杂度是（ ）。

A. $O(1)$
B. $O(\log(n))$
C. $O(n)$
D. $O(n^2)$

答案：C

解析：本题选 C。根据循环次数、递归规模或访问的数据结构规模估算，主导项对应 $O(n)$；其他选项高估或低估了主要操作次数。

## 第9题

假设输入参数 $m$ 和 $n$ 满足 $m \leq n$，则下面程序的最差情况的时间复杂度为（ ）。

```cpp
int gcd(int m, int n) {
    while (m > 0) {
        int t = m;
        m = n % m;
        n = t;
    }
    return n;
}
```

A. $O(\log(n))$
B. $O(n)$
C. $O(n \times m)$
D. $O(m \times \log(n))$

答案：A

解析：本题选 A。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 $O(\log(n))$ 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第10题

下面程序的时间复杂度为（ ）。

```cpp
long long power_mod(long long a, long long n, long long mod) {
    if (n == 0)
        return 1;
    a = a % mod;
    if (n == 1)
        return a;
    long long pw = power_mod(a, n / 2, mod);
    long long pw2 = pw * pw % mod;
    if (n % 2 == 0)
        return pw2;
    return pw2 * a % mod;
}
```

A. $O(n)$
B. $O(a^n)$
C. $O(\log(n))$
D. $O(\log(n) \times a)$

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 $O(\log(n))$ 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第11题

下面程序的时间复杂度为（ ）。

```cpp
int record_choose[MAXN][MAXM];
int choose(int n, int m) {
    if (m == 0 || m == n)
        return 1;
    if (record_choose[n][m] == 0)
        record_choose[n][m] = choose(n - 1, m - 1) + choose(n - 1, m);
    return record_choose[n][m];
}
```

A. $O(2^n)$
B. $O(2^m \times (n - m))$
C. $O(C(n, m))$
D. $O(m \times (n - m))$

答案：D

解析：本题选 D。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 $O(m \times (n - m))$ 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第12题

下面的程序使用出边的邻接表表达有向图，则下列选项中哪个是它表达的图？（ ）。

```cpp
#include <iostream>

struct Edge {
    int e;
    Edge * next;
};

struct Node {
    Edge * first;
};

int main() {
    Edge e[5] = {{1, nullptr}, {2, &e[2]},
                 {3, nullptr}, {3, nullptr}, {0, nullptr}};
    Node n[4] = {&e[0], &e[1], &e[3], &e[4]};
    // 其他处理
    return 0;
}
```

![](https://cdn.luogu.com.cn/upload/image_hosting/6psrqkmt.png)


A. A
B. B
C. C
D. D

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 B 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第13题

下面程序的输出为（ ）。

```cpp
#include <iostream>
using namespace std;

int main() {
    int cnt = 0;
    for (int a = 1; a <= 10; a++)
        for (int b = 1; b <= 10; b++)
            for (int h = 1; h <= 10; h++)
                if ((a + b) * h == 20)
                    cnt++;
    cout << cnt << endl;
    return 0;
}
```

A. 12
B. 18
C. 36
D. 42

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 18 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第14题

下面程序的输出为（ ）。

```cpp
#include <iostream>
using namespace std;

int main() {
    const int N = 30;
    int cnt = 0;
    for (int a = 1; a <= N; a++)
        for (int b = a; a + b <= N; b++)
            for (int c = b; a + b + c <= N; c++)
                if (a * a + b * b == c * c)
                    cnt++;
    cout << cnt << endl;
    return 0;
}
```

A. 3
B. 6
C. 11
D. 22

答案：A

解析：本题选 A。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 3 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第15题

下面的程序中，二维数组 $h$ 和 $v$ 分别代表如下图所示的网格中的水平边的时间消耗和垂直边的时间消耗。程序使用动态规划计算从左下角到右上角的最小时间消耗，则横线处应该填写下列哪个选项的代码？（ ）。

![](https://cdn.luogu.com.cn/upload/image_hosting/ia7a4flz.png)

```cpp
int dis[MAXY][MAXX];
int shortest_path(int x, int y) {
    dis[0][0] = 0;
    for (int i = 0; i < y; i++)
        dis[i + 1][0] = dis[i][0] + v[i][0];
    for (int j = 0; j < x; j++)
        dis[0][j + 1] = dis[0][j] + h[0][j];
    for (int i = 0; i < y; i++)
        for (int j = 0; j < x; j++)
            __________________; // 在此处填写代码
    return dis[y][x];
}
```

A. `dis[i][j] = min(dis[i - 1][j] + v[i - 1][j], dis[i][j - 1] + h[i][j - 1]);`    
B. `dis[i][j] = min(dis[i - 1][j] + h[i - 1][j], dis[i][j - 1] + v[i][j - 1]);`    
C. `dis[i + 1][j + 1] = min(dis[i][j + 1] + v[i][j + 1], dis[i + 1][j] + h[i + 1][j]);`     
D. `dis[i + 1][j + 1] = min(dis[i][j + 1] + h[i][j + 1], dis[i + 1][j] + v[i + 1][j]);`

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 `dis[i + 1][j + 1] = min(dis[i][j + 1] + v[i][j + 1], dis[i + 1][j] + h[i + 1][j]);` 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。
