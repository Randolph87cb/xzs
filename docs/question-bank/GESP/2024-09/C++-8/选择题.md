# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1164
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1633836360269856.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

下面关于 C++ 类和对象的说法，错误的是（ ）。

A. 类的析构函数可以为虚函数。
B. 类的构造函数不可以为虚函数。
C. class 中成员的默认访问权限为 private。
D. struct 中成员的默认访问权限为 private。

答案：D

解析：本题答案选 D。选项 D 的内容「struct 中成员的默认访问权限为 private。」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第2题

对于一个具有 $n$ 个顶点的无向图，若采用邻接矩阵表示，则该矩阵的大小为（ ）。

A. $n \times \frac{n}{2}$
B. $n \times n$
C. $(n - 1) \times (n - 1)$
D. $(n + 1) \times (n + 1)$

答案：B

解析：本题答案选 B。选项 B 的内容「$n \times n$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第3题

设有编号为 A、B、C、D、E 的 5 个球和编号为 A、B、C、D、E 的 5 个盒子。现将这 5 个球投入 5 个盒子，要求每个盒子放一个球，并且恰好有两个球的编号与盒子编号相同，问有多少种不同的方法？（ ）。

A. 5
B. 120
C. 20
D. 60

答案：C

解析：本题答案选 C。选项 C 的内容「20」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第4题

从甲地到乙地，可以乘高铁，也可以乘汽车，还可以乘轮船。一天中，高铁有 10 班，汽车有 5 班，轮船有 2 班。那么一天中乘坐这些交通工具从甲地到乙地共有多少种不同的走法？（ ）。

A. 100
B. 60
C. 30
D. 17

答案：D

解析：本题答案选 D。选项 D 的内容「17」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第5题

$n$ 个结点的二叉树，执行释放全部结点操作的时间复杂度是（ ）。

A. $O(n)$
B. $O(n \log n)$
C. $O(\log n)$
D. $O(2^n)$

答案：A

解析：本题答案选 A。选项 A 的内容「$O(n)$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第6题

在一个单位圆上，随机分布 $n$ 个点，求这 $n$ 个点能被一个单位半圆周全部覆盖的概率（ ）。

A. $\frac{n}{2^{n-1}}$
B. $\frac{1}{n^2}$
C. $\frac{1}{n}$
D. $\frac{1}{2^n}$

答案：A

解析：本题答案选 A。选项 A 的内容「$\frac{n}{2^{n-1}}$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第7题

下面 `pailie` 函数是一个实现排列的程序，横线处可以填入的是（）。

```cpp
#include <iostream>
using namespace std;
int sum = 0;
void swap(int & a, int & b) {
    int temp = a;
    a = b;
    b = temp;
}
void pailie(int begin, int end, int a[]) {
    if (begin == end) {
        for (int i = 0; i < end; i++)
            cout << a[i];
        cout << endl;
    }
    for (int i = begin; i < end; i++) {
        __________ // 在此处填入选项
    }
}
```

![](https://cdn.luogu.com.cn/upload/image_hosting/ocj7nnf1.png)


A. A
B. B
C. C
D. D

答案：C

解析：本题答案选 C。选项 C 的内容「C」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第8题

上一题中，如果主函数为如下的程序，则最后的排列数是多少个？（ ）。

```cpp
int main() {
    int a[5] = {1, 2, 3, 4, 5};
    pailie(0, 5, a);
    return 0;
}
```

A. 120
B. 60
C. 240
D. 180

答案：A

解析：本题答案选 A。选项 A 的内容「120」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第9题

下列程序实现了输出杨辉三角形，代码中横线部分应该填入的是（ ）。

```cpp
#include <iostream>
using namespace std;
#define N 35
int a[N][N];
int main() {
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++)
        for (int j = 1; j <= i; j++) {
            if (j == 1 || j == i)
                a[i][j] = 1;
            else
                ________ // 在此处填入选项
        }
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= i; j++)
            cout << a[i][j];
        cout << endl;
    }
    return 0;
}
```

A. `a[i][j] = a[i - 1][j - 1] + a[i - 1][j];`
B. `a[i][j] = a[i][j - 1] + a[i - 1][j];`
C. `a[i][j] = a[i - 1][j] + a[i - 1][j];`
D. `a[i][j] = a[i - 1][j - 1] + a[i][j];`

答案：A

解析：本题答案选 A。选项 A 的内容「`a[i][j] = a[i - 1][j - 1] + a[i - 1][j];`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第10题

下面最小生成树的 Kruskal 算法程序中，横线处应该填入的是（ ）。

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
struct Edge {
    int u, v, weight;
    bool operator <(const Edge & other) const {
        return weight < other.weight;
    }
};
int findParent(int vertex, vector<int> & parent) {
    if (parent[vertex] == -1)
        return vertex;
    return parent[vertex] = findParent(parent[vertex], parent);
}
int main() {
    int n, m;
    cin >> n >> m; // n: 顶点数, m: 边数
    vector<Edge> edges(m);
    vector<int> parent(n, -1);
    int totalWeight = 0;
    for (int i = 0; i < m; i++)
        cin >> edges[i].u >> edges[i].v >> edges[i].weight;
    sort(edges.begin(), edges.end());

    for (const auto & edge : edges) {
        int uParent = findParent(edge.u, parent);
        int vParent = findParent(edge.v, parent);
        if (________) { // 在此处填入选项
            parent[uParent] = vParent;
            totalWeight += edge.weight;
        }
    }
}
```

A. `uParent == vParent`
B. `uParent >= vParent`
C. `uParent != vParent`
D. `uParent <= vParent`

答案：C

解析：本题答案选 C。选项 C 的内容「`uParent != vParent`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第11题

下面 Prim 算法程序中，横线处应该填入的是（）。

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int prim(vector<vector<int>> & graph, int n) {
    vector<int> key(n, INT_MAX);
    vector<int> parent(n, -1);

    key[0] = 0;
    for (int i = 0; i < n; i++) {
        int u = min_element(key.begin(), key.end()) - key.begin();
        if (key[u] == INT_MAX)
            break;
        for (int v = 0; v < n; v++) {
            if (__________) { // 在此处填入选项
                key[v] = graph[u][v];
                parent[v] = u;
            }
        }
    }
    int sum = 0;
    for (int i = 0; i < n; i++) {
        if (parent[i] != -1) {
            cout << "Edge: " << parent[i] << " - " << i << " Weight: " << key[i] << endl;
            sum += key[i];
        }
    }
    return sum;
}
int main() {
    int n, m;
    cin >> n >> m;
    vector<vector<int>> graph(n, vector<int>(n, 0));
    for (int i = 0; i < m; i++) {
        int u, v, w;
        cin >> u >> v >> w;
        graph[u][v] = w;
        graph[v][u] = w;
    }
    int result = prim(graph, n);
    cout << "Total weight of the minimum spanning tree: " << result << endl;
    return 0;
}
```

A. `graph[u][v] >= 0 && key[v] > graph[u][v]`
B. `graph[u][v] <= 0 && key[v] > graph[u][v]`
C. `graph[u][v] == 0 && key[v] > graph[u][v]`
D. `graph[u][v] != 0 && key[v] > graph[u][v]`

答案：D

解析：本题答案选 D。选项 D 的内容「`graph[u][v] != 0 && key[v] > graph[u][v]`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第12题

下列 Dijkstra 算法中，横线处应该填入的是（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 100
int n, e, s;
const int inf = 0x7fffffff;
int dis[N + 1];
int cheak[N + 1];
int graph[N + 1][N + 1];

int main() {
    for (int i = 1; i <= N; i++)
        dis[i] = inf;
    cin >> n >> e;
    for (int i = 1; i <= e; i++) {
        int a, b, c;
        cin >> a >> b >> c;
        graph[a][b] = c;
    }
    cin >> s;
    dis[s] = 0;
    for (int i = 1; i <= n; i++) {
        int minn = inf, minx;
        for (int j = 1; j <= n; j++) {
            if (________) { // 在此处填入选项
                minn = dis[j];
                minx = j;
            }
        }
        cheak[minx] = 1;
        for (int j = 1; j <= n; j++) {
            if (graph[minx][j] > 0) {
                if (minn + graph[minx][j] < dis[j]) {
                    dis[j] = minn + graph[minx][j];
                }
            }
        }
    }
}
```

A. `dis[j] > minn && cheak[j] == 0`
B. `dis[j] < minn && cheak[j] == 0`
C. `dis[j] >= minn && cheak[j] == 0`
D. `dis[j] < minn && cheak[j] != 0`

答案：B

解析：本题答案选 B。选项 B 的内容「`dis[j] < minn && cheak[j] == 0`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第13题

下面 Floyd 算法中，横线处应该填入的是（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 21
#define INF 9999999
int map[N][N];

int main() {
    int n, m, t1, t2, t3;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            if (i == j)
                map[i][j] = 0;
            else
                map[i][j] = INF;
        }
    }

    for (int i = 1; i <= m; i++) {
        cin >> t1 >> t2 >> t3;
        map[t1][t2] = t3;
    }

    for (int k = 1; k <= n; k++)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                if (________) // 在此处填入选项
                    map[i][j] = map[i][k] + map[k][j];

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout.width(4);
            cout << map[i][j];
        }
        cout << endl;
    }
}
```

A. `map[i][j] < map[i][k] + map[k][j]`
B. `map[i][j] > map[i][k] + map[k][j]`
C. `map[i][j] > map[i][k] - map[k][j]`
D. `map[i][j] < map[i][k] - map[k][j]`

答案：B

解析：本题答案选 B。选项 B 的内容「`map[i][j] > map[i][k] + map[k][j]`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第14题

下面程序的 `Merge_Sort` 函数时间复杂度为（ ）。

```cpp
void Merge(int a[], int left, int mid, int right) {
    int temp[right - left + 1];
    int i = left;
    int j = mid + 1;
    int k = 0;
    while (i <= mid && j <= right) {
        if (a[i] < a[j])
            temp[k++] = a[i++];
        else
            temp[k++] = a[j++];
    }
    while (i <= mid)
        temp[k++] = a[i++];
    while (j <= right)
        temp[k++] = a[j++];
    for (int m = left, n = 0; m <= right; m++, n++)
        a[m] = temp[n];
}

void Merge_Sort(int a[], int left, int right) {
    if (left == right)
        return;
    int mid = (left + right) / 2;
    Merge_Sort(a, left, mid);
    Merge_Sort(a, mid + 1, right);
    Merge(a, left, mid, right);
}
```

A. $O(n \log n)$
B. $O(n^2)$
C. $O(2^n)$
D. $O(\log n)$

答案：A

解析：本题答案选 A。选项 A 的内容「$O(n \log n)$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第15题

下面 `fibonacci` 函数的时间复杂度为（ ）。

```cpp
int fibonacci(int n) {
    if (n <= 1)
        return n;
    else
        return fibonacci(n - 1) + fibonacci(n - 2);
}
```

洛谷注：本题原题无正确选项，洛谷上做了调整。

A. $O(1)$
B. $O(\phi^n), \phi = \frac{\sqrt{5}+1}{2}$
C. $O(n)$
D. $O(n \log n)$

答案：B

解析：本题答案选 B。选项 B 的内容「$O(\phi^n), \phi = \frac{\sqrt{5}+1}{2}$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。