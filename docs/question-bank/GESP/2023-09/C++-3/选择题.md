# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1129
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976220143648.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

人们所使用的手机上安装的 App 通常指的是（ ）。

【知识点：计算机基础常识】

A. 一款操作系统
B. 一款应用软件
C. 一种通话设备
D. 以上都不对

答案：B

解析：本题选 B。选项 B“一款应用软件”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第2题

下列流程图的输出结果是？（ ）

![](https://cdn.luogu.com.cn/upload/image_hosting/i3r3ksr7.png)

【知识点：控制结构与程序阅读】

A. 60
B. 20
C. 5
D. 1

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 20 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第3题

已知大写字符 'A' 的 ASCII 编码的十六进制表示为 0x41，则字符 'L' 的 ASCII 编码的十六进制表示为（ ）。

【知识点：ASCII/字符编码】

A. 4A
B. 4B
C. 4C
D. 52

答案：C

解析：本题选 C。需要按存储单位换算或按字节数计算容量，4C 与题目给出的单位关系相符；其他选项忽略了进制换算或字节数量。

## 第4题

以下哪个不是 C++ 语言中的运算符？（ ）

【知识点：位运算】

A. ~
B. ~~
C. <
D. <<

答案：B

解析：本题选 B。C++ 只支持规定的运算符形式，~~ 与题干要求相符；其他选项要么是合法运算符，要么不是 C++ 运算符。

## 第5题

如果数组定义为 `long long array[] = {3, 5, 7, 2};`，则数组 array 占用的字节数为（ ）。

【知识点：数组与字符数组】

A. 32
B. 16
C. 8
D. 4

答案：A

解析：本题选 A。需要按存储单位换算或按字节数计算容量，32 与题目给出的单位关系相符；其他选项忽略了进制换算或字节数量。

## 第6题

一个数组定义为 `double array[3];`，则可合理访问这个数组的元素的下标最大为（ ）。

【知识点：数组与字符数组】

A. 2
B. 3
C. 23
D. 24

答案：A

解析：本题选 A。选项 A“2”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第7题

以下数组定义，符合 C++ 语言语法的是（ ）。

【知识点：数组与字符数组】

A. `double a[];`
B. `double b[] = {1, 2.0, '3'};`
C. `double c[3.0];`
D. `double[] d = new double[3];`

答案：B

解析：本题选 B。该选项违反 C++ 语法规则，例如赋值左侧必须是可修改的对象、运算符用法必须合法；其他选项可以按隐式转换或正常表达式规则通过。

## 第8题

下列关于进制的叙述，正确的是（ ）。

【知识点：进制转换与进制字面量】

A. 只有十进制和二进制能够用来表示小数，八进制和十六进制不可以。
B. 常用的进制包括二进制、八进制、十进制、十六进制，其他进制在日常生活中很少使用。
C. 对任意正整数，其二进制表示不会比它的十进制表示更短。
D. 正整数的八进制表示中，每一位可能出现的最大数字是 8。

答案：C

解析：本题选 C。选项 C“对任意正整数，其二进制表示不会比它的十进制表示更短。”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第9题

下列关于 C++ 语言中数组的叙述，不正确的是（ ）。

【知识点：数组与字符数组】

A. 可以定义 0 个元素的数组。
B. 不能定义 -1 个元素的数组。
C. 数组下标越界访问会产生编译错误。
D. 程序运行时发生数组下标的越界访问，程序依然可能正常结束。

答案：C

解析：本题选 C。选项 C“数组下标越界访问会产生编译错误。”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第10题

如果 a 是 int 类型的变量，下列哪个表达式的值一定为 true？（ ）

【知识点：位运算】

A. `a + 1000 - 1000 == a`
B. `a * 2 / 2 == a`
C. `(a & 1) == 1`
D. `(a | 1) == a + 1`

答案：A

解析：本题选 A。选项 A“`a + 1000 - 1000 == a`”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第11题

如果 a 和 b 均为 int 类型的变量，下列表达式不能正确判断“a 等于 b”的是（ ）。

【知识点：位运算】

A. `(a >= b) && (a <= b)`
B. `(a >> 1) == (b >> 1)`
C. `(a + b) == (a + a)`
D. `(a ^ b) == 0`

答案：B

解析：本题选 B。选项 B“`(a >> 1) == (b >> 1)`”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第12题

如果 a 为 char 类型的变量，下列哪个表达式可以正确判断“a是大写字母”？（ ）

【知识点：运算符、表达式与类型转换】

A. `a - 'A' <= 26`
B. `'A' <= a <= 'Z'`
C. `'A' <= 'a' <= 'Z'`
D. `('A' <= a) && (a <= 'Z')`

答案：D

解析：本题选 D。C++ 只支持规定的运算符形式，`('A' <= a) && (a <= 'Z')` 与题干要求相符；其他选项要么是合法运算符，要么不是 C++ 运算符。

## 第13题

在下列代码的横线处填写（ ），可以使得输出是“20 10”。
```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 10, b = 20;
    a = (a << 8) | b;
    ______  // 在此处填入代码
    cout << a << " " << b << endl;
    return 0;
}
```

【知识点：位运算】

A. `a = a >> 8; b = a & 0xff`
B. `b = a >> 8; a = a & 0xff;`
C. `a = b; b = a & 0xff;`
D. `b = a; a = b;`

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 `b = a >> 8; a = a & 0xff;` 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第14题

在下列代码的横线处填写（ ），可以使得输出是“120”。
```cpp
#include <iostream>
using namespace std;

int main() {
    int array[5] = {1, 2, 3, 4, 5};
    int res = 0;
    for (int i = 0; i < 5; i++)
        ______; // 在此处填入代码
    cout << res << endl;
    return 0;
}
```

【知识点：数组与字符数组】

A. `res += array[i];`
B. `res *= array[i]`
C. `res = array[i]`
D. 以上均不对。

答案：D

解析：本题选 D。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 以上均不对。 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第15题

下列代码的输出是（ ）。
```cpp
#include <iostream>
using namespace std;

int main() {
    int array[10];
    for (int i = 0; i < 10; i++)
        array[i] = i;
    for (int p = 2; p < 10; p++)
        if (array[p] == p)
            for (int n = p; n < 10; n += p)
                array[n] = array[n] / p * (p - 1);
    int res = 0;
    for (int n = 1; n < 10; n++)
        res += array[n];
    cout << res << endl;
    return 0;
}

【知识点：数组与字符数组】

A. 15
B. 28
C. 45
D. 55

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 28 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。
