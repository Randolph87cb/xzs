# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1131
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976341778464.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

近年来，线上授课变得普遍，很多有助于改善教学效果的设备也逐渐流行，其中包括比较常用的手写板，那么它属于哪类设备？（ ）。

A. 输入
B. 输出
C. 控制
D. 记录

答案：A

解析：手写板的作用是把用户手写的轨迹、笔画或点击位置送入计算机，由计算机进一步识别或处理，因此属于输入设备。显示器、音箱等才是输出设备；“控制”和“记录”不是这里按信息流向划分设备类别时的正确答案。

## 第2题

如果 a 和 b 均为 int 类型的变量，且 b 的值不为 0，那么下列能正确判断“a 是 b 的3倍”的表达式是（ ）。

A. `(a >> 3 == b)`
B. `(a - b) % 3 == 0`
C. `(a / b == 3)`
D. `(a == 3 * b)`

答案：D

解析：要判断“a 是 b 的 3 倍”，应直接判断 `a` 是否等于 `3 * b`，所以 D 正确。A 中 `a >> 3` 是右移 3 位，近似相当于对非负整数除以 8，不表示 3 倍关系；B 只能说明 `a - b` 可被 3 整除，例如 `a=4,b=1` 成立但 4 不是 1 的 3 倍；C 使用整型除法，`a=7,b=2` 时 `a / b == 3` 也成立，但 7 不是 2 的 3 倍。

## 第3题

如果变量 a 和 b 分别为 double 类型和 int 类型，则表达式 `(a = 6, b = 3 * (7 + 8) / 2, b += a)` 的计算结果为（ ）。

A. 6
B. 21
C. 28
D. 不确定

答案：C

解析：逗号表达式从左到右依次计算，整个表达式的值是最后一个子表达式的值。先执行 `a = 6`，再算 `b = 3 * (7 + 8) / 2`；由于 `b` 是 `int`，`3 * 15 / 2 = 45 / 2 = 22`。最后执行 `b += a`，`a` 的值 6 转为 `int` 后加到 `b` 上，得到 `28`，因此结果为 C。

## 第4题

有关下面 C++ 代码说法错误的是（ ）。

```cpp
// sumA()和sumB()用于求从1到N之和
#include <iostream>
using namespace std;
int sumA(int n) {
    int sum = 0;
    for (int i = 1; i < n + 1; i++)
        sum += i;
    return sum;
}
int sumB(int n) {
    if (n == 1)
        return 1;
    else
        return n + sumB(n - 1);
}
int main() {
    int n = 0;
    cin >> n;
    cout << sumA(n) << " " << sumB(n) << endl;
    return 0;
}
```

A. sumA() 用循环方式求从 1 到 N 之和， sumB() 用递归方式求从 1 到 N 之和。
B. 默认情况下，如果输入正整数 1000，能实现求从 1 到 1000 之和。
C. 默认情况下，如果输入正整数 100000，能实现求从 1 到 100000 之和。
D. 一般说来， sumA() 的效率高于 sumB()。

答案：C

解析：A 正确，`sumA` 用 `for` 循环累加，`sumB` 用 `sumB(n - 1)` 递归累加。B 在通常环境下没有问题，`1 + ... + 1000 = 500500`，不会超过 `int` 范围，递归深度也较小。C 错误，`1 + ... + 100000 = 5000050000`，超过 32 位 `int` 最大值 `2147483647`，并且很深的递归调用还可能导致栈空间问题。D 也合理，循环版没有大量函数调用开销，一般比递归版更高效。

## 第5题

下面 C++ 代码以递归方式实现字符串反序，横线处应填上代码是（ ）。

```cpp
//字符串反序
#include <iostream>
#include <string>
using namespace std;
string sReverse(string sIn) {
    if (sIn.length() <= 1) {
        return sIn;
    } else {
        return ________ // 此处填写代码
    }
}
int main() {
    string sIn;
    cin >> sIn;
    cout << sReverse(sIn) << endl;
    return 0;
}
```

A. `sIn[sIn.length() - 1] + sReverse(sIn.substr(0, sIn.length() - 1));`
B. `sIn[0] + sReverse(sIn.substr(1, sIn.length() - 1));`
C. `sReverse(sIn.substr(0, sIn.length() - 1)) + sIn[sIn.length() - 1];`
D. `sReverse(sIn.substr(1, sIn.length() - 1)) + sIn[sIn.length() - 1];`

答案：A

解析：递归反序可以分解为“最后一个字符 + 前面部分的反序”。当字符串长度大于 1 时，A 中 `sIn[sIn.length() - 1]` 取最后一个字符，`sIn.substr(0, sIn.length() - 1)` 取去掉最后一个字符后的前缀，递归反序后拼接即可，所以 A 正确。B 中 `sIn[0] + sReverse(sIn.substr(1, sIn.length() - 1));` 把首字符放在最前面，不会反转首字符位置；C 中 `sReverse(sIn.substr(0, sIn.length() - 1)) + sIn[sIn.length() - 1];` 是“前缀反序 + 最后字符”，最后字符仍留在末尾；D 中 `sReverse(sIn.substr(1, sIn.length() - 1)) + sIn[sIn.length() - 1];` 是“去掉首字符后的后缀反序 + 最后字符”，会重复最后字符并丢失首字符。

## 第6题

印度古老的汉诺塔传说：创世时有三根金刚柱，其中一柱从下往上按照大小顺序摞着 64 片黄金圆盘，当圆盘逐一片从一柱借助另外一柱全部移动到另外一柱时，宇宙毁灭。移动规则：在小圆盘上不能放大圆盘，在三根柱子之间一次只能移动一个圆盘。下面的 C++ 代码以递归方式实现汉诺塔，横线处应填入代码是（ ）。

```cpp
#include <iostream>
using namespace std;
// 递归实现汉诺塔，将N个圆盘从A通过B移动C
// 圆盘从底到顶，半径必须从大到小
void Hanoi(string A, string B, string C, int N) {
    if (N == 1) {
        cout << A << " -> " << C << endl;
    } else {
        Hanoi(A, C, B, N - 1);
        cout << A << " -> " << C << endl;
        ________; // 此处填写代码
    }
}
int main() {
    Hanoi("甲", "乙", "丙", 3);
    return 0;
}
```

A. `Hanoi(B, C, A, N - 2)`
B. `Hanoi(B, A, C, N - 1)`
C. `Hanoi(A, B, C, N - 2)`
D. `Hanoi(C, B, A, N - 1)`

答案：B

解析：汉诺塔递归分三步：先把上面的 `N - 1` 个盘从 A 借助 C 移到 B，即代码中的 `Hanoi(A, C, B, N - 1)`；再把最大的盘从 A 移到 C；最后要把 B 上的 `N - 1` 个盘借助 A 移到 C，所以应调用 `Hanoi(B, A, C, N - 1)`。A 和 C 的盘数写成 `N - 2`，少移动了一个盘；D 的起点写成 C，方向相反。

## 第7题

根据下面 C++ 代码的注释，两个横线处应分别填入（ ）。

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

bool isOdd(int N) {
    return N % 2 == 1;
}

bool compare(int a, int b) {
    if (a % 2 == 0 && b % 2 == 1)
        return true;
    return false;
}

int main() {
    vector<int> lstA; // lstA是一个整型向量
    for (int i = 1; i < 100; i++)
        lstA.push_back(i);
    // 对lstA成员按比较函数执行结果排序
    sort(lstA.begin(), lstA.end(), ________); // 此处填写代码1

    vector<int> lstB;
    for (int i = 0; i < lstA.size(); i++) // lstB成员全为奇数
        if (________) // 此处填写代码2
            lstB.push_back(lstA[i]);

    cout << "lstA: ";
    for (int i = 0; i < lstA.size(); i++)
        cout << lstA[i] << " ";
    cout << endl;

    cout << "lstB: ";
    for (int i = 0; i < lstB.size(); i++)
        cout << lstB[i] << " ";
    cout << endl;
    return 0;
}
```

A. `compare` 和 `isOdd(lstA[i])`
B. `compare(x1,y1)` 和 `isOdd`
C. `compare` 和 `isOdd`
D. `compare(x1,y1)` 和 `isOdd(lstA[i])`

答案：A

解析：`sort` 的第三个参数需要传入比较函数本身，因此应写函数名 `compare`，而不是写成 `compare(x1,y1)` 这种调用形式；后者还涉及未定义的变量，并且传入的是调用结果而不是比较规则。第二个空位位于 `if` 条件中，需要判断当前元素 `lstA[i]` 是否为奇数，所以要调用 `isOdd(lstA[i])`。只写 `isOdd` 是函数名，不是在判断当前元素。

## 第8题

有关下面代码正确的是（ ）。

```cpp
// 在C++语言中，可以通过函数指针的形式，将一个函数作为另一个函数的参数。
// 具体来说：bool checkNum(bool (*Fx)(int), int N); 声明了一个函数，
// 其第一个参数是函数指针类型，指向一个接收一个int参数且返回值为bool的函数。
#include <iostream>
using namespace std;

bool isEven(int N) {
    return N % 2 == 0;
}

bool checkNum(bool (*Fx)(int), int N) {
    return Fx(N);
}

int main() {
    cout << checkNum(isEven, 10) << endl;
    return 0;
}
```

A. `checkNum()` 函数定义错误。
B. 将 `isEven` 作为 `checkNum()` 参数将导致错误。
C. 执行后将输出 1。
D. 运行时触发异常。

答案：C

解析：`checkNum` 的第一个形参类型是 `bool (*)(int)`，表示“指向一个参数为 `int`、返回值为 `bool` 的函数”的指针。`isEven` 正好符合这个类型，因此 `checkNum(isEven, 10)` 合法；函数内部执行 `Fx(N)`，等价于调用 `isEven(10)`。10 是偶数，`isEven(10)` 返回 `true`，用 `cout` 默认输出为 `1`。

## 第9题

有关下面 C++ 代码正确的是（ ）。
```cpp
#include <iostream>
using namespace std;

bool isOdd(int N) {
    return N % 2 == 1;
}

int Square(int N) {
    return N * N;
}

bool checkNum(bool (*Fx)(int), int x) {
    return Fx(x);
}

int main() {
    cout << checkNum(isOdd, 10) << endl;  // 输出行A
    cout << checkNum(Square, 10) << endl; // 输出行B
    return 0;
}
```

A. `checkNum()` 函数定义错误。
B. 输出行 A 的语句将导致编译错误。
C. 输出行 B 的语句将导致编译错误。
D. 该代码没有编译错误。

答案：C

解析：`checkNum` 要求第一个实参是返回 `bool`、参数为 `int` 的函数。`isOdd(int)` 返回 `bool`，所以输出行 A 的调用类型匹配，不会编译错误。`Square(int)` 虽然参数也是 `int`，但返回类型是 `int`，不能作为 `bool (*)(int)` 类型的函数指针传入，因此输出行 B 会导致编译错误，选 C。

## 第10题

下面代码执行后的输出是（ ）。

```cpp
#include <iostream>
using namespace std;

int jumpFloor(int N) {
    cout << N << "#";
    if (N == 1 || N == 2) {
        return N;
    } else {
        return jumpFloor(N - 1) + jumpFloor(N - 2);
    }
}

int main() {
    cout << jumpFloor(4) << endl;
    return 0;
}
```

A. `4#3#2#2#4`
B. `4#3#2#2#1#5`
C. `4#3#2#1#2#4`
D. `4#3#2#1#2#5`

答案：D

解析：外层语句先执行 `jumpFloor(4)`，每次进入函数都会先输出当前 `N` 和 `#`。调用过程为：`jumpFloor(4)` 输出 `4#`，再算 `jumpFloor(3) + jumpFloor(2)`；`jumpFloor(3)` 输出 `3#`，继续算 `jumpFloor(2) + jumpFloor(1)`，依次输出 `2#`、`1#` 并返回 `2 + 1 = 3`；随后右侧的 `jumpFloor(2)` 输出 `2#` 并返回 2。最终 `jumpFloor(4)` 返回 `3 + 2 = 5`，外层 `cout` 再输出 `5`，所以完整结果是 `4#3#2#1#2#5`。

## 第11题

下面代码中的 `isPrimeA()` 和 `isPrimeB()` 都用于判断参数 N 是否素数，有关其时间复杂度的正确说法是（ ）。
```cpp
#include <iostream>
#include <cmath>
using namespace std;

bool isPrimeA(int N) {
    if (N < 2)
        return false;
    for (int i = 2; i < N; i++)
        if (N % i == 0)
            return false;
    return true;
}

bool isPrimeB(int N) {
    if (N < 2)
        return false;
    int endNum = int(sqrt(N));
    for (int i = 2; i <= endNum; i++)
        if (N % i == 0)
            return false;
    return true;
}

int main() {
    cout << boolalpha;
    cout << isPrimeA(13) << " " << isPrimeB(13) << endl;
    return 0;
}
```

A. `isPrimeA()` 的最坏时间复杂度是 $O(N)$，`isPrimeB()` 的最坏时间复杂度是 $O(\log N)$，`isPrimeB()` 优于 `isPrimeA()`。
B. `isPrimeA()` 的最坏时间复杂度是 $O(N)$，`isPrimeB()` 的最坏时间复杂度是 $O(N^{\frac{1}{2}})$，`isPrimeB()` 优于 `isPrimeA()`。
C. `isPrimeA()` 的最坏时间复杂度是 $O(N^{\frac{1}{2}})$，`isPrimeB()` 的最坏时间复杂度是 $O(N)$，`isPrimeA()` 优于 `isPrimeB()`。
D. `isPrimeA()` 的最坏时间复杂度是 $O(\log N)$，`isPrimeB()` 的最坏时间复杂度是 $O(N)$，`isPrimeA()` 优于 `isPrimeB()`。

答案：B

解析：`isPrimeA` 在最坏情况下需要从 2 一直试除到 `N - 1`，循环次数与 `N` 同阶，最坏时间复杂度为 $O(N)$。`isPrimeB` 只试除到 `sqrt(N)`，因为如果 `N` 有一个大于平方根的因子，就必然还有一个小于平方根的配对因子；所以它的最坏时间复杂度为 $O(N^{\frac{1}{2}})$。$O(N^{\frac{1}{2}})$ 优于 $O(N)$，因此选 B。

## 第12题

下面代码用于归并排序，其中 merge() 函数被调用次数为（ ）。

```cpp
#include <iostream>
using namespace std;

void mergeSort(int * listData, int start, int end);
void merge(int * listData, int start, int middle, int end);

void mergeSort(int * listData, int start, int end) {
    if (start >= end)
        return;
    int middle = (start + end) / 2;
    mergeSort(listData, start, middle);
    mergeSort(listData, middle + 1, end);
    merge(listData, start, middle, end);
}

void merge(int * listData, int start, int middle, int end) {
    int leftSize = middle - start + 1;
    int rightSize = end - middle;

    int * left = new int[leftSize];
    int * right = new int[rightSize];
    for (int i = 0; i < leftSize; i++)
        left[i] = listData[start + i];
    for (int j = 0; j < rightSize; j++)
        right[j] = listData[middle + 1 + j];

    int i = 0, j = 0, k = start;
    while (i < leftSize && j < rightSize) {
        if (left[i] <= right[j]) {
            listData[k] = left[i];
            i++;
        } else {
            listData[k] = right[j];
            j++;
        }
        k++;
    }

    while (i < leftSize) {
        listData[k] = left[i];
        i++;
        k++;
    }
    while (j < rightSize) {
        listData[k] = right[j];
        j++;
        k++;
    }
    delete[] left;
    delete[] right;
}

int main() {
    int lstA[] = {1, 3, 2, 7, 11, 5, 3};
    int size = sizeof(lstA) / sizeof(lstA[0]);
    mergeSort(lstA, 0, size - 1); // 对lstA执行归并排序

    for (int i = 0; i < size; i++)
        cout << lstA[i] << " ";
    cout << endl;
    return 0;
}
```

A. 0
B. 1
C. 6
D. 7

答案：C

解析：`merge` 只会在 `mergeSort` 处理的区间长度大于 1 时调用一次。数组长度为 7，递归拆分到 7 个单元素区间后，需要把这些区间合并成一个整体；从 7 个叶子区间合并成 1 个区间，一共需要 `7 - 1 = 6` 次合并。也可以按递归树看：每个非叶子递归节点调用一次 `merge`，7 个叶子对应 6 个非叶子节点，所以选 C。

## 第13题

在上题的归并排序算法中，`mergeSort(listData, start, middle);` 和 `mergeSort(listData, middle + 1, end);` 涉及到的算法为（ ）。

A. 搜索算法
B. 分治算法
C. 贪心算法
D. 递推算法

答案：B

解析：这两句递归调用把原区间 `[start, end]` 分成左半区间 `[start, middle]` 和右半区间 `[middle + 1, end]` 分别排序，最后再用 `merge` 合并结果。这正是“分解子问题、分别求解、合并结果”的分治思想，不是搜索、贪心或递推。

## 第14题

归并排序算法的基本思想是（ ）。

A. 将数组分成两个子数组，分别排序后再合并。
B. 随机选择一个元素作为枢轴，将数组划分为两个部分。
C. 从数组的最后一个元素开始，依次与前一个元素比较并交换位置。
D. 比较相邻的两个元素，如果顺序错误就交换位置。

答案：A

解析：归并排序的核心是先把数组递归地分成两个子数组，分别排好序，再把两个有序子数组合并成一个有序数组，因此 A 正确。B 描述的是快速排序的划分思想；C 更接近从后向前的局部比较过程，不是归并排序；D 描述的是冒泡排序的相邻比较交换。

## 第15题

有关下面代码的说法正确的是（ ）。

```cpp
#include <iostream>

class Node {
public:
    int Value;
    Node * Next;

    Node(int Val, Node * Nxt = nullptr) {
        Value = Val;
        Next = Nxt;
    }
};

int main() {
    Node * firstNode = new Node(10);
    firstNode->Next = new Node(100);
    firstNode->Next->Next = new Node(111, firstNode);
    return 0;
}
```

A. 上述代码构成单向链表。
B. 上述代码构成双向链表。
C. 上述代码构成循环链表。
D. 上述代码构成指针链表。

答案：C

解析：代码先创建值为 10 的第一个节点，再让它的 `Next` 指向值为 100 的第二个节点，第二个节点的 `Next` 又指向值为 111 的第三个节点。第三个节点构造时传入 `firstNode`，因此它的 `Next` 指回第一个节点，形成 `10 -> 100 -> 111 -> 10` 的环，所以这是循环链表。它不是双向链表，因为每个节点只有一个 `Next` 指针，没有指向前驱的指针。
