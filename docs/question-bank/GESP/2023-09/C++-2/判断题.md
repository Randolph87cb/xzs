# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1128
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976138354720.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

我们常说的互联网（Internet）是一个覆盖全球的广域网络，它不属于任何一个国家。

答案：正确

解析：本题判断为正确。IPv4 点分十进制每段范围包含 0 到 255；域名从右向左看层级，最右侧才是顶级域名。

## 第2题

神威·太湖之光超级计算机是中国自主研制的超级计算机，在全球超级计算机TOP500排行榜中多次荣膺榜首。

答案：正确

解析：本题判断为正确。根据题目涉及的 C++ 语法、计算机基础或算法定义逐项核对，题干表述与标准规则一致。

## 第3题

C++ 表达式 `7.8 / 2` 的值为 $3.9$，类型为 float。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第4题

C++ 表达式 `(2 * 3) || (2 + 5)` 的值为 $67$。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第5题

如果 m 和 n 为 int 类型变量，则执行 `for (m = 0, n = 1; n < 9; ) n = ((m = 3 * n, m + 1), m - 1);` 之后 n 的值为偶数。

答案：正确

解析：本题判断为正确。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第6题

如果 a 为 int 类型的变量，则表达式 `(a >= 5 && a <= 10)` 与 `(5 <= a <= 10)` 的值总是相同的。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第7题

下面 C++ 代码执行后的输出为 10。

```cpp
int cnt = 0;
for (int i = 1; i < 10; i++) {
    cnt += 1;
    i += 1;
}
cout << cnt;
```

答案：错误

解析：本题判断为错误。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第8题

执行以下 C++ 代码后的输出为 0。

```cpp
int rst = 0;
for (int i = -100; i < 100; i += 2)
    rst += i;
cout << rst;

```

答案：错误

解析：本题判断为错误。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第9题

执行以下 C++ 代码后的输出为 30。

```cpp
int rst = 0;
for (int i = 0; i < 10; i += 2)
    rst += i;
cout << rst;
```

答案：错误

解析：本题判断为错误。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第10题

C++ 是一种高级程序设计语言。

答案：正确

解析：本题判断为正确。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。
