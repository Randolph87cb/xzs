# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1127
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976041885728.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

早期计算机内存不够大，可以将字库固化在一个包含只读存储器的扩展卡中插入计算机主板帮助处理汉字。

答案：正确

解析：本题判断为正确。按容量单位和字节数换算即可验证，题干给出的大小关系或比例与实际计算结果不一致或一致。

## 第2题

神威·太湖之光超级计算机是中国自主研制的超级计算机，在全球超级计算机 TOP500 排行榜中多次荣膺榜首。

答案：正确

解析：本题判断为正确。根据题目涉及的 C++ 语法、计算机基础或算法定义逐项核对，题干表述与标准规则一致。

## 第3题

C++ 表达式 `int(3.14)` 的值为 3。

答案：正确

解析：本题判断为正确。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第4题

C++ 语句 `cout << (2, 3, "23")` 的输出为 2, 3, 23。

答案：错误

解析：本题判断为错误。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。

## 第5题

C++ 的循环语句 `for (int i = 0; i < 10; i += 2)` 表示 $ i $ 从 0 开始到 10 结束但不包含 10，间隔为 2。

答案：正确

解析：本题判断为正确。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第6题

C++ 表达式 `('1' + '1')` 的值为 '2'。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第7题

在 C++ 语言中，do-while 循环不可能导致死循环，但 while 有可能。

答案：错误

解析：本题判断为错误。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第8题

在下面的 C++ 代码中，由于循环中的 continue 是无条件被执行，因此将导致死循环。

```cpp
for (int i = 1; i < 10; i++) continue;
```

答案：错误

解析：本题判断为错误。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第9题

在 C++ 代码中，不可以将变量命名为 cout，因为 cout 是 C++ 的关键字。

答案：错误

解析：本题判断为错误。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。

## 第10题

C++ 是一种高级程序设计语言。

答案：正确

解析：本题判断为正确。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。
