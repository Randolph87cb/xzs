# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1127
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976041885728.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

我们通常说的“内存”属于计算机中的（ ）。

A. 输出设备
B. 输入设备
C. 存储设备
D. 打印设备

答案：C

解析：本题选 C。需要按存储单位换算或按字节数计算容量，存储设备 与题目给出的单位关系相符；其他选项忽略了进制换算或字节数量。

## 第2题

以下 C++ 不可以作为变量的名称的是（ ）。

A. redStar
B. RedStar
C. red_star
D. red star

答案：D

解析：本题选 D。选项 D“red star”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第3题

C++ 表达式 `2 - 1 && 2 % 10` 的值是（ ）。

A. 0
B. 1
C. 2
D. 3

答案：B

解析：本题选 B。选项 B“1”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第4题

下面 C++ 代码段执行后的输出是（ ）。

```cpp
int a = 3, b = 4;
cout << "a+b=" << a+b;
```

A. 3+4= 7
B. 3+4=7
C. a+b=7
D. a+b=a+b

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 a+b=7 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第5题

在 C++ 语言中，int 类型的变量 x、y、z 的值分别为 2、4、6，以下表达式的值为真的是（ ）。

A. x > y || x > z
B. x != z - y
C. z > y + x
D. x < y || !x < z

答案：D

解析：本题选 D。选项 D“x < y || !x < z”与题干要求一致；其余选项在概念、公式、语法或计算过程上不满足题意。

## 第6题

对 int 类型的变量 a、b、c，下列语句不符合 C++ 语法的是（ ）。

A. `c += 5;`
B. `b = c % 2.5;`
C. `a = (b = 3, c = 4, b + c);`
D. `a -= a = (b = 6) / (c = 2);`

答案：B

解析：本题选 B。该选项违反 C++ 语法规则，例如赋值左侧必须是可修改的对象、运算符用法必须合法；其他选项可以按隐式转换或正常表达式规则通过。

## 第7题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int m = 14;
int n = 12;
if (m % 2 == 0 && n % 2 == 0)
    cout << "都是偶数";
else if (m % 2 == 1 && n % 2 == 1)
    cout << "都是奇数";
else
    cout << "不都是偶数或奇数";
```

A. 都是偶数
B. 都是奇数
C. 不都是偶数或奇数
D. 以上说法都不正确

答案：A

解析：本题选 A。该表达式准确刻画了题目要求的条件；其他选项存在链式比较含义错误、逻辑连接错误或只覆盖了部分情况的问题。

## 第8题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int m = 14;
int n = 12;
if (m % 2 && n % 2)
    cout << "都是偶数";
else if (m % 2 == 1 && n % 2 == 1)
    cout << "都是奇数";
else
    cout << "不都是偶数或奇数";
```

A. 都是偶数
B. 都是奇数
C. 不都是偶数或奇数
D. 以上说法都不正确

答案：C

解析：本题选 C。该表达式准确刻画了题目要求的条件；其他选项存在链式比较含义错误、逻辑连接错误或只覆盖了部分情况的问题。

## 第9题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int m = 7;
if (m / 5 || m / 3)
    cout << 0;
else if (m / 3)
    cout << 1;
else if (m / 5)
    cout << 2;
else
    cout << 3;
```

A. 0
B. 1
C. 2
D. 3

答案：A

解析：本题选 A。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 0 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第10题

下面 C++ 代码段执行后的输出是（ ）。

```cpp
int cnt = 0;
for (int i = 1; i <= 5; i++)
    cnt = cnt + 1;
cout << cnt;
```

A. 1
B. 4
C. 5
D. 10

答案：C

解析：本题选 C。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 5 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第11题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int tnt = 0;
for (int i = 1; i < 5; i += 2)
    tnt = tnt + i;
cout << tnt;
```

A. 2
B. 4
C. 9
D. 10

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 4 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第12题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int n = 5;
int cnt = 1;
while (n >= 0) {
    cnt += 1;
    n -= 2;
}
cout << cnt;
```

A. 3
B. 4
C. 6
D. 7

答案：B

解析：本题选 B。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 4 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第13题

下面 C++ 代码用于求正整数的所有因数，即输出所有能整除一个正整数的数。如，输入 10，则输出为 1、2、5、10；输入 12，则输出为 1、2、3、4、6、12；输入 17，则输出为 1、17。在横线处应填入代码是（ ）。

```
```cpp
int n = 0;
cout << "请输入一个正整数：";
cin >> n;

for (__________________) // 此处填写代码
    if (n % i == 0)
        cout << i << endl;
```

A. `int i = 1; i < n; i + 1`
B. `int i = 1; i < n + 1; i + 1`
C. `int i = 1; i < n; i++`
D. `int i = 1; i < n + 1; i++`

答案：D

解析：本题选 D。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 `int i = 1; i < n + 1; i++` 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第14题

在下列代码的横线处填写（ ），可以使得输出是正整数 1234 各位数字的平方和。

```cpp
int n = 1234, s = 0;
for (; n; n /= 10)
    s += ____________; // 此处填写代码
cout << s << endl;
```

A. `n / 10`
B. `(n / 10) * (n / 10)`
C. `n % 10`
D. `(n % 10) * (n % 10)`

答案：D

解析：本题选 D。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 `(n % 10) * (n % 10)` 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。

## 第15题

执行以下 C++ 语言程序后，输出结果是（ ）。

```cpp
int n = 5, s = 1;
for (; n = 0; n--)
    s *= n;
cout << s << endl;
```

A. 1
B. 0
C. 120
D. 无法确定

答案：A

解析：本题选 A。把选项代入程序后，按赋值、循环和输出顺序逐步计算，只有 1 会得到题目要求的结果；其他选项会改变变量更新过程或输出序列。
