# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1132
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1580976400498720.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

TCP/IP 的传输层的两个不同的协议分别是 UDP 和 TCP。

答案：正确

解析：本题判断为正确。根据题目涉及的 C++ 语法、计算机基础或算法定义逐项核对，题干表述与标准规则一致。

## 第2题

5G 网络中，5G 中的 G 表示 Gigabytes/s，其中 1 GB = 1024 MB。

答案：错误

解析：本题判断为错误。按容量单位和字节数换算即可验证，题干给出的大小关系或比例与实际计算结果不一致或一致。

## 第3题

在面向对象中，类是对象的实例。

答案：错误

解析：本题判断为错误。对象创建时调用匹配的构造函数，不会先自动执行一个缺省构造函数再执行用户定义构造函数。

## 第4题

在 C++ 类的定义中，使用 `static` 修饰符定义的静态成员被该类的所有对象共享。

答案：正确

解析：本题判断为正确。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。

## 第5题

在 C++ 类的定义中，可以定义初始化函数或运算符函数等。

答案：正确

解析：本题判断为正确。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。

## 第6题

DFS 是深度优先算法的英文简写。

答案：正确

解析：本题判断为正确。图的连通性和顶点度数可用遍历或存储结构分析；邻接矩阵与邻接表的访问代价并不总相同，BFS/DFS 也都可用于连通性判断。

## 第7题

哈夫曼编码是一种有损压缩算法。

答案：错误

解析：本题判断为错误。按容量单位和字节数换算即可验证，题干给出的大小关系或比例与实际计算结果不一致或一致。

## 第8题

有些算法或数据结构在 C/C++ 语言中使用指针实现，一个典型的例子就是链表。因此，链表这一数据结构在 C/C++ 语言中只能使用指针来实现。

答案：错误

解析：本题判断为错误。赋值号 `=` 与相等比较 `==` 含义不同，C++ 运算符也必须写成规定形式；题干说法需与这些语法规则一致。

## 第9题

如果节点数为 $N$，广度搜索算法的最差时间复杂度为 $O(N)$。

答案：正确

解析：本题判断为正确。图的连通性和顶点度数可用遍历或存储结构分析；邻接矩阵与邻接表的访问代价并不总相同，BFS/DFS 也都可用于连通性判断。

## 第10题

二叉搜索树的左右子树也是二叉搜索树。

答案：正确

解析：本题判断为正确。树的深度、叶结点和查找复杂度要按具体形态计算，不能只凭节点数或二叉排序树名称固定为某个值。
