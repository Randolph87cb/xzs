# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1176
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1669256863285280.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

关于下述代码，说法错误的是（ ）。

```cpp
int multiply(int x, int y);

int main() {
    int a = 4;
    int b = 5;
    int result = multiply(a, b);
    std::cout << "The result is: " << result << std::endl;
    return 0;
}

int multiply(int x, int y) {
    return x * y;
}
```

A. 函数 `multiply` 的定义应该放到函数 `main` 之前。
B. 函数声明 `int multiply(int x, int y);` 中明确指定了函数 `multiply()` 的返回值为整数类型。
C. 在 `main` 函数中，函数 `multiply` 通过 `multiply(a, b)` 被调用，其中 `a` 和 `b` 是定义在 `main` 函数中的变量，它们作为实参传递给了 `multiply` 函数的形参 `x` 和 `y`。
D. 运行上述代码，将输出 `The result is: 20`。

答案：A

解析：本题问不符合要求的一项，A 项「函数 multiply 的定义应该放到函数 main 之前。」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 A。



## 第2题

执行下述代码将输出（ ）。

```cpp
int x = 10;
void func() { int x = 20; std::cout << x; }
int main() {
    func();
    std::cout << x;
    return 0;
}
```

A. 2020
B. 2010
C. 1010
D. 编译错误

答案：B

解析：B 项「2010」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

执行下述代码后，变量 `a` 的值为（ ）。

```cpp
int a = 10;
int* p = &a;
*p = 20;
```

A. 10
B. 20
C. 随机值
D. 编译错误

答案：B

解析：B 项「20」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

以下哪种参数传递方式可以避免拷贝大型对象？

A. 只能用值传递
B. 只能用引用传递
C. 只能用指针传递
D. 引用传递和指针传递均可

答案：D

解析：D 项「引用传递和指针传递均可」符合题干要求。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

执行下述代码，将输出（ ）。

```cpp
void swap(int a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}
int main() {
    int x = 1, y = 2;
    swap(x, y);
    std::cout << x << y;
    return 0;
}
```

A. 12
B. 21
C. 22
D. 11

答案：D

解析：D 项「11」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

下面的描述中，（ ）正确定义一个名为 Person 的结构体并正确初始化了一个 Person 结构体的变量 p。

A.
```cpp
struct Person {
    string name;
    int age;
};
Person p("Yang", 10);
```

B.
```cpp
struct Person {
    string name,
    int age;
};
Person p;
p.name = "Yang";
p.age = 10;
```

C.
```cpp
struct Person {
    string name;
    int age;
};
Person p = { "Yang", 10 };
```

D.
```cpp
struct Person {
    string name;
    int age;
};
Person p = new Person("Yang", 10);
```

答案：C

解析：C 项「代码片段」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

给定如下代码，

```cpp
struct Person {
    std::string name;
    int age;
    struct Address {
        std::string street;
        std::string city;
    };
    Address address;
};
```

下面描述错误的是（ ）。

A. 结构 `Person` 内嵌套结构 `Address`
B. `Person` 有一个 `Address` 类型的 `address` 成员
C. 一个 `Person` 类型的变量 `p` 的 `address` 的初始化可以写成：`p.address.street = "123 Main St"; p.address.city = "Anytown";`
D. 结构的嵌套可以减少命名冲突，因此可以不必控制嵌套层次

答案：D

解析：本题问不符合要求的一项，D 项「结构的嵌套可以减少命名冲突，因此可以不必控制嵌套层次」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 D。



## 第8题

假设 `int arr[2][3] = {{1,2,3},{4,5,6}};`，则 `arr[1][2]` 的值是（ ）。

A. 2
B. 3
C. 5
D. 6

答案：D

解析：D 项「6」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

下面（ ）正确定义了二维数组。

A. `int arr[3,4];`
B. `int arr[3][4];`
C. `int arr(3,4);`
D. `int a[3-4];`

答案：B

解析：B 项「int arr[3][4];」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

小杨正在爬楼梯，需要爬 $ n $ 阶才能到达楼顶。如果每次可以爬 1 个或 2 个台阶，下面代码采用递推算法来计算一共有多少种不同的方法可以爬到楼顶，则横线上应填写（ ）。

```cpp
int f(int n) {
    if (n == 1 || n == 2)
        return n;

    int f1 = 1;
    int f2 = 2;
    int res = 0;
    for (int i = 3; i <= n; i++) {
        // 在此处填入代码
    }
    return res;
}
```

A.
```cpp
res += f1 + f2;
f1 = f2;
f2 = res;
```

B.
```cpp
res = f1 + f2;
f1 = f2;
f2 = res;
```

C.
```cpp
res += f1 + f2;
f2 = res;
f1 = f2;
```

D.
```cpp
res = f1 + f2;
f2 = res;
f1 = f2;
```

答案：B

解析：B 项「代码片段」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

给定如下算法，其时间复杂度为（ ）。

```cpp
bool f(int arr[], int n, int target) {
    for (int i = 0; i < (1 << n); i++) {
        int sum = 0;
        for (int j = 0; j < n; j++) {
            if (i & (1 << j)) {
                sum += arr[j];
            }
        }
        if (sum == target) return true;
    }
    return false;
}
```

A. $O(n^2)$
B. $O(n \times 2^n)$
C. $O(1)$
D. $O(n^3)$

答案：B

解析：B 项「O(n \times 2^n)」符合题干要求。复杂度题要统计循环执行次数或递推式的总规模，忽略常数和低阶项后得到渐近量级。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

下面关于排序稳定性的描述，正确的是（ ）。

A. 稳定性指算法的时间复杂度恒定
B. 稳定排序保证相同元素的相对顺序不变
C. 选择排序是稳定排序
D. 插入排序不是稳定排序

答案：B

解析：B 项「稳定排序保证相同元素的相对顺序不变」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

对数组 `arr[]={5, 3, 8, 1}` 进行升序排序，执行第一轮冒泡排序后数组 `arr` 中的内容为（ ）。

A. 3, 5, 1, 8
B. 3, 1, 5, 8
C. 3, 5, 8, 1
D. 5, 3, 8, 1

答案：A

解析：A 项「3, 5, 1, 8」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

运行下面的代码，将出现（ ）。

```cpp
double hmean(double a, double b) {
    if (a == -b)
        throw runtime_error("Runtime error occurred.");
    return 2.0*a*b/(a + b);
}

int main() {
    double x = 10;
    double y = -10;

    try {
        int result = hmean(x, y);
        cout << "hmean: " << result << endl;
    }

    catch (const runtime_error& e) {
        cout << "Caught: " << e.what() << endl;
    } catch (...) {
        cout << "Caught an unknown exception." << endl;
    }
    return 0;
}
```

A. 屏幕上输出 `Caught: Runtime error occurred.`
B. 屏幕上输出 `Caught an unknown exception.`
C. 程序调用 `std::terminate()`
D. 编译错误

答案：A

解析：A 项「屏幕上输出 Caught: Runtime error occurred.」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

下面哪种方式不能实现将字符串 "Happy Spring!" 输出重定向到文件 log.txt （ ）。

A.
```cpp
freopen("log.txt", "w", stdout);
cout << "Happy Spring!" << endl;
fclose(stdout);
```

B.
```cpp
std::ofstream outFile("log.txt");
outFile << "Happy Spring!" << endl;
outFile.close();
```

C.
```cpp
std::ofstream outFile("log.txt");
cout << "Happy Spring!" << endl;
outFile.close();
```

D.
```cpp
ofstream log_file("log.txt");
streambuf* org_cout = cout.rdbuf();
cout.rdbuf(log_file.rdbuf());
cout << "Happy Spring!" << endl;
cout.rdbuf(org_cout);
```

答案：C

解析：本题问不符合要求的一项，C 项「代码片段」正是与题干规则冲突的说法或做法。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 其余选项能够满足题意或表述成立，因此应选 C。





