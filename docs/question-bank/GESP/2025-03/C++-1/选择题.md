# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1173
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1669256632598560.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

2025 年春节有两件轰动全球的事件，一个是 DeepSeek 横空出世，另一个是贺岁片《哪吒2》票房惊人，入了全球票房榜。下面关于 DeepSeek 与《哪吒2》的描述成立的是（ ）。

A. 《哪吒2》是一款新型操作系统
B. DeepSeek 是深海钻探软件
C. 《哪吒2》可以生成新的软件
D. DeepSeek 可以根据《哪吒2》的场景生成剧情脚本

答案：D

解析：D 项「DeepSeek 可以根据《哪吒2》的场景生成剧情脚本」符合题干要求。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

在某集成开发环境中编辑一个源代码文件时不可以执行下面（ ）操作。

A. 修改变量定义
B. 保存代码修改
C. 撤销代码修改
D. 插入执行截图

答案：D

解析：D 项「插入执行截图」符合题干要求。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

在 C++ 中，下列可以做变量的是（ ）。

A. Var-1
B. $1
C. %%1
D. _Var_1

答案：D

解析：D 项「_Var_1」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

以下哪个是 C++ 语言的关键字？（ ）

A. abs
B. cin
C. do
D. endl

答案：C

解析：C 项「do」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

下面的框架在 L1 和 L2 标记的两行处分别填写选项中的代码，哪组不能通过编译（ ）。

```cpp
int main() {
    int i = 0;
    ____________ // L1
    break;
    ____________ // L2
    return 0;
}
```
A.
```cpp
do {
} while (i != 0);
```

B.
```cpp
for (; i < 10; i++) {
}
```

C.
```cpp
if (i == 0) {
}
```

D.
```cpp
switch (i) {
}
```

A. A
B. B
C. C
D. D

答案：C

解析：本题问不符合要求的一项，C 项「代码片段」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 C。



## 第6题

下面 C++ 语句（ ）执行后的输出是 __ 0322$$ 。

A. printf("__ %2d%02d$$", 3, 22)
B. printf("__ %02d%2d$$", 3, 22)
C. printf("__ %02d%02d$$$$", 3, 22)
D. printf("____ %02d%02d$$$$", 3, 22)

答案：B

解析：B 项「printf("__ %02d%2d", 3, 22)」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

有关下列 C++ 代码的说法，错误的是（ ）。

```cpp
printf("我爱码代码！");
```

A. 配对双引号内的汉字改为英文 Hello ,C++ 代码能正确执行
B. 配对双引号内的汉字改为 Hello 代码！ ,C++ 代码能正确执行
C. 代码中的每个双引号，都可以改为两个单引号
D. 代码中的每个双引号，都可以改为三个双引号

答案：C

解析：本题问不符合要求的一项，C 项「代码中的每个双引号，都可以改为两个单引号」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 C。



## 第8题

C++ 表达式 `16 / 4 % 2` 的值是（ ）。

A. 8
B. 4
C. 2
D. 0

答案：D

解析：D 项「0」符合题干要求。表达式和控制结构题要按运算符优先级、整数除法、取余规则以及循环控制语句逐步执行。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

$N$ 是 C++ 的正整数，值为 12，则 `cout << (N % 3 + N / 5)` 的输出是（ ）。

A. 6.4
B. 2.4
C. 6
D. 2

答案：D

解析：D 项「2」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int N = 10;
printf("{N}*{N}={%d*%d}", N, N, N * N);
```

A. 10*10={10*10}
B. 100=10
C. N*N=100
D. {N}*{N}={10*10}

答案：D

解析：D 项「{N}*{N}={10*10}」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

执行下面的 C++ 代码，在键盘上先后输入 100 和 200，输出是（ ）。

```cpp
int first, second;
cout << "请输入第1个正整数：";
cin >> first;
cout << "请输入第2个正整数：";
cin >> second;
cout << (first / second * second) << endl;
```

A. 200
B. 100
C. 1
D. 0

答案：D

解析：D 项「0」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

下面 C++ 代码执行后，将输出能被 2 整除且除以 7 余数为 2 的数。下列选项不能实现的是（ ）。

```cpp
for (int i = 0; i < 100; i++)
    if ______________________
        cout << i << " ";
```

A. `((i % 2 == 0) && (i % 7 == 2))`
B. `((!(i % 2)) && (i % 7 == 2))`
C. `((!(i % 2)) && (!(i % 7)))`
D. `((i % 2 != 1) && (i % 7 == 2))`

答案：C

解析：本题问不符合要求的一项，C 项「((!(i % 2)) && (!(i % 7)))」正是与题干规则冲突的说法或做法。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项能够满足题意或表述成立，因此应选 C。



## 第13题

下面 C++ 代码执行后输出是（ ）。

```cpp
int tnt = 0;
for (int i = -1000; i < 1000; i++)
    tnt += i;
cout << tnt << endl;
```

A. -1000
B. 0
C. 999
D. 1000

答案：A

解析：A 项「-1000」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

下面 C++ 代码执行后输出的是（ ）。

```cpp
int i;
for (i = 1; i < 100; i += 5)
    continue;
cout << i << endl;
```

A. 101
B. 100
C. 99
D. 96

答案：A

解析：A 项「101」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

下面 C++ 代码执行后输出的是（ ）。

```cpp
int tnt = 0;
for (int i = 5; i < 100; i += 5){
    if (i % 2 == 0)
        continue;
    tnt += 1;
    if (i % 3 == 0 && i % 7 == 0)
        break;
}
cout << tnt << endl;
```

A. 500
B. 450
C. 10
D. 1

答案：C

解析：C 项「10」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。





