# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1180
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1669257026863136.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

国家“以旧换新”政策仍在继续，小杨家决定在家里旧的冰箱、电视、洗衣机、微波炉中选两种换新。其中，冰箱有 4 种型号可选，电视有 6 种型号可选，洗衣机有 3 种型号可选，微波炉有 5 种型号可选。请问小杨家共有多少种换新的方案？（ ）。

A. 18
B. 119
C. 238
D. 360

答案：B

解析：B 项「119」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

小杨和 3 位朋友约好一起去看电影“哪吒 2”。打开购票软件，他们发现，已经没有同一排连续的四个座位了（图中每个方框代表一个座位，红色方框代表已经售出）。朋友们商量了一下，决定分为两组，每组两人在同一排的相邻两个座位，且两组之间至少有一对座位是前后相邻的。请问共有多少种购票方案？（ ）。

![](https://cdn.luogu.com.cn/upload/image_hosting/ael7nxn6.png)

A. 495
B. 96
C. 7
D. 4

答案：C

解析：C 项「7」符合题干要求。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

下面关于 C++ 类构造和析构函数的说法，错误的是（ ）。

A. 构造函数不能声明为虚函数。
B. 析构函数必须声明为虚函数。
C. 类的默认构造函数可以被声明为 `private`。
D. 类的析构函数可以被声明为 `private`。

答案：B

解析：本题问不符合要求的一项，B 项「析构函数必须声明为虚函数。」正是与题干规则冲突的说法或做法。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 其余选项能够满足题意或表述成立，因此应选 B。



## 第4题

下列关于树和图的说法，错误的是（ ）。

A. 树是一种有向无环图，有向无环图都是一棵树。
B. 如果把树看做有向图，每个节点指向其子节点点，则该图是弱连通图。
C. $N$ 个顶点且连通的无向图，其最小生成树一定包含 $N - 1$ 个条边。
D. $N + 1$ 个顶点、$N$ 条边的有向图，一定不是强连通的。

答案：A

解析：本题问不符合要求的一项，A 项「树是一种有向无环图，有向无环图都是一棵树。」正是与题干规则冲突的说法或做法。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 其余选项能够满足题意或表述成立，因此应选 A。



## 第5题

从 1 到 2025 这 2025 个数中，包含数字 5 的个数（ ）。

A. 600
B. 601
C. 602
D. 603

答案：D

解析：D 项「603」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

已定义 `double` 类型的变量 `r` 和 `theta`，分别表示图中圆半径和圆心角。下列表达式中可以求出弦长 `s` 的是（ ）。

![](https://cdn.luogu.com.cn/upload/image_hosting/am1tqjqu.png)

A. `r * cos(theta)`
B. `r * cos(theta / 2) * 2`
C. `r * sin(theta)`
D. `r * sin(theta / 2) * 2`

答案：D

解析：D 项「r * sin(theta / 2) * 2」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

$N$ 个节点的平衡二叉树的高为（ ）。

A. $\lfloor \log_2 N \rfloor$
B. $\lceil \log_2 N \rceil$
C. $\lfloor \log_2 N \rfloor + 1$
D. 无法确定。

答案：D

解析：D 项「无法确定。」符合题干要求。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

下列关于算法的说法，错误的是（ ）。

A. 如果有足够的时间和空间，枚举法能解决一切有限的问题。
B. 分治算法将原问题分为多个子问题进行求解，且分解出的子问题必须相互独立。
C. 如果能找到合理的贪心原则，贪心算法往往能够比其他方法更快求解。
D. 倍增法在搜索未知长度的有序数组时，通过动态倍增或减半步长，快速定位目标范围。

答案：B

解析：本题问不符合要求的一项，B 项「分治算法将原问题分为多个子问题进行求解，且分解出的子问题必须相互独立。」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 B。



## 第9题

2025 是个神奇的数字，因为它是由两个数 20 和 25 拼接而成，而且 $2025 = (20 + 25)^2$。小杨决定写个程序找找小于 $N$ 的正整数中共有多少这样神奇的数字。下面程序横线处应填入的是（ ）。

```cpp
#include <string>
int count_miracle(int N) {
    int cnt = 0;
    for (int n = 1; n * n < N; n++) {
        int n2 = n * n;
        std::string s = std::to_string(n2);
        for (int i = 1; i < s.length(); i++)
            if (s[i] != '0') {
                std::string s1 = s.substr(0, i);
                std::string sr = s.substr(i);
                int n1 = std::stoi(s1);
                int nr = std::stoi(sr);
                if (__________) // 在此处填入选项
                    cnt++;
            }
    }
    return cnt;
}
```

A. `n1 + nr == n`
B. `n1 + nr == n2`
C. `(n1 + nr) * (n1 + nr) == n`
D. `(n1 + nr) ^ 2 == n2`

答案：A

解析：A 项「n1 + nr == n」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

2025 是个神奇的数字，因为它是由两个数 20 和 25 拼接而成，而且 $2025 = (20 + 25)^2$。小杨决定写个程序找找小于 $N$ 的正整数中共有多少这样神奇的数字。该函数的时间复杂度为（ ）。

```cpp
#include <string>
int count_miracle(int N) {
    int cnt = 0;
    for (int n = 1; n * n < N; n++) {
        int n2 = n * n;
        std::string s = std::to_string(n2);
        for (int i = 1; i < s.length(); i++)
            if (s[i] != '0') {
                std::string s1 = s.substr(0, i);
                std::string sr = s.substr(i);
                int n1 = std::stoi(s1);
                int nr = std::stoi(sr);
                if (__________) // 在此处填入选项
                    cnt++;
            }
    }
    return cnt;
}
```

A. $O(N \log N)$
B. $O(N^{1/2})$
C. $O(N^{1/2} \log N)$
D. $O(N^{1/2} (\log N)^2)$

答案：D

解析：D 项「O(N^{1/2} (\log N)^2)」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

下面的欧氏筛法程序中，两个横线处应填入的分别是（ ）。

```cpp
int primes[MAXP], num = 0;
bool isPrime[MAXN + 1] = {false};
void sieve() {
    for (int n = 2; n <= MAXN; n++) {
        if (!isPrime[n]) {
            primes[num++] = n;
            for (int i = 0; i < num && ________; i++) { // 在此处填入选项
                isPrime[n * primes[i]] = true;
                if (________) // 在此处填入选项
                    break;
            }
        }
    }
}
```

A.
```cpp
n * primes[i] < MAXN
n % primes[i] == 0
```

B.
```cpp
n * primes[i] < MAXN
primes[i] > n
```

C.
```cpp
n * primes[i] <= MAXN
n % primes[i] == 0
```

D.
```cpp
n * primes[i] <= MAXN
primes[i] > n
```

A. A
B. B
C. C
D. D

答案：C

解析：C 项「代码片段」符合题干要求。数论题要抓住定义：欧几里得算法最终非零值为最大公约数，筛法通过标记合数得到质数，唯一分解只对大于 1 的整数按素数幂唯一分解。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

下面 Floyd 算法中，横线处应该填入的是（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 21
#define INF 9999999
int map[N][N];
int main() {
    int n, m, t1, t2, t3;
    cin >> n >> m;

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            if (i == j)
                map[i][j] = 0;
            else
                map[i][j] = INF;
        }
    }

    for (int i = 1; i <= m; i++) {
        cin >> t1 >> t2 >> t3;
        map[t1][t2] = t3;
    }

    for (int k = 1; k <= n; k++)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                if (map[i][j] > map[i][k] + map[k][j])
                    ________; // 在此处填入选项

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout.width(4);
            cout << map[i][j];
        }
        cout << endl;
    }
}
```

A. `map[i][j] = map[i][k] + map[k][j]`
B. `map[i][k] = map[i][j] - map[k][j]`
C. `map[i][j] = map[i][k] - map[k][j]`
D. `map[k][j] = map[i][j] - map[i][k]`

答案：A

解析：A 项「map[i][j] = map[i][k] + map[k][j]」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

下面 Floyd 算法程序的时间复杂度为（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 21
#define INF 9999999
int map[N][N];
int main() {
    int n, m, t1, t2, t3;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            if (i == j)
                map[i][j] = 0;
            else
                map[i][j] = INF;
        }
    }

    for (int i = 1; i <= m; i++) {
        cin >> t1 >> t2 >> t3;
        map[t1][t2] = t3;
    }

    for (int k = 1; k <= n; k++)
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                if (map[i][j] > map[i][k] + map[k][j])
                    ________; // 在此处填入选项

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout.width(4);
            cout << map[i][j];
        }
        cout << endl;
    }
}
```

A. $O(N)$
B. $O(N^2)$
C. $O(N^3)$
D. $O(N^2 \log N)$

答案：C

解析：C 项「O(N^3)」符合题干要求。复杂度题要统计循环执行次数或递推式的总规模，忽略常数和低阶项后得到渐近量级。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

下列程序实现了输出杨辉三角形，代码中横线部分应该填入的是（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 35
int a[N];
int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        a[i] = 1;
        for (int j = i - 1; j > 0; j--)
            ________; // 在此处填入选项
        for (int j = 0; j <= i; j++)
            cout << a[j] << " ";
        cout << endl;
    }
    return 0;
}
```

A. `a[j] += a[j + 1]`
B. `a[j] += a[j - 1]`
C. `a[j - 1] += a[j]`
D. `a[j + 1] += a[j]`

答案：B

解析：B 项「a[j] += a[j - 1]」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

下列程序实现了输出杨辉三角形，其时间复杂度为（ ）。

```cpp
#include <iostream>
using namespace std;

#define N 35
int a[N];
int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        a[i] = 1;
        for (int j = i - 1; j > 0; j--)
            ________; // 在此处填入选项
        for (int j = 0; j <= i; j++)
            cout << a[j] << " ";
        cout << endl;
    }
    return 0;
}
```

A. $O(n)$
B. $O(n \log n)$
C. $O(n^2)$
D. $O(n^3)$

答案：C

解析：C 项「O(n^2)」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。





