# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1178
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1669256961851424.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

哈夫曼树在构造过程中，每次合并权值最小的两个节点，最终生成的树带权路径长度最小。

答案：正确

解析：题干表述成立。哈夫曼编码/哈夫曼树的核心是每次合并权值最小的两个结点，从而得到最小带权路径长度。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第2题

格雷编码的相邻两个编码之间必须有多位不同，以避免数据传输错误。

答案：错误

解析：题干表述不成立。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第3题

在树的深度优先搜索（DFS）中，使用队列作为辅助数据结构以实现“先进后出”的访问顺序。

答案：错误

解析：题干表述不成立。栈遵循后进先出，队列遵循先进先出；循环队列还要用取模维护队首和队尾位置。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第4题

以下代码实现的是二叉树的中序遍历：

```cpp
void traverse(TreeNode* root) {
    if (root == nullptr) return;
    traverse(root->left);
    cout << root->val << " ";
    traverse(root->right);
}
```

答案：正确

解析：题干表述成立。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第5题

C++ 支持构造函数重载，但默认无参数的构造函数只能有一个。

答案：正确

解析：题干表述成立。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第6题

二叉排序树（BST）中，若某节点的左子树为空，则该节点一定是树中的最小值节点。

答案：错误

解析：题干表述不成立。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第7题

在动态规划解决一维硬币找零问题时，若硬币面额为 $[1, 3, 4]$，目标金额为 $6$，则最少需要 2 枚硬币（$3+3$）。

答案：正确

解析：题干表述成立。动态规划需要明确状态含义，并按状态转移只保留满足约束的最优值。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第8题

面向对象编程中，封装是指将数据和行为绑定在一起，并对外隐藏实现细节。

答案：正确

解析：题干表述成立。面向对象题要区分类、对象、访问权限、构造析构顺序和虚函数动态绑定等基本规则。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第9题

以下代码创建的树是一棵完全二叉树：

```cpp
TreeNode* root = new TreeNode{1};
root->left = new TreeNode{2};
root->right = new TreeNode{3};
root->left->left = new TreeNode{4};
```

答案：正确

解析：题干表述成立。树结构题要依据左右子树、遍历顺序或完全二叉树层次性质判断，BST 还要满足左小右大的约束。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第10题

栈和队列均可以用双向链表实现，插入和删除操作的时间复杂度为 $O(1)$。

答案：正确

解析：题干表述成立。链表操作的关键是先保存待删或待连的结点，再修改前后指针，避免断链或访问已经释放的结点。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。





