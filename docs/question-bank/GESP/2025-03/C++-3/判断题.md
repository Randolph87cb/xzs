# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1175
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1670868226801696.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

判断一个三角形是否成立的条件只有：任意两边长度之和大于第三边的长度

【知识点：枚举与基础算法】

答案：错误

解析：题干表述不成立。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第2题

这段程序进行的是判断一个从键盘输入的字符的 ASCII 是否是奇数，若是，输出 YES，否则，输出 NO

```cpp
int main()
{
    char x;
    scanf("%c", &x);
    int ASCII = (int)x;
    cout << (x & 1 ? "YES" : "NO") << '\n';
    return 0;
}
```

【知识点：位运算】

答案：正确

解析：题干表述成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第3题

闰年的定义：
- 普通闰年：公历年份是 4 的倍数，且不是 100 的倍数的，为闰年（如 2004 年、2020 年等就是闰年）。
- 世纪闰年：公历年份是整百数的，必须是 400 的倍数才是闰年（如 1900 年不是闰年，2000 年是闰年）。

下面程序是判断是否是闰年的正确程序

```cpp
cin >> n;
cout << ((n % 4 == 0 && n % 100 != 0) || (n % 400 == 0)) ? 1 : 0;
return 0;
```

【知识点：运算符、表达式与类型转换】

答案：正确

解析：题干表述成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第4题

C++语句 `cout << (n % 15 == 0 ? "YES" : "NO");` 能够判断一个整数能否被 3 和 5 同时整除

【知识点：运算符、表达式与类型转换】

答案：正确

解析：题干表述成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第5题

有 $ n $ 个同学，从中抽取任意个人数来参加学校组织的大合唱，共有 $ 2^n $ 种方法

【知识点：位运算】

答案：正确

解析：题干表述成立。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第6题

若将一个正整数化为二进制数，在此二进制数中，我们将数字 1 的个数是偶数的这类二进制数称为 A 类数，否则就称其为 B 类数。

例如：
- $ (13)_{10} = (1101)_2 $，其中 1 的个数为 3，则称此数为 B 类数；
- $ (10)_{10} = (1010)_2 $，其中 1 的个数为 2，称此数为 A 类数；

判断 $ (2025)_{10} $ 化为二进制后，1 的个数为偶数个，因此 2025 为 A 类数。

【知识点：进制转换与进制字面量】

答案：正确

解析：题干表述成立。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第7题

该段程序将 $ n $ 不停地除以 2，并输出此时的商和余数，直到 $ n = 0 $ 为止。

```cpp
long long n;
cin >> n;
while(n != 0) {
    cout << n/2 << ' ' << n%2 << '\n';
    n /= 2;
}
```

【知识点：控制结构与程序阅读】

答案：正确

解析：题干表述成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第8题

两个 13 进制的数 A 和 B，在 10 进制下分别表示 10 和 11。$ (A + B)_{13} = (18)_{13} $，也就是说 13 进制数 A 加上 13 进制数 B，和是 13 进制数 18。

【知识点：进制转换与进制字面量】

答案：正确

解析：题干表述成立。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第9题

$ k $ 进制，逢 $ k $ 进第二位，$ k^2 $ 进百位，$ k^3 $ 进千位。

【知识点：位运算】

答案：错误

解析：题干表述不成立。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第10题

$ \text{CCF}$（十九进制）$= 21\text{AC}$（十三进制）（不区分大小写）

【知识点：计算机基础常识】

答案：错误

解析：题干表述不成立。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。





