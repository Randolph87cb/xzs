# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1175
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1670868226801696.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

Base64 编码将每 3 字节的输入数据编码为 4 字节的输出数据。如果输入数据长度不是 3 的倍数，会用 = 号填充。在 Base64 编码中，如果输入字符串的长度为 10 字节，编码后的字符串长度是多少（ ）

【知识点：字符串处理】

A. 12 字节
B. 13 字节
C. 14 字节
D. 16 字节

答案：D

解析：D 项「16 字节」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

UTF-8 编码规则如下：

1 字节：0xxxxxxx

2 字节：110xxxxx 10xxxxxx

3 字节：1110xxxx 10xxxxxx 10xxxxxx

4 字节：11110xxx 10xxxxxx 10xxxxxx 10xxxxxx

以下哪个字节序列是合法的 UTF-8 编码（ ）

【知识点：进制转换与进制字面量】

A. 0xC0 0x80
B. 0xF0 0x90 0x80 0x80
C. 0x80 0x80 0x80
D. 0xFF 0xFE 0xFD

答案：B

解析：B 项「0xF0 0x90 0x80 0x80」符合题干要求。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

在 8 位二进制原码表示中，八进制数 -5 的二进制形式是什么（ ）

【知识点：原码反码补码与整数表示】

A. 10000101
B. 11111010
C. 11111011
D. 00000101

答案：A

解析：A 项「10000101」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

十进制数 $111.111$ 的二进制表示可以是下面的（ ）。

【知识点：进制转换与进制字面量】

A. 1101111.0001110001
B. 1101110.1001110001
C. 1101111.1001110001
D. 1101111.0011110001

答案：A

解析：A 项「1101111.0001110001」符合题干要求。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

在 C++ 中，补码的主要作用是（ ）

【知识点：原码反码补码与整数表示】

A. 提高浮点数的精度
B. 简化整数的加减法运算
C. 增加整数的表示范围
D. 优化内存分配

答案：B

解析：B 项「简化整数的加减法运算」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

在 C++ 中，一个 8 位有符号整数（使用补码表示）的范围是（ ）

【知识点：原码反码补码与整数表示】

A. -128 到 127
B. -127 到 128
C. -256 到 255
D. 0 到 255

答案：A

解析：A 项「-128 到 127」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

在 C++ 中，以下代码的输出是什么（ ）

```cpp
int a = -5;
unsigned int b = a;
cout << b;
```

【知识点：C++语法规则与编译模型】

A. -5
B. 5
C. 4294967291
D. 编译错误

答案：C

解析：C 项「4294967291」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

下列程序的作用是（ ）

```cpp
int main() {
    int decimal = 25;
    cout << oct << decimal;
    return 0;
}
```

【知识点：进制转换与进制字面量】

A. 将十进制数转换成八进制数
B. 将八进制数转换成十进制数
C. 将二进制数转换成八进制数
D. 将八进制数转换成 16 进制数

答案：A

解析：A 项「将十进制数转换成八进制数」符合题干要求。编码和进制题要按位权展开或按编码规则分组计算，不能把不同进制下的数字直接按十进制理解。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

下面程序目的是将十进制数 255 转换为十六进制输出，横线处应填写的语句是（ ）

```cpp
#include <iostream>
using namespace std;

int main() {
    int decimal = 255;
    _______________________
    return 0;
}
```

【知识点：进制转换与进制字面量】

A. cout << oct << decimal;
B. cout << decimal << decimal;
C. cout << hex << decimal;
D. 不能正确执行

答案：C

解析：C 项「cout << hex << decimal;」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

以下代码的说法正确的是什么（ ）

```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 0b1101;
    int b = 0b1011;
    cout << (a ^ b);
    return 0;
}
```

【知识点：位运算】

A. 进行的是整体异或运算
B. 进行的是按位同或运算
C. 进行的是按位与运算
D. 进行的是按位异或运算

答案：D

解析：D 项「进行的是按位异或运算」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

下面枚举法查找最大值索引程序中，横线处应该填写的是（ ）

```cpp
#include <iostream>
using namespace std;

int main() {
    int arr[] = {3, 7, 2, 9, 5};
    int maxIndex = 0;
    for (int i = 1; i < 5; i++) {
        _______________________
        {
            maxIndex = i;
        }
    }
    cout << maxIndex;
    return 0;
}
```

【知识点：数组与字符数组】

A. `if (arr[maxIndex] > arr[i])`
B. `if (arr[i]-1 > arr[maxIndex])`
C. `if (arr[i]+1 > arr[maxIndex])`
D. `if (arr[i] > arr[maxIndex])`

答案：D

解析：D 项「if (arr[i] > arr[maxIndex])」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

以下代码的功能是将数组中的奇数和偶数分别放在数组的前半部分和后半部分，横线处应该填入的是（ ）

```cpp
#include <iostream>
using namespace std;

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int left = 0, right = 4;
    while (left < right) {
        while (arr[left] % 2 == 1 && left < right) left++;
        _______________________
        if (left < right) {
            swap(arr[left], arr[right]);
        }
    }
    for (int i = 0; i < 5; i++) {
        cout << arr[i] << " ";
    }
    return 0;
}
```

【知识点：数组与字符数组】

A. `while (arr[left] % 2 == 0 && left < right) right--;`
B. `while (arr[right] % 2 == 0 && left < right) left--;`
C. `while (arr[right] % 2 != 0 && left < right) right--;`
D. `while (arr[right] % 2 == 0 && left < right) right--;`

答案：D

解析：D 项「while (arr[right] % 2 == 0 && left < right) right--;」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

下面程序最后能够得到 HelloC++ 的是（ ）

```cpp
int main() {
    string str = "HelloWorld";
    _______________________
    cout << str;
    return 0;
}
```

【知识点：字符串处理】

A. `str.replace(0, 5, "C++");`
B. `str.replace(5, 5, "C++");`
C. `str.replace(1, 5, "C++");`
D. `str.replace(4, 5, "C++");`

答案：B

解析：B 项「str.replace(5, 5, "C++");」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

想要得到字符串 world，下面程序横线处应该填入的是（）

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string str = "HelloC++";
    _____________
    _____________
    return 0;
}
```

【知识点：字符串处理】

A.
```cpp
str.insert(4, "World");
cout << str.substr(4, 4);
```

B.
```cpp
cout << str.substr(5, 5);
```

C.
```cpp
str.insert("World");
cout << str.substr(5, 5);
```

D.
```cpp
str.insert(5, "World");
cout << str.substr(5, 5);
```

A. A
B. B
C. C
D. D

答案：D

解析：D 项「代码片段」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

有 $n$ 个正整数，假设一个正整数是美丽数字当且仅当该正整数是 9 的倍数但不是 8 的倍数。下面的程序是编写计算 $n$ 个正整数中美丽数字的数量，横线处应该填入的是（ ）

```cpp
for (int i = 1; i <= n; i++) {
    cin >> a;
    _______________________
    cnt++;
}
```

【知识点：位运算】

A. `if (a % 9 != 0 && a % 8 != 0)`
B. `if (a % 9 == 0 && a % 8 == 0)`
C. `if (a % 9 == 0 && a % 8 != 0)`
D. `if (a % 9 == 0 & a % 8 != 0)`

答案：C

解析：C 项「if (a % 9 == 0 && a % 8 != 0)」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。





