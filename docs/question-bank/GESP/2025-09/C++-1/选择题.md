# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1189
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1703972987469856.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

人工智能现在非常火，小杨就想多了解一下，其中就经常听人提到“大模型”。那么请问这里说的“大模型”最贴切是指（ ）。

A. 大电脑模型
B. 大规模智能
C. 智能的单位
D. 大语言模型

答案：D

解析：D 项「大语言模型」符合题干要求。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

小杨这学期刚开学就选修了一门编程课，然后就想编写程序来计算 1 到 10001 之间的所有偶数的和。他希望程序采用简单累加整数的方法，同时希望源程序尽可能清晰、简洁，则下面关于实现累加时采用的主要控制结构的哪个说法最不合适（ ）。

A. 使用循环结构
B. 使用循环和分支的组合
C. 仅使用顺序结构
D. 不使用分支结构

答案：C

解析：C 项「仅使用顺序结构」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

下面的 C++ 代码用于输入姓名，然后输出姓名，正确的说法是（ ）。

```cpp
string XingMing;
cout << "请输入您的姓名：";
cin >> XingMing;
cout << XingMing;
```

A. XingMing 是汉语拼音，不能作为变量名称
B. 可以将 XingMing 改为 Xing Ming
C. 可以将 XingMing 改为 xingming
D. 可以将 XingMing 改为 Xing-Ming

答案：C

解析：C 项「可以将 XingMing 改为 xingming」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

下列 C++ 代码中 a 和 b 都是整型变量，执行后，其结果是（ ）。

```cpp
a = 13;
b = 5;
cout << a / b << a %// b << a % b;
a * b;
```

A. 2 3
B. 23
C. 20
D. 以上都不准确

答案：C

解析：C 项「20」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

C++ 表达式 `3 * 4 % 5 / 6` 的值是（ ）。

A. 10
B. 5
C. 2
D. 0

答案：D

解析：D 项「0」符合题干要求。表达式和控制结构题要按运算符优先级、整数除法、取余规则以及循环控制语句逐步执行。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

下面的 C++ 代码中变量 N 和 M 都是整型，则执行时如果先输入 10 并输入一个制表符后输入 20 并回车，其输出的数值是（ ）。

```cpp
scanf("%d", &N);
scanf("%d", &M);
printf("{%d}", N + M);
```

A. {30}
B. 1020
C. {N+M}
D. 不输出，继续等待输入

答案：A

解析：A 项「{30}」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

当前是 9 月，编写 C++ 代码求 N 个月后的月份。横线处应填入的代码是（ ）。

```cpp
int N, M;
cin >> N;
M = ___________;
if (M == 0)
    printf("%d个月后12月", N);
else
    printf("%d个月后是%d月", N, M);
```

A. `N % 12`
B. `9 + N % 12`
C. `(9 + N) / 12`
D. `(9 + N) % 12`

答案：D

解析：D 项「(9 + N) % 12」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int n = 0;
for (int i = 0; i < 100; i++)
    n += i % 2;
cout << n;
```

A. 5050
B. 4950
C. 50
D. 49

答案：C

解析：C 项「50」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

下面的 C++ 代码执行后输出是（ ）。

```cpp
int N = 0, i;
for (i = -100; i < 100; i++)
    N += i % 10;
cout << N;
```

A. 900
B. 100
C. 0
D. -100

答案：C

解析：C 项「0」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

下面 C++ 代码执行后输出是（ ）。

```cpp
int i;
for(i = 1; i < 5; i++){
    if(i % 3 == 0)
        break;
    printf("%d#",i);
}
if(i > 5) printf("END\n");
```

A. 1#2#
B. 1#2#END
C. 1#2
D. 1#2#3#4#END

答案：A

解析：A 项「1#2#」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

下面的 C++ 代码用于求 $N$ 的镜面数（$N$ 的个位到最高位的各位数字依次反过来出现在数字中，但高位 $0$ 将被忽略，不输出），如输入 $1234$，则将输出 $4321$，又如输入 $120$，则将输出 $21$，错误的选项是（ ）。

```cpp
cout << "请输入个位数不为0的正整数：";
cin >> N;
rst = 0; // 保存逆序结果
while (_____________){
    rst = rst * 10 + N % 10;
    N = N / 10;
}
cout << rst << endl;
```

A. `N != 0`
B. `not (N == 0)`
C. `N = 0`
D. `N > 0`

答案：C

解析：C 项「N = 0」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

下面 C++ 代码用于交换两个正整数 $a$ 和 $b$ 的值，不能实现交换的代码是（ ）。

A.
```cpp
cout << "输入第一个正整数：";
cin >> a;
cout << "输入第二个正整数：";
cin >> b;
temp = a;
a = b;
b = temp;
cout << "a=" << a << " b=" << b << endl;
```

B.
```cpp
cout << "输入第一个正整数：";
cin >> a;
cout << "输入第二个正整数：";
cin >> b;
b = a - b;
a = a - b;
b = a + b;
cout << "a=" << a << " b=" << b << endl;
```

C.
```cpp
cout << "输入第一个正整数：";
cin >> a;
cout << "输入第二个正整数：";
cin >> b;
a = a + b;
b = a - b;
a = a - b;
cout << "a=" << a << " b=" << b << endl;
```

D.
```cpp
cout << "输入第一个正整数：";
cin >> a;
cout << "输入第二个正整数：";
cin >> b;
a, b = b, a;
cout << "a=" << a << " b=" << b << endl;
```

A. A
B. B
C. C
D. D

答案：D

解析：D 项「代码片段」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

下面 C++ 代码用于获得正整数 $N$ 的第 $M$ 位数，约定个位数为第 1 位，如 $N$ 等于 $1234$，$M$ 等于 $2$，则输出 $3$。假设 $M$ 的值是大于等于 $1$ 且小于等于 $N$ 的位数。横线处应填入的代码是（ ）。

```cpp
int N, M, div=1;
cout << "请输入一个正整数：";
cin >> N;
cout << "请输入从右到左取第几位数：";
cin >> M;
for (int i = 0; i < (M - 1); i++) div *= 10;
cout << (_____________);
```

A. `N % div / 10`
B. `N / div / 10`
C. `N % div % 10`
D. `N / div % 10`

答案：D

解析：D 项「N / div % 10」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

下面 C++ 代码执行后输出是（ ）。

```cpp
num = 0;
while (num <= 5){
    num += 1;
    if (num == 3)
        continue;
    printf("%d#", num);
}
```

A. 1#2#4#5#6#
B. 1#2#4#5#6
C. 1#2#3#4#5#6#
D. 1#2#3#4#5#6

答案：A

解析：A 项「1#2#4#5#6#」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

下面 C++ 代码用于记录多个输入数中的最大数和最小数（输入 $-999$ 则输入结束），相关说法错误的是（ ）。

```cpp
cin >> now_num;
min_num = max_num = now_num;
while (now_num != -999){
    if (max_num < now_num)
        max_num = now_num;
    if (min_num > now_num)
        min_num = now_num;
    cin >> now_num;
}
cout << min_num << ' ' << max_num;
```

A. 程序运行时如果第一个数输入 $-999$，则输出将是 $-999$ $-999$
B. 程序输入过程中，如果输入的第一个数不是 $-999$，则如果待输入的数据中没有 $-999$，则程序能求出已输入整数中的最大数和最小数
C. 如果用于输入考试成绩，即成绩中不可能有 $-999$，则程序能求出已输入成绩中的最高成绩和最低成绩
D. 可以将 `cin >> now_num;` 移动到 `while (now_num != -999) {` 下面，结果不变

答案：D

解析：本题问不符合要求的一项，D 项「可以将 cin >> now_num; 移动到 while (now_num != -999) { 下面，结果不变」正是与题干规则冲突的说法或做法。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项能够满足题意或表述成立，因此应选 D。





