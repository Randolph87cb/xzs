# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1189
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1703972987469856.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

在集成开发环境里调试程序时，要注意不能修改源程序，因为如果修改，就要终止调试、关闭该文件并重新打开，才能再次开始调试。（ ）

答案：错误

解析：题干表述不成立。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第2题

执行 C++ 表达式 `10 % 0.5` 将报错，因为 `0.5` 所在位置只能是整数。（ ）

答案：正确

解析：题干表述成立。表达式和控制结构题要按运算符优先级、整数除法、取余规则以及循环控制语句逐步执行。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第3题

下面 C++ 代码执行后将输出 $9$。（ ）
```cpp
for (i = 0; i < 10; i++)
    break;
cout << i;
```

答案：错误

解析：题干表述不成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第4题

下面 C++ 代码执行后将输出 $55$。（ ）
```cpp
n = 0;
for (int i = 0; i > -10; i--)
    n = n + i * -1;
cout << n;
```

答案：错误

解析：题干表述不成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第5题

将下面 C++ 代码中的 L1 行的 $i = 0$ 修改为 $i = 1$，其输出与当前代码输出相同。（ ）
```cpp
cnt = 0;
for (int i = 0; i < 100; i++) // L1
    cnt += i;
cout << cnt;
```

答案：正确

解析：题干表述成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第6题

将下面 C++ 代码中的 `i < 10` 修改为 `i <= 10`，其执行后输出相同。（ ）
```cpp
int n, i;
n = i = 0;
while (i < 10){
    n += i;
    i += 1;
}
cout << n;
```

答案：错误

解析：题干表述不成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第7题

下面的 C++ 代码执行后将输出 $45$。（ ）
```cpp
int n, i;
n = i = 0;
while (i < 10){
    i += 1;
    n += i;
}
cout << n;
```

答案：错误

解析：题干表述不成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第8题

执行 C++ 代码 `cout << (12 + 12.12)` 将报错，因为 $12$ 是 `int` 类型，而 $12.12$ 是 `float` 类型，不同类型不能直接运算。（ ）

答案：错误

解析：题干表述不成立。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第9题

下面 C++ 代码执行时将导致无限循环（也称死循环）。（ ）
```cpp
int count = 0;
while (count < 5){
    count += 1;
    if (count == 3)
        continue;
    cout << count << ' ';
}
```

答案：错误

解析：题干表述不成立。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第10题

下列 C++ 代码用于求斐波那契数列，即第 1 个数为 $0$、第 2 个数为 $1$，从第三个数开始，依次是其前两个数之和。如果输入的值为大于 1 的正整数，该代码能实现所求。（ ）
```cpp
cin >> n;
a = 0, b = 1;
for (int j = 0; j < n; j++){
    cout << a << " ";
    b = b + a;
    a = b - a;
}
```

答案：正确

解析：题干表述成立。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。





