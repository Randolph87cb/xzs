# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1191
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1703975921385504.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

执行以下 C++ 代码后，`c` 的数值是（ ）。

```cpp
int a = 10, b = 3;
double c = a / b;
```

【知识点：运算符、表达式与类型转换】

A. 3.33333
B. 3.333
C. 3.0
D. 3.3

答案：C

解析：C 项「3.0」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

下列 C++ 表达式的结果为 `true` 的是（ ）。

【知识点：运算符、表达式与类型转换】

A. `(5 <= 5) && (7 < 5)`
B. `!(10 > 5)`
C. `(10 != 10) || (5 >= 3)`
D. `(5 == 3) && (4 > 2)`

答案：C

解析：C 项「(10 != 10) || (5 >= 3)」符合题干要求。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第3题

以下关于 C++ 数组的说法，错误的是（ ）。

【知识点：数组与字符数组】

A. 数组的下标通常从 0 开始。
B. `int arr[5];` 声明了一个包含 5 个整数的数组。
C. 数组的大小必须在编译时确定，不能使用变量定义大小。
D. 可以通过 `arr[5]` 来访问 `int arr[5];` 数组的最后一个元素。

答案：D

解析：本题问不符合要求的一项，D 项「可以通过 arr[5] 来访问 int arr[5]; 数组的最后一个元素。」正是与题干规则冲突的说法或做法。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项能够满足题意或表述成立，因此应选 D。



## 第4题

执行以下 C++ 代码后，变量 `sum` 的值是（ ）。

```cpp
int sum = 0;
for (int i = 1; i <= 5; i += 2) {
    sum += i;
    int sum=0;
}
```

【知识点：控制结构与程序阅读】

A. 6
B. 9
C. 15
D. 死循环

答案：B

解析：B 项「9」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

要正确定义一个返回两个整数中较大值的函数 `max`，应该使用（ ）。

【知识点：控制结构与程序阅读】

A. `void max(int a, int b) { return a > b ? a : b; }`
B. `int max(int a, int b) { if (a > b) return a; else return b; }`
C. `int max(a, b) { if (a > b) return a; else return b; }`
D. `void max(a, b) { cout << (a > b ? a : b); }`

答案：B

解析：B 项「int max(int a, int b) { if (a > b) return a; else return b; }」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

执行以下 C++ 代码后，数组 `arr` 的内容是（ ）。

```cpp
int arr[4] = {1, 2, 3};
arr[3] = arr[0] + arr[2];
```

【知识点：数组与字符数组】

A. {1, 2, 3, 3}
B. {1, 2, 3, 4}
C. {1, 2, 3, 5}
D. {1, 2, 3, 6}

答案：B

解析：B 项「{1, 2, 3, 4}」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

以下关于 C++ 函数的描述，正确的是（ ）。

【知识点：函数与标准库】

A. 函数必须要有参数。
B. 函数通过 `return` 语句只能返回一个值。但是可以通过很多间接的方式返回多个值。
C. `main` 函数可以被其他函数调用。
D. 函数的定义可以直接嵌套，即一个函数内部可以真正定义另一个函数。

答案：B

解析：B 项「函数通过 return 语句只能返回一个值。但是可以通过很多间接的方式返回多个值。」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

以下 C++ 代码 `count++` 执行的次数是（ ）。

```cpp
int i = 10;
int count=0;
while (i > 0) {
    i -= 3;
    continue;
    count++;
}
```

【知识点：控制结构与程序阅读】

A. 2
B. 3
C. 4
D. 0

答案：D

解析：D 项「0」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

以下 C++ 代码段的输出是（ ）。

```cpp
for (int i = 0; i < 4; i++) {
    for (int j = 0; j <= i; j++) {
        cout << j;
    }
    cout << "#";
}
```

【知识点：进制转换与进制字面量】

A. 0#01#012#0123#
B. 1#12#123#1234#
C. 0#1#2#3#
D. 0#01#012#012343#

答案：A

解析：A 项「0#01#012#0123#」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第10题

以下关于 C++ 变量作用域的说法，错误的是（ ）。

【知识点：控制结构与程序阅读】

A. 在 `for` 循环语句中声明的变量，其作用域仅限于该循环体内。
B. 在函数内部声明的变量（局部变量），仅在函数内部有效。
C. 在所有函数外部声明的变量，在整个程序中都有效。
D. 不同函数中的局部变量可以同名，它们代表不同的内存单元。

答案：C

解析：本题问不符合要求的一项，C 项「在所有函数外部声明的变量，在整个程序中都有效。」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 C。



## 第11题

关于以下代码的说法正确的是（ ）。

```cpp
int reversed = 0;
while (x != 0) {
    int digit = x % 10;
    x /= 10;
    reversed = reversed * 10 + digit;
}
```

【知识点：枚举与基础算法】

A. 能够反转任何位数的整数
B. 能够反转的最大位数正整数是 $2147483647$
C. 能够反转的最大位数正整数是 $2147483648$
D. 能够反转的最大位数正整数是 $1463847412$

答案：D

解析：D 项「能够反转的最大位数正整数是 1463847412」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

以下 C++ 代码试图查找数组中的最大值，划线处应填入（ ）。

```cpp
#include <iostream>
using namespace std;

int findMax(int arr[], int size) {
    int maxVal = _______; // 划线处
    for (int i = 1; i < size; i++) {
        if (arr[i] > maxVal) {
            maxVal = arr[i];
        }
    }
    return maxVal;
}
```

【知识点：数组与字符数组】

A. 0
B. `arr[-1]`
C. `arr[0]`
D. `size`

答案：C

解析：C 项「arr[0]」符合题干要求。图算法题要看存储方式和遍历规则：DFS 深入递归/栈，BFS 按层队列推进，邻接矩阵遍历邻点通常要扫描整行。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

以下关于 C++ 函数的说法，正确的是（ ）。

【知识点：函数与标准库】

A. 函数参数传递只有值传递一种方式。
B. 函数的形参在函数调用结束后依然占用内存空间
C. 没有返回值的函数必须声明为 `void` 类型，且不能包含 `return` 语句
D. C++11 及之后标准要求函数必须显式声明返回类型，不允许默认返回 `int`

答案：D

解析：D 项「C++11 及之后标准要求函数必须显式声明返回类型，不允许默认返回 int」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

以下 C++ 代码中存在几处错误（ ）。

```cpp
#include <iostream>
using namespace std;

int main() {
    const int SIZE = 5;
    int arr[SIZE];
    for (int i = 0; i <= SIZE; i++) {
        arr[i] = i * 2;
    }
    cout << arr[SIZE] << endl;
    return 0;
}
```

【知识点：数组与字符数组】

A. 0 处
B. 1 处
C. 2 处
D. 3 处

答案：C

解析：本题问不符合要求的一项，C 项「2 处」正是与题干规则冲突的说法或做法。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项能够满足题意或表述成立，因此应选 C。



## 第15题

以下关于 C++ 中 `string` 类和字符数组（`char[]`）的说法，错误的是（ ）。

【知识点：数组与字符数组】

A. `string` 对象可以使用 `=` 进行赋值，而字符数组需要使用 `strcpy`。
B. `string` 对象的长度可以使用 `length()` 成员函数获取，而字符数组需要使用 `strlen()` 函数。
C. `string` 对象在内存中是动态分配空间的，因此可以自动处理字符串长度的变化。
D. `string` 对象和字符数组都可以使用 `==` 运算符来直接比较两个字符串的内容是否相同。

答案：D

解析：本题问不符合要求的一项，D 项「string 对象和字符数组都可以使用 == 运算符来直接比较两个字符串的内容是否相同。」正是与题干规则冲突的说法或做法。C++ 基础语法题要依据标识符、关键字、数据类型、整型范围、ASCII 或补码规则判断。 其余选项能够满足题意或表述成立，因此应选 D。





