# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1193
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1704013600915488.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

基于下面定义的函数，通过判断 `isDivisibleBy9(n) == isDigitSumDivisibleBy9(n)` 代码可验算如果一个数能被 9 整除，则它的各位数字之和能被 9 整除。

```cpp
bool isDivisibleBy9(int n) {
    return n % 9 == 0;
}

bool isDigitSumDivisibleBy9(int n) {
    int sum = 0;
    string numStr = to_string(n);
    for (char c : numStr) {
        sum += (c - '0');
    }
    return sum % 9 == 0;
}
```

答案：正确

解析：题干表述成立。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第2题

假设函数 `gcd()` 能正确求两个正整数的最大公约数，则下面的 `findMusicalPattern(4, 6)` 函数返回 2。

```cpp
void findMusicalPattern(int rhythm1, int rhythm2) {
    int commonDivisor = gcd(rhythm1, rhythm2);
    int patternLength = (rhythm1 * rhythm2) / commonDivisor;
    return patternLength;
}
```

答案：错误

解析：题干表述不成立。数论题要抓住定义：欧几里得算法最终非零值为最大公约数，筛法通过标记合数得到质数，唯一分解只对大于 1 的整数按素数幂唯一分解。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第3题

下面递归实现的斐波那契数列的时间复杂度为 $O(2^n)$。

```cpp
long long fib_memo(int n, long long memo[]) {
    if (n <= 1) return n;
    if (memo[n] != -1) return memo[n];
    memo[n] = fib_memo(n - 1, memo) + fib_memo(n - 2, memo);
    return memo[n];
}

int main() {
    int n = 40;
    long long memo[100];
    fill_n(memo, 100, -1);
    long long result2 = fib_memo(n, memo);
    return 0;
}
```

答案：错误

解析：题干表述不成立。递归题要先看终止条件，再按递推式逐层计算；缺少终止条件会导致无限递归或栈溢出。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第4题

链表通过更改指针实现高效的结点插入与删除，但结点访问效率低、占用内存较多，且对缓存利用不友好。

答案：正确

解析：题干表述成立。计算机基础题要按概念本身判断：软件、硬件、网络范围、开发环境和人工智能工具的作用各不相同。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第5题

二分查找依赖数据的有序性，通过循环逐步缩减一半搜索区间来进行查找，且仅适用于数组或基于数组实现的数据结构。

答案：正确

解析：题干表述成立。二分查找依赖单调性：条件满足时收缩一侧区间，不满足时移动另一侧边界。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第6题

线性筛关键是“每个合数只会被最小质因子筛到一次”，因此为 $O(n)$。

答案：正确

解析：题干表述成立。数论题要抓住定义：欧几里得算法最终非零值为最大公约数，筛法通过标记合数得到质数，唯一分解只对大于 1 的整数按素数幂唯一分解。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第7题

快速排序和归并排序都是稳定的排序算法。

答案：错误

解析：题干表述不成立。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第8题

下面代码采用分治算法求解标准 3 柱汉诺塔问题，时间复杂度为 $O(n \log n)$。

```cpp
void move(vector<int> &src, vector<int> &tar) {
    int pan = src.back();
    src.pop_back();
    tar.push_back(pan);
}

void dfs(int n, vector<int> &src, vector<int> &buf, vector<int> &tar) {
    if (n == 1) {
        move(src, tar);
        return;
    }

    dfs(n - 1, src, tar, buf);
    move(src, tar);
    dfs(n - 1, buf, src, tar);
}

void solveHanota(vector<int> &A, vector<int> &B, vector<int> &C) {
    int n = A.size();
    dfs(n, A, B, C);
}
```

答案：错误

解析：题干表述不成立。复杂度题要统计循环执行次数或递推式的总规模，忽略常数和低阶项后得到渐近量级。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。



## 第9题

所有递归算法都可以转换为迭代算法。

答案：正确

解析：题干表述成立。递归题要先看终止条件，再按递推式逐层计算；缺少终止条件会导致无限递归或栈溢出。 将题干条件代入相关规则后不会产生矛盾，因此判断为正确。



## 第10题

贪心算法总能得到全局最优解。

答案：错误

解析：题干表述不成立。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 题干中的结论与实际规则或推导结果不一致，因此判断为错误。





