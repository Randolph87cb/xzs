# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1192
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1703973044092960.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

运行下面程序后变量 `a` 的值是（ ）。

```cpp
int a = 42;
int* p = &a;
*p = *p + 1;
```

A. 42
B. 43
C. 编译错误
D. 不确定

答案：B

解析：B 项「43」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第2题

以下关于数组的描述中，（ ）是错误的。

A. 数组名是一个指针常量
B. 随机访问数组的元素方便快捷
C. 数组可以像指针一样进行自增操作
D. `sizeof(arr)` 返回的是整个数组 `arr` 占用的字节数

答案：C

解析：本题问不符合要求的一项，C 项「数组可以像指针一样进行自增操作」正是与题干规则冲突的说法或做法。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项能够满足题意或表述成立，因此应选 C。



## 第3题

给定如下定义的数组 `arr`，则 `*(*(arr + 1) + 2)` 的值是（ ）。

```cpp
int arr[2][3] = {{1, 2, 3}, {4, 5, 6}};
```

A. 2
B. 5
C. 4
D. 6

答案：D

解析：D 项「6」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第4题

下面这段代码会输出（ ）。

```cpp
int add(int a, int b = 1); // 函数声明

int main() {
    cout << add(2) << " " << add(2, 3);
    return 0;
}

int add(int a, int b) { // 函数定义
    return a + b;
}
```

A. 3 5
B. 编译失败：定义处少了默认参数
C. 运行错误
D. 链接失败：未定义引用

答案：A

解析：A 项「3 5」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第5题

下面这段代码会输出（ ）。

```cpp
int x = 5;

void foo() {
    int x = 10;
    cout << x << " ";
}

void bar() {
    cout << x << " ";
}

int main() {
    foo();
    bar();
}
```

A. 5 5
B. 10 10
C. 5 10
D. 10 5

答案：D

解析：D 项「10 5」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第6题

下面程序运行的结果是（ ）。

```cpp
void increaseA(int x) {
    x++;
}

void increaseB(int* p) {
    (*p)++;
}

int main() {
    int a = 5;
    increaseA(a);
    cout << a << " ";
    increaseB(&a);
    cout << a;
}
```

A. 6 7
B. 6 6
C. 5 6
D. 5 5

答案：C

解析：C 项「5 6」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第7题

关于结构体初始化，以下哪个选项中正确的是（ ）。

```cpp
struct Point {int x,y;};
```

A. `Point p = (1,2);`
B. `Point p = {1,2};`
C. `Point p = new {1,2};`
D. `Point p = <1,2>;`

答案：B

解析：B 项「Point p = {1,2};」符合题干要求。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第8题

运行如下代码会输出（ ）。

```cpp
struct Cat {
    string name;
    int age;
};

void birthday(Cat& c) {
    c.age++;
}

int main() {
    Cat kitty{"Mimi", 2};
    birthday(kitty);
    cout << kitty.name << " " << kitty.age;
}
```

A. Mimi 2
B. Mimi 3
C. kitty 3
D. kitty 2

答案：B

解析：B 项「Mimi 3」符合题干要求。输入输出题要注意格式控制符、转义字符、整数除法和输出语句中字面量与变量的区别。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第9题

关于排序算法的稳定性，以下说法错误的是（ ）。

A. 稳定的排序算法不改变相等元素的相对位置
B. 冒泡排序是稳定的排序算法
C. 选择排序是稳定的排序算法
D. 插入排序是稳定的排序算法

答案：C

解析：本题问不符合要求的一项，C 项「选择排序是稳定的排序算法」正是与题干规则冲突的说法或做法。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项能够满足题意或表述成立，因此应选 C。



## 第10题

下面代码试图实现选择排序，使其能对数组 `nums` 排序为升序，则横线上应分别填写（ ）。

```cpp
void selectionSort(vector<int>& nums) {
    int n = nums.size();
    for (int i = 0; i < n - 1; ++i) {
        int minIndex = i;
        for (int j = i + 1; j < n; ++j) {
            if (__________) {  // 在此处填入代码
                minIndex = j;
            }
        }
        ______________; // 在此处填入代码
    }
}
```

A.  
```cpp
nums[j] < nums[minIndex]
swap(nums[i], nums[minIndex])
```

B.  
```cpp
nums[j] > nums[minIndex]
swap(nums[i], nums[minIndex])
```

C.  
```cpp
nums[j] <= nums[minIndex]
swap(nums[j], nums[minIndex])
```

D.  
```cpp
nums[j] <= nums[minIndex]
swap(nums[i], nums[j])
```

A. A
B. B
C. C
D. D

答案：A

解析：A 项「代码片段」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第11题

下面程序实现插入排序（升序排序），则横线上应分别填写（ ）。

```cpp
void insertionSort(int arr[], int n) {
    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;
        while (j >= 0 && ________________) { // 在此处填入代码
            arr[j + 1] = arr[j];
            j--;
        }
        ________________; // 在此处填入代码
    }
}
```

A.  
```cpp
arr[j] > key
arr[j + 1] = key
```

B.  
```cpp
arr[j] < key
arr[j + 1] = key
```

C.  
```cpp
arr[j] > key
arr[j] = key
```

D.  
```cpp
arr[j] < key
arr[j] = key
```

A. A
B. B
C. C
D. D

答案：A

解析：A 项「代码片段」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第12题

关于插入排序的时间复杂度，下列说法正确的是（ ）。

A. 最好情况和最坏情况的时间复杂度都是 $O(n^2)$
B. 最好情况是 $O(n)$，最坏情况是 $O(n^2)$
C. 最好情况是 $O(n)$，最坏情况是 $O(2^n)$
D. 最好情况是 $O(n^2)$，最坏情况是 $O(2^n)$

答案：B

解析：B 项「最好情况是 O(n)，最坏情况是 O(n^2)」符合题干要求。排序题要区分算法性质：快速排序平均 $O(n\log n)$ 但通常不稳定，归并排序稳定且合并有序子区间，冒泡和插入排序通常稳定，选择排序通常不稳定。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第13题

小杨正在爬楼梯，需要 $n$ 阶才能到达楼顶，每次可以爬 $1$ 阶或 $2$ 阶，求小杨有多少种不同的方法可以爬到楼顶，横线上应填写（ ）。

```cpp
int climbStairs(int n) {
    if (n <= 2) return n;
    int prev2 = 1;
    int prev1 = 2;
    int current = 0;
    for (int i = 3; i <= n; ++i) {
        ________________ // 在此处填入代码
    }
    return current;
}
```

A.  
```cpp
prev2 = prev1;
prev1 = current;
current = prev1 + prev2;
```

B.  
```cpp
current = prev1 + prev2;
prev2 = prev1;
prev1 = current;
```

C.  
```cpp
current = prev1 + prev2;
prev1 = current;
prev2 = prev1;
```

D.  
```cpp
prev1 = current;
prev2 = prev1;
current = prev1 + prev2;
```

A. A
B. B
C. C
D. D

答案：B

解析：B 项「代码片段」符合题干要求。计数题要先区分是否有顺序、是否可重复、是否互斥，再用乘法原理、加法原理或组合数计算。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第14题

假设有一个班级的成绩单，存储在一个长度为 $n$ 的数组 `scores` 中，每个元素是一个学生的分数。老师想要找出所有满足 `scores[i] + scores[j] + scores[k] == 300` 的三元组，其中 $i < j < k$。下面代码实现该功能，请问其时间复杂度是（ ）。

```cpp
int cnt = 0;
for (int i = 0; i < n; i++) {
    for (int j = i + 1; j < n; j++) {
        for (int k = j + 1; k < n; k++) {
            if (scores[i] + scores[j] + scores[k] == 300) {
                cnt++;
            }
        }
    }
}
```

A. $O(n)$
B. $O(n^2)$
C. $O(n^3)$
D. $O(2^n)$

答案：C

解析：C 项「O(n^3)」符合题干要求。数组和字符串题要注意下标从 0 开始、数组名和元素含义，以及字符串函数的返回值定义。 其余选项要么改变了关键条件，要么与 C++ 语法、算法性质或计算结果不一致，因此排除。



## 第15题

关于异常处理，以下说法错误的是（ ）。

A. `try` 块中的代码可能会抛出异常
B. `catch` 块可以有多个，处理不同类型的异常
C. `throw` 语句用于抛出异常
D. 所有异常都必须被捕获，否则程序会崩溃

答案：D

解析：本题问不符合要求的一项，D 项「所有异常都必须被捕获，否则程序会崩溃」正是与题干规则冲突的说法或做法。解题时应把题干条件逐项代入，按 C++ 语法、算法定义或数学公式推导结论。 其余选项能够满足题意或表述成立，因此应选 D。





