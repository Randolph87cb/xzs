# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1152
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1621071528722464.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

下列代码中，输出结果是（ ）
```cpp
#include<iostream>
using namespace std;
int func(int x,int y)
{
    int a=x,b=y;
    int t;
    t=a;
    a=b;
    b=t;
    cout<<a<<" "<<b<<" ";
}
int main()
{
    int c,d;
    c=12;
    d=24;
    func(12,24);
    cout<<c<<" "<<d<<endl;
}

A. 12 24 24 12
B. 24 12 12 24
C. 12 12 24 24
D. 24 24 12 12

答案：B

解析：本题答案选 B。选项 B 的内容「24 12 12 24」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第2题

下面函数不能正常执行的是（ ）

![](https://cdn.luogu.com.cn/upload/image_hosting/t9bm0grf.png)


A. A
B. B
C. C
D. D

答案：B

解析：本题答案选 B。选项 B 的内容「B」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第3题

下面程序输出的是（ ）

```cpp
#include<iostream>
using namespace std;
int func();
int main()
{
    int i=2;
    cout<<i<<endl;
    for(int x=0;x<1;x++)
    {
        int i=10;
        cout<<i<<endl;
    }
    i=i+1;
    cout<<i<<endl;
    {
        i=i*i;
        cout<<i<<endl;
    }
}
```

A. 2 2 3 9
B. 2 10 3 9
C. 2 10 11 121
D. 2 10 3 100

答案：B

解析：本题答案选 B。选项 B 的内容「2 10 3 9」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第4题

假设变量 `a` 的地址是 0x6ffe14，下面程序的输出是（ ）。
```cpp
#include<iostream>
using namespace std;
int main()
{
    int *p;
    int a=10;
    p=&a;
    p++;
    cout<<p<<endl;
}
```

A. 10
B. 0x6ffe14
C. 0x6ffe15
D. 0x6ffe18

答案：D

解析：本题答案选 D。选项 D 的内容「0x6ffe18」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第5题

如果下列程序输出的地址是 0x6ffe00，则 `cout<<a+1<<endl;` 输出的是（ ）

```cpp
#include<iostream>
using namespace std;
int main()
{
    int a[2][3]={0};
    cout<<a<<endl;
}
```

A. 0x6ffe04
B. 0x6ffe0C
C. 0x6ffe08
D. 0x6ffe00

答案：B

解析：本题答案选 B。选项 B 的内容「0x6ffe0C」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第6题

C++ 中，关于文件路径说法错误的是（ ）

A. "GESP.txt"：指定与当前工作目录中的程序文件相同目录中的 GESP.txt 文件
B. "../data/GESP.txt"：指定与当前工作目录中的程序文件上一级目录下的 data 目录中的 GESP.txt 文件
C. "./data/GESP.txt"：指定与当前工作目录中的程序文件同级目录下的 data 目录中的 GESP.txt 文件
D. "GESP.txt"是绝对路径

答案：D

解析：本题答案选 D。选项 D 的内容「"GESP.txt"是绝对路径」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第7题

关于直接插入排序，下列说法错误的是（ ）

A. 插入排序的最好情况是数组已经有序，此时只需要进行 $n - 1$ 次比较，时间复杂度为 $O(n)$
B. 最坏情况是数组逆序排序，此时需要进行 $n(n - 1)/2$ 次比较以及 $n - 1$ 次赋值操作（插入）
C. 平均来说插入排序算法的复杂度为 $O(n^2)$
D. 空间复杂度上，直接插入法是就地排序，空间复杂度为 $O(n)$

答案：D

解析：本题答案选 D。选项 D 的内容「空间复杂度上，直接插入法是就地排序，空间复杂度为 $O(n)$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第8题

下列程序横线处，应该输入的是（ ）。

```cpp
#include<iostream>
using namespace std;
int n,a[10001];
void swap(int &a,int &b)
{
    int t=a;
    a=b;
    b=t;
}
int main()
{
    cin>>n;
    for(int i=1;i<=n;i++)
        cin>>a[i];
    for(int i=n;i>1;i--)
        for(int j=1;j<i;j++)
            if(a[j]>a[j+1])
                ;
    for(int i=1;i<=n;i++)
        cout<<a[i]<<" ";
    cout<<endl;
    return 0;
}
```

A. swap(a[j],a[j+1]);
B. swap(a[j-1],a[j]);
C. swap(a[j-1],a[j+1]);
D. swap(&a[j-1],&a[j+1]);

答案：A

解析：本题答案选 A。选项 A 的内容「swap(a[j],a[j+1]);」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第9题

下面关于递推的说法不正确的是（ ）。

A. 递推表现为自己调用自己
B. 递推是从简单问题出发，一步步的向前发展，最终求得问题。是正向的
C. 递推中，问题的 $n$ 要求是在计算中确定，不要求计算前就知道 $n$
D. 斐波那契数列可以用递推实现求解

答案：A

解析：本题答案选 A。选项 A 的内容「递推表现为自己调用自己」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第10题

关于几种排序算法的说法，下面说法错误的是（ ）。

A. 选择排序不是一个稳定的排序算法
B. 冒泡排序算法不是一种稳定的排序算法
C. 插入排序是一种稳定的排序算法
D. 如果排序前2个相等的数在序列中的前后位置顺序和排序后它们2个的前后位置顺序相同，则称为一种稳定的排序算法

答案：B

解析：本题答案选 B。选项 B 的内容「冒泡排序算法不是一种稳定的排序算法」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第11题

数组 $\{45,66,23,1,10,97,52,88,5,33\}$ 进行从小到大冒泡排序过程中，第一遍冒泡过后的序列是（ ）。

A. $\{45,23,1,10,66,52,88,5,33,97\}$
B. $\{45,66,1,23,10,97,52,88,5,33\}$
C. $\{45,66,23,1,10,52,88,5,33,97\}$
D. $\{45,66,23,1,10,97,52,88,33,5\}$

答案：A

解析：本题答案选 A。选项 A 的内容「$\{45,23,1,10,66,52,88,5,33,97\}$」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第12题

下面的排序算法程序中，横线处应该填入的是（ ）。
```cpp
int a[8]={ 2,3, 4, 5, 6,2,3,1};
for (int i=1;i<8;i++)
{
    int key = a[i];
    int j=i-1;
    while(a[j]>key && j>=0)
    {
        ______;
        j -= 1;
    }
    a[j + 1]= key;
}
```

A. a[j]=a[j-1];
B. a[j]=a[j+1];
C. a[j+1]=a[j-1];
D. a[j+1]=a[j];

答案：D

解析：本题答案选 D。选项 D 的内容「a[j+1]=a[j];」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第13题

下面的程序中，如果输入 10 0 ，会输出（ ）。

```cpp
#include<iostream>
using namespace std;

double Division(int a, int b)
{
    if (b == 0)
        throw "Division by zero condition!";
    else
        return ((double)a / (double)b);
}

void func()
{
    int len, time;
    cin >> len >> time;
    cout << Division(len, time) << endl;
}

int main()
{
    try {
        func();
    }
    catch (const char* errmsg)
    {
        cout << errmsg << endl;
    }
    catch (const int errmsg)
    {
        cout << errmsg << endl;
    }
    return 0;
}
```

A. Division by zero condition!
B. 0
C. 10
D. 100

答案：A

解析：本题答案选 A。选项 A 的内容「Division by zero condition!」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第14题

10 条直线，最多可以把平面分为多少个区域（ ）。

A. 55
B. 56
C. 54
D. 58

答案：B

解析：本题答案选 B。选项 B 的内容「56」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第15题

下面程序中，如果语句 `cout<<p<<endl;` 输出的是 `0x6ffe00`，则 `cout<<++p<<endl;` 输出的是（ ）

```cpp
int x[10][10][10]={{0}};
int *p;
p=&x[0][0][0];
cout<<p<<endl;
cout<<++p<<endl;
```

A. 0x6ffe0c
B. 0x6ffe09
C. 0x6ffe06
D. 0x6ffe04

答案：D

解析：本题答案选 D。选项 D 的内容「0x6ffe04」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。