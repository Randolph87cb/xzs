# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1150
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1621071434350624.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

小杨父母带他到某培训机构给他报名参加 CCF 组织的 GESP 认证考试的第 1 级，那他可以选择的认证语言有几种？（ ）

A. 1
B. 2
C. 3
D. 4

答案：C

解析：本题答案选 C。选项 C 的内容「3」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第2题

下面流程图在 yr 输入 2024 时，可以判定 yr 代表闰年，并输出 2 月是 29 天，则图中菱形框中应该填入（ ）。

![](https://cdn.luogu.com.cn/upload/image_hosting/shumi9qb.png)

A. `(yr%400==0) || (yr%4==0)`
B. `(yr%400==0) || (yr%4==0 && yr%100!=0)`
C. `(yr%400==0) && (yr%4==0)`
D. `(yr%400==0) && (yr%4==0 && yr%100!=0)`

答案：B

解析：本题答案选 B。选项 B 的内容「`(yr%400==0) || (yr%4==0 && yr%100!=0)`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第3题

在 C++ 中，下列不可做变量的是（ ）。

A. `five-Star`
B. `five_star`
C. `fiveStar`
D. `_fiveStar`

答案：A

解析：本题答案选 A。选项 A 的内容「`five-Star`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第4题

在 C++ 中，与 `for(int i=0; i<10; i++)` 效果相同的是（ ）。

A. `for(int i=0; i<10; i+=1)`
B. `for(int i=1; i<=10; i++)`
C. `for(int i=10; i>0; i--)`
D. `for(int i=10; i<1; i++)`

答案：A

解析：本题答案选 A。选项 A 的内容「`for(int i=0; i<10; i+=1)`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第5题

在 C++ 中，`cout << (5 % 2 && 5 % 3)` 的输出是（ ）。

A. `1`
B. `2`
C. `true`
D. `false`

答案：A

解析：本题答案选 A。选项 A 的内容「`1`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第6题

执行下面的 C++ 代码时输入 1，则输出是（ ）。

```cpp
int month;

cin >> month;

switch(month){
    case 1:
        cout << "Jan ";
    case 3:
        cout << "Mar ";
        break;
    default:
        ;
}
```

A. Jan
B. Mar
C. Jan Mar
D. 以上均不对

答案：C

解析：本题答案选 C。选项 C 的内容「Jan Mar」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第7题

执行下面 C++ 代码后，有关说法错误的是（ ）。

```cpp
int a, b;
cin >> a >> b;
if (a && b)
    cout << "1";
else if (!(a || b))
    cout << "2";
else if (a || b)
    cout << "3";
else
    cout << "4";
```

A. 如果先后输入 1 和 1，则将输出 1
B. 如果先后输入 0 和 1 或者 1 和 0，则将输出 3
C. 如果先后输入 0 和 0，则将输出 2
D. 如果先后输入 0 和 0，则将输出 4

答案：D

解析：本题答案选 D。选项 D 的内容「如果先后输入 0 和 0，则将输出 4」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第8题

某货币由 5 元、2 元和 1 元组成。输入金额（假设为正整数），计算出最少数量。为实现其功能，横线处应填入代码是（ ）。

```cpp
int N;
cin >> N;

int M5, M2, M1;
M5 = N / 5;
M2 = _______________;
M1 = _______________;
printf("5*%d+2*%d+1*%d", M5, M2, M1);
```

A. 第 1 横线处应填入：`N / 2`  第 2 横线处应填入：`N - M5 - M2`
B. 第 1 横线处应填入：`(N - M5 * 5) / 2`  第 2 横线处应填入：`N - M5 * 5 - M2 * 2`
C. 第 1 横线处应填入：`N - M5 * 5 / 2`  第 2 横线处应填入：`N - M5 * 5 - M2 * 2`
D. 第 1 横线处应填入：`(N - M5 * 5) / 2`  第 2 横线处应填入：`N - M5 - M2`

答案：B

解析：本题答案选 B。选项 B 的内容「第 1 横线处应填入：`(N - M5 * 5) / 2` 第 2 横线处应填入：`N - M5 * 5 - M2 * 2`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第9题

下面 C++ 代码执行后的输出是（ ）。

```cpp
int loopCount = 0;
for (int i=0; i < 10; i++)
    for (int j=1; j < i; j++)
        loopCount += 1;
cout << loopCount;
```

A. 55
B. 45
C. 36
D. 28

答案：C

解析：本题答案选 C。选项 C 的内容「36」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第10题

面 C++ 代码执行后的输出是（ ）。

```cpp
int loopCount = 0;
for (int i=0; i < 10; i++){
    for (int j=0; j < i; j++)
        if (i * j % 2)
            break;
    loopCount += 1;
}
cout << loopCount;
```

A. 25
B. 16
C. 10
D. 9

答案：C

解析：本题答案选 C。选项 C 的内容「10」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第11题

假设下面 C++ 代码执行过程中仅输入正负整数或 0，有关说法错误的是（ ）。

```cpp
int N, Sum = 0;
cin >> N;
while (N){
    Sum += N;
    cin >> N;
}
cout << Sum;
```

A. 执行上面代码如果输入 0，将终止循环
B. 执行上面代码能实现所有非 0 整数的求和
C. 执行上面代码第一次输入 0，最后将输出 0
D. 执行上面代码将陷入死循环，可将 `while (N)` 改为 `while (N==0)`

答案：D

解析：本题答案选 D。选项 D 的内容「执行上面代码将陷入死循环，可将 `while (N)` 改为 `while (N==0)`」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第12题

执行下面的 C++ 代码，有关说法正确的是（ ）【质数是指仅能被 1 和它本身整除的正整数】。

```cpp
int N;
cin >> N;
bool Flag = true;
for (int i = 2; i < N; i++){
    if (i * i > N)
        break;
    if (N % i == 0){
        Flag = false;
        break;
    }
}
if (Flag)
    cout << N << "是质数" << endl;
else
    cout << N << "不是质数" << endl;
```

A. 如果输入正整数，上面代码能正确判断 $N$ 是否为质数
B. 如果输入整数，上面代码能正确判断 $N$ 是否为质数
C. 如果输入大于等于 0 的整数，上面代码能正确判断 $N$ 是否为质数
D. 如将 `Flag = true` 修改为 `Flag = N>=2? true:false` 则能判断所有整数包括负整数、0、正整数是否为质数

答案：D

解析：本题答案选 D。选项 D 的内容「如将 `Flag = true` 修改为 `Flag = N>=2? true:false` 则能判断所有整数包括负整数、0、正整数是否为质数」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第13题

下面 C++ 代码用于实现如下图所示的效果，其有关说法正确的是（ ）。

```
1
2 4
3 6 9
4 8 12 16
5 10 15 20 25
```

```cpp
for (int i = 1; i < 6; i++) { // L1
    for (int j = 1; j < i+1; j++) // L2
        cout << i*j << " ";
    cout << endl;
}
```

A. 当前代码能实现预期效果，无需调整代码
B. 如果 `cout << endl;` 移到循环 L2 内部，则可实现预期效果
C. 如果 `cout << endl;` 移到循环 L1 外部，则可实现预期效果
D. 删除 `cout << endl;` 行，则可实现预期效果

答案：A

解析：本题答案选 A。选项 A 的内容「当前代码能实现预期效果，无需调整代码」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第14题

下面 C++ 代码执行后，输出是（ ）。

```cpp
int cnt1 = 0, cnt2 = 0;
for (int i = 0; i < 10; i++){
    if (i % 2 == 0)
        continue;
    if (i % 2)
        cnt1 += 1;
    else if (i % 3 == 0)
        cnt2 += 1;
}
cout << cnt1 << " " << cnt2;
```

A. 5 2
B. 5 0
C. 0 2
D. 0 0

答案：B

解析：本题答案选 B。选项 B 的内容「5 0」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。

## 第15题

在下面的 C++ 代码中，$N$ 必须是小于 $10$ 大于 $1$ 的整数，$M$ 为正整数（大于 $0$）。如果 $M$ 被 $N$ 整除则 $M$ 为幸运数，如果 $M$ 中含有 $N$ 且能被 $N$ 整除，则为超级幸运数，否则不是幸运数。程序用于判断 $M$ 是否为幸运数或超级幸运数或非幸运数。阅读下面代码，有关说法正确的是（ ）。

```cpp
int N, M;
cout << "请输入幸运数字：";
cin >> N;
cout << "请输入正整数：";
cin >> M;

bool Lucky;
if (M % N == 0)
    Lucky = true;
else
    Lucky = false;
while (M){
    if (M % 10 == N && Lucky){
        printf("%d是%d的超级幸运数!", M, N);
        break;
    }
    M /= 10;
}
if (M == 0)
    if (Lucky)
        printf("%d是%d的幸运数!", M, N);
    else
        printf("%d非%d的幸运数!", M, N);
```

A. 如果 $N$ 输入 $3$，$M$ 输入 $36$ 则将输出：$36$ 是 $3$ 的超级幸运数！
B. 如果 $N$ 输入 $7$，$M$ 输入 $21$ 则将输出：$21$ 是 $7$ 的幸运数！
C. 如果 $N$ 输入 $8$，$M$ 输入 $36$ 则将输出：$36$ 非 $8$ 的超级幸运数！
D. 如果 $N$ 输入 $3$，$M$ 输入 $63$ 则将输出：$63$ 是 $3$ 的超级幸运数！

答案：D

解析：本题答案选 D。选项 D 的内容「如果 $N$ 输入 $3$，$M$ 输入 $63$ 则将输出：$63$ 是 $3$ 的超级幸运数！」与题干要求一致。解题时应按题干给出的表达式、代码流程或概念定义逐步推导；其他选项与实际结果、语法规则或概念含义不符，因此排除。