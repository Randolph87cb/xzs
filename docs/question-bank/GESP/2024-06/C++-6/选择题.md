# 选择题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1154
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1621071589539872.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

面向对象的编程思想主要包括（ ）原则。

A. 贪心、动态规划、回溯
B. 并发、并行、异步
C. 递归、循环、分治
D. 封装、继承、多态

答案：D

解析：面向对象的核心思想是把数据和操作封装到对象中，通过继承复用和扩展已有类，并用多态让同一接口在不同对象上表现出不同实现。A、C 是算法设计方法，B 是程序执行方式，不属于面向对象三大基本原则。

## 第2题

运行下列代码，屏幕上输出（ ）。

```cpp
#include <iostream>
using namespace std;
class my_class {
	public:
		static int count;
		my_class() {
			count++;
		}
		~my_class() {
			count--;
		}
		static void print_count() {
			cout << count << " ";
		}
};
int my_class::count = 0;
int main() {
	my_class obj1;
	my_class::print_count();
	my_class obj2;
	obj2.print_count();
	my_class obj3;
	obj3.print_count();
	return 0;
}
```

A. 1 1 1
B. 1 2 3
C. 1 1 2
D. 1 2 2

答案：B

解析：`count` 是静态成员变量，所有 `my_class` 对象共享同一个计数。创建 `obj1` 后构造函数使 `count` 变为 1，创建 `obj2` 后变为 2，创建 `obj3` 后变为 3；三次输出发生在 `main` 结束、对象析构之前，所以输出 `1 2 3`。

## 第3题

运行下列代码，屏幕上输出（ ）。

```cpp
#include <iostream>
using namespace std;

class shape {
protected:
    int width, height;
public:
    shape(int a = 0, int b = 0) {
        width = a;
        height = b;
    }
    virtual int area() {
        cout << "parent class area: " << endl;
        return 0;
    }
};

class rectangle: public shape {
public:
    rectangle(int a = 0, int b = 0) : shape(a, b) { }

    int area() {
        cout << "rectangle area: ";
        return (width * height);
    }
};

class triangle: public shape {
public:
    triangle(int a = 0, int b = 0) : shape(a, b) { }

    int area() {
        cout << "triangle area: ";
        return (width * height / 2);
    }
};

int main() {
    shape *pshape;
    rectangle rec(10, 7);
    triangle tri(10, 5);

    pshape = &rec;
    pshape->area();

    pshape = &tri;
    pshape->area();
    return 0;
}
```

A. rectangle area: triangle area:
B. parent class area: parent class area:
C. 运行时报错
D. 编译时报错

答案：A

解析：基类 `shape` 的 `area()` 是 `virtual` 虚函数，`shape*` 指向 `rectangle` 时会动态绑定到 `rectangle::area()`，指向 `triangle` 时会动态绑定到 `triangle::area()`。代码没有输出返回值，只输出函数内部的提示文字，因此依次显示 `rectangle area: ` 和 `triangle area: `。

## 第4题

向一个栈顶为 `hs` 的链式栈中插入一个指针为 `s` 的结点时，应执行（ ）。

A. hs->next = s;
B. s->next = hs; hs = s;
C. s->next = hs->next; hs->next = s;
D. s->next = hs; hs = hs->next;

答案：B

解析：链式栈的入栈要把新结点放到原栈顶之前。先令 `s->next = hs`，让新结点指向原栈顶；再令 `hs = s`，把栈顶指针更新为新结点。若只改 `hs->next` 或把 `hs` 改成 `hs->next`，都会破坏“栈顶指向新结点”这一要求。

## 第5题

在栈数据结构中，元素的添加和删除是按照什么原则进行的？（ ）

A. 先进先出
B. 先进后出
C. 最小值先出
D. 随机顺序

答案：B

解析：栈只允许在栈顶进行插入和删除，后压入的元素位于更靠近栈顶的位置，会先被弹出。因此栈遵循“先进后出”（也可说“后进先出”）原则，而队列才是“先进先出”。

## 第6题

要实现将一个输入的十进制正整数转化为二进制表示，下面横线上应填入的代码为（ ）。
```cpp
#include <iostream>
using namespace std;

stack<int> ten2bin(int n) {
    stack<int> st;
    int r, m;

    r = n % 2;
    m = n / 2;
    st.push(r);

    while (m != 1) {
        r = m % 2;
        st.push(r);
        m = m / 2;
    }
    st.push(m);
    return st;
}

int main() {
    int n;
    cin >> n;
    stack<int> bin;
    bin = ten2bin(n);
    while (!bin.empty()) {
        __________________ // 在此处填入代码
    }
    return 0;
}
```

A. cout << bin.top(); bin.pop();
B. bin.pop(); cout << bin.top();
C. cout << bin.back(); bin.pop();
D. cout << bin.front(); bin.pop();

答案：A

解析：`ten2bin` 把每次除以 2 得到的余数压入栈，最后最高位在栈顶；输出时应先读取当前栈顶 `bin.top()`，再用 `bin.pop()` 弹出，直到栈空。题面可按已能使用 `stack` 理解，本题只考查 `top()` 和 `pop()` 的操作顺序，不必纠结代码中只展示了 `#include <iostream>`。若先 `pop()` 会漏掉最高位，`stack` 也没有 `front()`、`back()` 成员函数。

## 第7题

下面定义了一个循环队列的类，请补全判断队列是否满的函数，横向上应填写（ ）。

```cpp
#include <iostream>

using namespace std;

class circular_queue {
private:
    int *arr; // 数组用于存储队列元素
    int capacity; // 队列容量
    int front; // 队头指针
    int rear; // 队尾指针

public:
    circular_queue(int size) {
        capacity = size + 1; // 为了避免队列满时与队列空时指针相等的情况，多预留一个空间
        arr = new int[capacity];
        front = 0;
        rear = 0;
    }

    ~circular_queue() {
        delete[] arr;
    }

    bool is_empty() {
        return front == rear;
    }

    bool is_full() {
        // 在此处填入代码
    }

    void en_queue(int data) {
        if (is_full()) {
            cout << "队列已满，无法入队！" << endl;
            return -1;
        }
        arr[rear] = data;
        rear = (rear + 1) % capacity;
        return 1;
    }

    int de_queue() {
        if (is_empty()) {
            cout << "队列为空，无法出队！" << endl;
            return -1; // 出队失败，返回一个特殊值
        }
        int data = arr[front];
        front = (front + 1) % capacity;
        return data;
    }
};
```

A. `return (rear + 1) % capacity == front;`
B. `return rear % capacity == front;`
C. `return rear == front;`
D. `return (rear + 1) == front;`

答案：A

解析：这个循环队列预留了一个空位置，用来区分队空和队满。队满的条件不是 `rear == front`，而是队尾再前进一格就会追上队头，即 `(rear + 1) % capacity == front`；取模是为了处理 `rear` 位于数组末尾时回到下标 0 的情况。题面 `void en_queue` 中写 `return -1`、`return 1` 不严谨，但不影响本题判断；本题只考查 `is_full()` 的环形队列判满条件。

## 第8题

对“classmycls”使用哈夫曼（Huffman）编码，最少需要（ ）比特。

A. $10$
B. $20$
C. $25$
D. $30$

答案：C

解析：字符串 `classmycls` 中各字符频次为 `s:3`、`c:2`、`l:2`、`a:1`、`m:1`、`y:1`。哈夫曼树每次合并最小权值：`1+1=2`、`1+2=3`、`2+2=4`、`3+3=6`、`4+6=10`，最少比特数等于这些合并权值之和，即 `2+3+4+6+10=25`。

## 第9题

二叉树的（ ）第一个访问的节点是根节点。

A. 先序遍历
B. 中序遍历
C. 后序遍历
D. 以上都是

答案：A

解析：二叉树先序遍历的访问顺序是“根、左子树、右子树”，因此第一个访问的结点一定是根结点。中序遍历先访问左子树，后序遍历最后访问根结点，所以不能保证第一个访问根。

## 第10题

一棵 $5$ 层的满二叉树中节点数为（ ）。

A. $31$
B. $32$
C. $33$
D. $16$

答案：A

解析：满二叉树第 $i$ 层有 $2^{i-1}$ 个结点，5 层总数为 $1+2+4+8+16=31$，也可用公式 $2^5-1=31$ 计算。

## 第11题

在求解最优化问题时，动态规划常常涉及到两个重要性质，即最优子结构和（ ）。

A. 重叠子问题
B. 分治法
C. 贪心策略
D. 回溯算法

答案：A

解析：动态规划适合把问题拆成子问题求解，并保存子问题结果。它通常要求问题具有最优子结构，同时不同求解路径会反复遇到相同子问题，也就是“重叠子问题”；分治、贪心、回溯是其他算法思想。

## 第12题

青蛙每次能跳 $1$ 或 $2$ 步，下面代码计算青蛙跳到第 $n$ 步台阶有多少种不同跳法。则下列说法，错误的是（ ）。
```cpp
int jump_recur(int n) {
    if (n == 1) return 1;
    if (n == 2) return 2;
    return jump_recur(n - 1) + jump_recur(n - 2);
}

int jump_dp(int n) {
    vector<int> dp(n + 1); // 创建一个动态规划数组，用于保存已计算的值
    // 初始化前两个数
    dp[1] = 1;
    dp[2] = 2;
    // 从第三个数开始计算斐波那契数列
    for (int i = 3; i <= n; ++i) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}
```

A. 函数 `jump_recur()` 采用递归方式。
B. 函数 `jump_dp()` 采用动态规划方法。
C. 当 $n$ 较大时，函数 `jump_recur()` 存在大量重复计算，执行效率低。
D. 函数 `jump_recur()` 代码量小，执行效率高。

答案：D

解析：`jump_recur()` 直接调用自身计算 `n-1` 和 `n-2`，确实是递归；但它会反复计算同一个状态，例如 `jump_recur(n-2)` 会在多条递归路径中出现，效率随 $n$ 增大迅速下降。`jump_dp()` 用数组保存 `dp[i] = dp[i-1] + dp[i-2]`，避免重复计算，所以“递归代码量小，执行效率高”这一说法错误。

## 第13题

阅读以下二叉树的广度优先搜索代码:
```cpp
#include <iostream>
#include <queue>

using namespace std;

// 二叉树节点的定义
struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

// 宽度优先搜索（BFS）迭代实现
TreeNode* bfs(TreeNode* root, int a) {
    if (root == nullptr) return nullptr;

    queue<TreeNode*> q;
    q.push(root);

    while (!q.empty()) {
        TreeNode* node = q.front();
        q.pop();

        if (node->val == a)
            return node;

        cout << node->val << " "; // 先访问当前节点

        if (node->left) q.push(node->left); // 将左子节点入队
        if (node->right) q.push(node->right); // 将右子节点入队
    }
    return nullptr;
}
```
使用以上算法，在以下这棵树搜索数值 $20$ 时，可能的输出是（ ）。

![](https://cdn.luogu.com.cn/upload/image_hosting/sac4p2ik.png)

A. 5 2 -4 3 17 9
B. -4 2 3 5 9 17
C. 5 2 17 -4 3 9
D. 以上都不对

答案：C

解析：BFS 使用队列，按层从左到右搜索；代码先判断是否等于目标值，若不是才输出并把左右孩子入队。树中没有结点 `20`，搜索 20 时会依次比较并输出 `5 2 17 -4 3 9`，之后队列清空，函数返回 `nullptr`。

## 第14题

同上题中的二叉树，阅读以下二叉树的深度优先搜索代码:
```cpp
#include <iostream>
#include <stack>

using namespace std;

// 非递归深度优先搜索（DFS）
TreeNode* dfs(TreeNode* root, int a) {
    if (root == nullptr) return nullptr;

    stack<TreeNode*> stk;
    stk.push(root);

    while (!stk.empty()) {
        TreeNode* node = stk.top();
        stk.pop();
        if (node->val == a)
            return node;

        cout << node->val << " "; // 访问当前节点

        if (node->right) stk.push(node->right); // 先压入右子节点
        if (node->left) stk.push(node->left); // 再压入左子节点
    }
    return nullptr;
}
```

使用以上算法，在二叉树搜索数值 $20$ 时，可能的输出是（ ）。

A. 5 2 -4 3 17 9
B. -4 2 3 5 9 17
C. 5 2 17 -4 3 9
D. 以上都不对

答案：A

解析：DFS 使用栈，代码先压右孩子、再压左孩子，所以弹出时左孩子会先被访问，整体相当于“根、左、右”的先序搜索。树中没有结点 `20`，搜索 20 时会按顺序输出 `5 2 -4 3 17 9`，之后栈清空，函数返回 `nullptr`。

## 第15题

在上题的树中搜索数值 $3$ 时，采用深度优先搜索一共比较的节点数为（ ）。

A. $2$
B. $3$
C. $4$
D. $5$

答案：C

解析：沿用第 14 题的 DFS 顺序，搜索 3 时依次比较 `5`、`2`、`-4`、`3`。代码每弹出一个结点就先执行 `node->val == a` 的比较，比较到第 4 个结点 3 时命中并返回，所以一共比较 4 个结点。
