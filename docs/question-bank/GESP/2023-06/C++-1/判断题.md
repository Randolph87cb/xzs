# 判断题

来源：
- 洛谷有题页：https://ti.luogu.com.cn/problemset/1123
- 官网原卷 PDF：https://gesp.ccf.org.cn/101/attach/1569361525473312.pdf

说明：本文件优先采用洛谷结构化数据整理，并以官网 PDF 做交叉校对。

## 第1题

计算机硬件主要包括运算器、控制器、存储器、输入设备和输出设备。

答案：正确

解析：本题判断为正确。根据题目涉及的 C++ 语法、计算机基础或算法定义逐项核对，题干表述与标准规则一致。

## 第2题

诞生于 1958 年的 103 机是中国第一台通用数字电子计算机，比 1946 年在美国诞生的第一台通用电子计算机 ENIAC 晚了十多年。

答案：正确

解析：本题判断为正确。根据题目涉及的 C++ 语法、计算机基础或算法定义逐项核对，题干表述与标准规则一致。

## 第3题

在 C++ 语言中，计算结果必须存储在变量中才能输出。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第4题

在 C++ 语言中，标识符的命名不能完全由数字组成，至少有一个字母就可以。

答案：错误

解析：本题判断为错误。C++ 标识符可以包含字母、数字和下划线，不能以数字开头；下划线可以出现在标识符中，也可以作为开头。

## 第5题

10 是一个 int 类型常量。

答案：正确

解析：本题判断为正确。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第6题

if 语句可以没有 else 子句。

答案：正确

解析：本题判断为正确。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第7题

do ... while 语句的循环体至少会执行一次。

答案：正确

解析：本题判断为正确。按控制语句语义判断：if 可以没有 else，while/for 先判断条件，do while 先执行循环体一次，嵌套语句可用大括号明确范围。

## 第8题

如果 a 和 b 为 int 类型的变量，则表达式 `a = b` 可以判断 a 和 b 是否相等。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第9题

如果 a 为 int 类型的变量，则表达式 `(a % 4 == 2)` 可以判断 a 的值是否为偶数。

答案：错误

解析：本题判断为错误。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。

## 第10题

表达式 `(37 / 4)` 的计算结果为 9，且结果类型为 int。

答案：正确

解析：本题判断为正确。需要按 C++ 字面量和算术转换规则判断：字符常量、整型常量和浮点运算的结果类型不同，表达式不会随描述随意改变类型。
